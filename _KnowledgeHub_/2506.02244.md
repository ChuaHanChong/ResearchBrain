---
id: "2506.02244"
title: "Physics-Guided Motion Loss for Video Generation Model"
link: "https://www.alphaxiv.org/overview/2506.02244"
authors:
  - "Bowen Xue"
  - "Giuseppe Claudio Guarnera"
  - "Shuang Zhao"
  - "Zahra Montazeri"
tags:
  - video-generation
  - diffusion
  - video-understanding
  - generative-model
  - robustness
  - physics-aware
aliases:
  - "PGML"
  - "Physics-Guided-Motion-Loss"
---
# Physics-Guided Motion Loss for Video Generation Model

> [!summary] Summary
> PGML is a frequency-domain, model-free physics regularizer for video generation that computes a continuous scalar penalty on a clip by checking how well its 3D Fourier-spectrum signature matches the spectra implied by rigid translation, rotation, and scaling. It contains no neural network and no VLM judge — only a 3D FFT followed by closed-form spectral fits. Despite that simplicity, it improves motion accuracy by ~11% and reduces warping error by 22–37% across Open-Sora, MVDIT, and Hunyuan backbones, and is positioned as a drop-in tool for any video model that needs a physics-aware signal.

## Problem

Modern video diffusion models produce visually plausible but physically violating content: rubber-sheet deformations, inconsistent velocities, periodic flicker, and incorrect zoom/rotation. Existing fixes are either heavy (VLM judges, large reward models) or require architectural changes. The authors target a regularizer that (i) is model-agnostic, (ii) is differentiable end-to-end, (iii) avoids any pretrained scorer, and (iv) is cheap enough to apply during diffusion training.

## Method

The core insight is that ==rigid motions in pixel space have characteristic 3D Fourier signatures==. **PGML** decomposes general rigid motion into three sub-losses:

- ==Translational Loss==: pure translation in `(x, y)` produces an affine plane in `(ω_x, ω_y, ω_t)` spectral space. PGML fits a plane to the spectral energy and penalizes deviation.
- ==Rotational Loss==: rotation produces concentric annular energy distributions plus tilted-line concentration in `(m, ω_t)` space. PGML scores both annular concentration and tilt-ratio energy.
- ==Scaling Loss==: zooming produces radial-temporal gradients aligned with the spectral centroid trajectory; PGML uses radial gradient alignment plus centroid-trend regularization.

A ==temperature-based softmax adaptive weighting== combines the three based on which motion mode dominates the clip. Critically, PGML uses only **2.7%** of frequency coefficients while preserving **97%+** of spectral energy, making it cheap. The signal is differentiable, so it can act as both a training regularizer and a callable inference-time scorer.

## Results

- **+11%** average improvement in motion accuracy and action recognition on OpenVID-1M across **Open-Sora**, **MVDIT**, and **Hunyuan** backbones.
- **74–83%** user-study preference for PGML-regularized outputs vs baselines.
- **22–37%** reduction in warping error (backbone-dependent).
- Improved temporal consistency scores while preserving visual quality (no FVD regression reported).
- Generalizes across three architectures with no per-model tuning, validating the model-agnostic claim.

## Takeaways

PGML is a rare example of a physics-aware video signal that is small, differentiable, and grounded in classical signal processing rather than learned representations. It detects rigid-motion violations (non-constant velocity, rubber-sheet deformation, incorrect zoom/rotation) but does NOT directly catch contact-level violations (penetration, anti-gravity, object permanence) — its physics axis is "smooth rigid motion of objects in pixel space," not "object-state contact physics." That makes it complementary to (not a replacement for) contact/permanence-aware scorers, and structurally orthogonal to V-JEPA-2-style frozen-encoder surprise (which lives in latent space).

For world-model rollout reranking and reward signals on `(o_t, o_{t+1})` pairs, PGML is callable as `D_phys` if extended over a small temporal window (≥4 frames for FFT stability). It composes naturally with EVA's gripper-kinematic axis since both signals are scalar-valued and differentiable.

%%
## BibTeX

```bibtex
@misc{xue2025physicsguidedmotionlossvideo,
      title={Physics-Guided Motion Loss for Video Generation Model},
      author={Bowen Xue and Giuseppe Claudio Guarnera and Shuang Zhao and Zahra Montazeri},
      year={2025},
      eprint={2506.02244},
      archivePrefix={arXiv},
      primaryClass={cs.CV},
      url={https://arxiv.org/abs/2506.02244},
}
```
%%
