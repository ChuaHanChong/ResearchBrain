---
title: "Promising Research Directions: 3D/4D Spatial & Geometric Representation"
aliases:
  - "Spatial-4D"
  - "3D/4D Geometric Representation Directions"
tags:
  - research-directions
  - embodied-AI
  - 3D-understanding
  - spatial-reasoning
---

# Promising Research Directions: 3D/4D Spatial & Geometric Representation

> [!abstract] Overview
> Every policy, reasoner, and world model stands on *some* representation of the scene, and the field's default is RGB tokens — pixels a 2D backbone is hoped to turn into the geometry an action needs. The structural fact this doc bets on: **geometry is what the task keeps fixed; appearance is the noise on top.** A gripper, an object, and their contacts sit at metric 3D positions that do not move when the lighting, texture, or camera does — so a representation built around that geometry carries the action-relevant signal directly, and an RGB token leaves it implicit and pays to re-infer it every step.
> These **10 directions across 4 clusters** organize the bets by *where the explicit geometric state lives in the stack*: inside the action head (A), upstream of it as a 3D-grounded cognition layer (B), as the world-model and memory substrate (C), and as the reconstructed assets an agent acts in (D). The axis is **Mechanism** — the geometric representation itself, independent of which robot uses it.
> The non-consensus bet: the geometric channel, not the appearance channel, carries the action-relevant signal — and the gap to RGB *widens* exactly where geometry holds and pixels move (cross-embodiment, viewpoint shift, occlusion, long horizons). The field treats explicit 3D as overhead a big enough 2D model makes unnecessary; the directions here treat it as the thing the loss should be built around.

---

## Methodology

**Scope.** This doc reads the vault's 3D/4D-spatial and geometric-representation corpus in `_KnowledgeHub_/` — five structural surveys + benchmark suites (the Survey Landscape below) plus ~95 method papers — cross-checked against [[../General/02_Computer-Vision-and-3D|02_Computer-Vision-and-3D]] and [[../General/11_Robotics-and-Embodied-AI|11_Robotics-and-Embodied-AI]] and the deep-dives [[../Embodied-AI/07_Latent-World-Models|07_Latent-World-Models]], [[../Embodied-AI/08_Physics-Aware-Embodied-AI|08_Physics-Aware-Embodied-AI]], [[../Embodied-AI/05_VLA-Reasoning-and-CoT|05_VLA-Reasoning-and-CoT]], and [[../Embodied-AI/04_VLA|04_VLA]]. No date filter — 2024 (Video2Game) through 2026-Q2. It owns the *model-agnostic 3D/4D representation* — a geometric state any policy or model stands on. Sibling [[WAM|WAM]] owns *WAM machinery* (latent backbone, training, grounding), so the latent-4D substrate ([[2604.26694|X-WAM]], folded into C1's comparator scope) and persistent geometric memory ([[2603.17117|MosaicMem]], [[2603.24576|Chameleon-Episodic-Memory]], as C3) live here treated as *representations*; sibling [[Sim2Real|Sim2Real]] owns the *transfer gap* of 3DGS reconstruction ([[2604.25459|GS-Playground]], [[2511.04665|Real-to-Sim-GS]]), so Cluster D owns only *interaction-readiness*. Where a card borders a sibling, one clause states the delta.

---

## 3D/4D Spatial Survey Landscape

| Survey / Benchmark | The open problem it names (surveys) / what it measures (benchmarks) | Fuels |
|---|---|---|
| [[2506.20134\|3D-World-Models-Survey]] | Field is mid-transition "from 2D visual perception to comprehensive 3D spatial cognition"; explicit-3D action and forward-model interfaces remain rare and embodiment-specific | A1, A2, A3, B1, C1 |
| [[2510.16732\|World-Models-for-Embodied-AI-Survey]] | Spatial-representation axis is evolving latent → token → explicit 3D; explicit-geometry world models are nascent and rarely externally renderable | A1, A2, B2, C1, C2 |
| [[2604.22748\|Agentic-World-Modeling-Survey]] | The L2 Simulator must "compose multi-step rollouts that respect domain laws" — but geometric (not pixel) forward models are underbuilt and break over long horizons | A1, A2, B1, C1, C3 |
| [[2504.05786\|3D-Spatial-Reasoning-in-LLM-Survey]] | MLLM spatial reasoning is *ungrounded*: coordinate-metric reasoning lags object-naming; no consensus interface between language reasoning and metric 3D | A3, B1, B2 |
| [[2604.26509\|3D-Generation-for-Embodied-AI-Survey]] | "Simulation-readiness over visual fidelity" is the bottleneck; scarce physical annotations; geometry-vs-physical-validity trade-off; deformable assets unsolved | D1, D2 |
| [[2306.03310\|LIBERO]] | The 4-suite manipulation success-rate bar (spatial / object / goal / long); the standard ID surface where RGB backbones already saturate (~97%) and the geometry advantage must instead show on shift | A1, A3, B2, C1 |
| [[2406.02523\|RoboCasa]] | Kitchen-scale generalization-stress manipulation (24 tasks + unseen); the geometry-bound long-horizon suite where pixel/latent world models drift | A1, A2, C1, D1 |
| [[2601.13304\|CausalSpatial]] | MLLMs fail *causal* spatial reasoning (collision/occlusion/trajectory) and hallucinate when ungrounded; GPT-5 54.17% vs human 84.49%, low Not-Sure-Rate | B1, B2 |
| [[2605.29074\|Embodied3DBench]] | 13 SOTA VLMs, none robust on low-level metric 3D + interaction perception; score correlates with downstream LIBERO SR — the metric-3D gap is load-bearing for action | B1, B2 |
| [[2605.27367\|SpatialBench-SFM]] | Spatial-foundation-model benchmark (19 datasets, 31 models) exposing depth/pose gaps across density and egocentric views; the perception substrate upstream reasoning depends on | B1, A3 |
| [[2605.10921\|RoboMemArena]] | Robotic-memory benchmark (26 sim + 5 real); 68.9% of subtasks genuinely need history; reactive policies fail them | C3 |
| [[2608.08036\|RoboGraph]] (RoboGraphBench) | Compiles tasks into executable scene graphs and scores agents on task-state horizon — how far state must be maintained/explored/updated; open-source models collapse to 9.3%/0% where GPT-5.5 holds 78.4%/82.5% | B1, C3 |
| [[2603.04639\|RoboMME]] | Memory-by-representation benchmark: 14 memory-augmented VLA variants (symbolic / perceptual / recurrent) over a 4-way taxonomy (Counting-temporal / Permanence-spatial / Reference-identity / Imitation-procedural), 16 tasks × 1,600 demos in ManiSkill; perceptual best at 44.51%, symbolic excels at counting — "no single representation dominates" | C3 |
| [[2605.21572\|PhysX-Omni]] | Physics-readiness benchmark + generator (PhysX-Bench/PhysXVerse): material / affordance / kinematic-structure scoring across rigid + deformable + articulated, which fidelity metrics ignore | D2 |
| [[2410.01345\|GemBench]] | Progressive 4-level generalization benchmark on RLBench (16 train / 44 test tasks): novel placements (L1) → novel rigid objects (L2) → novel articulated objects (L3) → novel long-horizon (L4) — the graded cross-object/articulation generalization surface where native point-cloud heads (3D-LOTUS++) hold (64.5%/41.5%) and 2D drops to 0.3% on L4 | A1 |
| [[2606.13877\|ContactWorld]] | Vision-tactile WM benchmark of 12 contact-rich manipulation tasks (insertion/disassembly/screwing/exploration); measures which representation properties (spatiality / continuity / contact-sensitivity) drive stable long-horizon planning via JEPA-latent MPC — point-cloud explicit-3D 32.1% vs wrist-view-image 20.7%, +tactile-force-field 36.1% — corroborating the explicit-geometry-beats-image-substrate evidence now folded into C1's X-WAM discussion (its Depth-Architecture ablation) | C1 |
| [[2506.19816\|CronusVLA]] | temporal+spatial observational-robustness suite (24 disturbance types × 120 severities) over SimplerEnv, scored by R-Score (e.g. 96.2 on sparse temporal disturbance) — the temporal-OOD substrate H1/H3 lack, since LIBERO-Plus and WAM vs VLA Robustness perturb spatial/visual factors, not temporal disturbances | B2 |
| [[2602.06556\|LIBERO-X]] | graded 5-level progressive-difficulty robustness litmus (spatial layout / object properties / instruction semantics) with multi-label diagnostics; VLA avg 39.4% at L1, −31.2% L1→L5 — a graded-difficulty OOD axis complementing LIBERO-Plus's 7 isolated factor axes | B2 |
| [[2605.13632\|GTA-VLA]] (SimplerEnv-Plus) | OOD extension of SimplerEnv (camera variation / lighting / unseen objects / language perturbation): unseen-object 58.3% vs 36.2% baseline; trace-guidance recovers ~20% of failures (SimplerEnv-Bridge 81.2%→86.1%) — the named OOD-SimplerEnv suite H3's traces-help-OOD arm lacks | B2 |
| [[2607.21072\|ProVisE]] (SpatialGen-Bench) | Spatial cognition scored through *generated pixels*, not text: top text model 61.04% vs top visual model 54.49% vs human 87.79%; 88.03% of visual-model errors are genuine spatial-judgment failures, not parsing artifacts, a second, orthogonal instrument for the same metric gap [[2504.05786\|3D-Spatial-Reasoning-in-LLM-Survey]]/[[2601.13304\|CausalSpatial]] measure through text output | B1, B2 |

> [!tip] Convergence patterns
> - **The RGB-token tax — policies and world models default to pixels, leaving metric 3D motion, contact geometry, and spatial constraints implicit** (4-way): [[2506.20134|3D-World-Models-Survey]] (field is only now moving "from 2D visual perception to comprehensive 3D spatial cognition"), [[2510.16732|World-Models-for-Embodied-AI-Survey]] (the spatial axis is *still* evolving latent → token → explicit 3D, with explicit-geometry world models nascent), [[2604.22748|Agentic-World-Modeling-Survey]] (the L2 Simulator must compose rollouts that "respect domain laws," yet geometric forward models are underbuilt), and [[2406.02523|RoboCasa]] (a geometry-bound generalization suite where the pixel/latent default visibly drifts) — four sources name the same supervision cost explicit geometry would erase, the mandate for Clusters A and C.
> - **The grounding gap is metric, not semantic — the failure is *placing* objects in 3D and predicting how they move, not *naming* them** (5-way): [[2504.05786|3D-Spatial-Reasoning-in-LLM-Survey]] (coordinate-metric reasoning lags object-naming; no language↔metric-3D interface), [[2601.13304|CausalSpatial]] (GPT-5 sits **30 points** below humans on causal spatial tasks and is overconfident — a low Not-Sure-Rate), [[2605.29074|Embodied3DBench]] (13 VLMs, **none robust** on low-level metric 3D, and the score correlates with downstream LIBERO SR), [[2605.27367|SpatialBench-SFM]] (19 datasets / 31 models expose persistent depth/pose gaps), and [[2607.21072|ProVisE]] (the same **30+ point** human gap reappears when models must *show* a spatial answer as generated pixels rather than describe it in text, with **88.03%** of visual-model errors being genuine spatial-judgment failures): five sources, two independent answer-interfaces, converge that the spatial deficit is a *missing metric representation*, the mandate for Cluster B.
> - **Readiness beats fidelity — the field optimizes appearance (PSNR/FID) when the embodied bottleneck is interaction-readiness: geometry that carries physics, articulation, and a usable export format** (3-way): [[2604.26509|3D-Generation-for-Embodied-AI-Survey]] (names "simulation-readiness over visual fidelity," scarce physical annotations, and the deformable-asset gap as *the* bottleneck), [[2605.21572|PhysX-Omni]] (introduces a physics-readiness benchmark precisely because fidelity metrics do not score material / kinematics / affordance), and [[2406.02523|RoboCasa]] (a manipulation suite where a visually-faithful-but-physics-thin asset still fails the task) — three sources invert the fidelity objective, the mandate for Cluster D.

---

## Formal Framing

The central object is an **explicit geometric state** $G_t$ — a representation of scene structure that is *metric* and *externally interpretable*, as opposed to an appearance latent $z_t$ that is only decodable to pixels. Every direction is a statement about *where $G_t$ lives in the stack and who reads it*.

| Object | Definition | Owning cluster |
|---|---|---|
| Geometry-conditioned action head | $a_t = \pi(G_t, l)$ with $G_t$ an explicit metric state (point cloud $\in \mathbb{R}^{N\times 3}$, depth channel, or distilled 3D prior) the head conditions on instead of RGB tokens | A |
| Geometric forward model | $\hat G_{t+1} = f(G_t, a_t)$ over an explicit grid — occupancy $O \in \{0,1,\dots,K\}^{H\times W\times D}$ or rigid-body state — that the planner rolls forward | A (in-loop), C (substrate) |
| Spatial-cognition layer | a scene-graph / coordinate state $S$ over geometric entities s.t. $a_t = \pi(\text{reason}(S))$ — reasoning is *over* metric geometry, upstream of the action head | B |
| Renderable / 4D geometry WM | $G_t$ = explicit 4D (pointmap sequence, occupancy, or latent-4D) decoded for imagination (internal) or read by a third-party renderer/tracker to recover 6-DoF pose (external) | C |
| Interaction-ready asset | a reconstructed scene or object carrying geometry **+** physics parameters **+** kinematic structure (URDF/MJCF-exportable), not just radiance | D |

**The decodability axis.** $G_t$ differs by *who reads it*, and this is the doc's deepest organizing principle — sharper than 2D-vs-3D. At one end $G_t$ is **explicit and external**: occupancy a planner collision-checks (C1), pointmaps a tracker reads for 6-DoF (C2), assets a simulator loads (D1, D2). At the other end $G_t$ is **latent and internal**: a 4D state decoded inside the model for imagination ([[2604.26694|X-WAM]], now folded into C1's comparator scope) or a scene-graph an end-to-end head consumes (B1, B2). Between them sits **persistent**: geometry pinned to a world-frame so it survives long horizons (C3). Externality buys debuggability, composability, and tool-reuse at the cost of a decoder/extractor step; internality is cheaper but opaque. The split from [[WAM|WAM]] is about *ownership* (Spatial-4D owns the representation model-agnostically; WAM owns the machinery), not decodability — C1 and C2 sit on the same axis inside *this* doc.

> [[2604.26509|3D-Generation-for-Embodied-AI-Survey]] gives the canonical definition of the readiness criterion this doc adopts as the line between Cluster D and pure rendering:
> "simulation readiness as a primary evaluation criterion: geometric validity, physical parameterization, kinematic executability, and simulator format compatibility (URDF, MJCF)."

---

## Cluster Overview

| Cluster | Directions | Shared bottleneck | Cross-direction synergy |
|---|---|---|---|
| **A — Geometry-Native Policies** | A1, A2, A3 | The action head reads or predicts RGB tokens, leaving metric 3D implicit and paying an embodiment-specific data tax | **A1 is the lead** — its point-cloud state is the conditioning A2's occupancy forward model rolls forward and the upper bound A3's depth-token bridge approximates cheaply; the three span the *cost/benefit frontier* of explicit geometry (full point branch → in-loop occupancy → side-channel depth). [[2606.03943\|PointAction]] and [[2508.09071\|GeoVLA]] set the bar for A1; [[2510.14836\|QDepth-VLA]] for A3 |
| **B — 3D-Grounded Cognition** | B1, B2 | Reasoning isn't grounded in metric geometry: language CoT over RGB hallucinates spatial relations and dynamics | B1 grounds geometry in *space* (a scene-graph), B2 in *time* (4D consistency); together they are the spatial and temporal halves of one cognition layer, and both supply the upstream geometry Cluster A's action heads consume. [[2507.13362\|VLM-Spatial-Reasoning-RL]] and [[2605.05126\|ConsisVLA-4D]] set the bar |
| **C — Geometry-Native World Models & Memory** | C1, C2, C3 | World models imagine in pixels; geometry is recovered after the fact, isn't externally usable, isn't natively 4D, and isn't kept over long horizons | Organized by the **explicit-external ↔ latent-internal ↔ persistent** decodability axis: C1 (occupancy, now also home to the latent-4D comparator via [[2604.26694\|X-WAM]]) and C2 (4D-video pointmaps) are *external* — a planner/tracker reads them; C3 is *memory* — pinning C1's occupancy and latent-4D, and C2's pointmaps, to a world-frame so it survives minute-scale horizons. [[2603.28887\|OccSim]], [[2604.26694\|X-WAM]], [[2603.17117\|MosaicMem]] set the bars |
| **D — Reconstruction for Embodied Perception** | D1, D2 | Reconstruction optimizes radiance, not interaction-readiness; assets aren't physics- or kinematics-ready | D1 builds the *scene* you act in; D2 builds the *objects* that populate it (D2 → D1 composition); both supply the geometry the others consume — readable scenes for C's world models, point-cloud assets for A's policies. [[2404.09833\|Video2Game]] and [[2605.21572\|PhysX-Omni]] set the bars |


---

## Cluster A — Geometry-Native Policies

*The action head consumes or predicts explicit 3D — the policy's conditioning state is metric geometry, not RGB tokens. The three directions span the cost/benefit frontier of putting that geometry in the loop: a full point-cloud head where geometry is central, an occupancy forward model the planner rolls forward, and a cheap depth-token side-channel for the RGB policies nobody wants to re-train. The cluster does **not** pre-decide that "geometry is settled" and the centre of gravity should shift A1→A3 toward parameter-efficient injection: that shift is the conclusion of an experiment not yet run, not a premise. The primary gate is A1's explicit-point-head vs distilled/latent-geometry ([[2602.10098\|VLA-JEPA]]-class) comparison on a genuinely geometry-bound OOD split (A1-H5/H6); A2 (occupancy forward model) and A3 (depth-token bridge) are framed as **contingent on the result** — if explicit metric structure still beats the distilled latent on the geometry-bound tail, A1's native head is load-bearing and A2/A3 are the cost-reduction frontier around it; if the latent matches it, the cluster's gravity legitimately moves to A3's cheap injection and A1 demotes to a confirmatory ablation.*

### A1 — Point-Cloud-Native Action Heads vs RGB-Token Policies

| | |
|---|---|
| **Cluster** | A — Geometry-Native Policies |
| **Thesis** | An action is a function of *where things are in metric 3D* — and a point cloud says that directly, while an RGB token leaves it implicit. The policy field skips explicit 3D because it assumes a 2D-pretrained backbone already encodes the geometry the head needs. That assumption breaks under appearance shift, where geometry stays fixed but pixels move. The bet is in First-principles below. |
| **Anchor papers** | [[2506.20134\|3D-World-Models-Survey]] (survey), [[2510.16732\|World-Models-for-Embodied-AI-Survey]] (survey), [[2604.22748\|Agentic-World-Modeling-Survey]] (survey), [[2402.02500\|Point-Cloud-Matters]] (benchmark), [[2606.03943\|PointAction]] (method), [[2508.09071\|GeoVLA]] (method), [[2605.21414\|PointACT]] (method), [[2503.08950\|FP3]] (method), [[2411.18623\|Lift3D-Foundation-Policy]] (method), [[2503.04877\|Adapt3R]] (method) |
| **Key targets** | Cross-embodiment zero-shot SR ≥43.0% ([[2606.03943\|PointAction]] xArm7); ID SR ≥47.7% RoboCasa365; LIBERO ≥97.7% ([[2508.09071\|GeoVLA]]); RLBench ≥82.3% ([[2605.21414\|PointACT]]); graded-shift sweep point > RGB by up to **76.92%** mean SR across **125** tasks ([[2402.02500\|Point-Cloud-Matters]]); [[2506.07961\|BridgeVLA]] RLBench-18 ≥88.2% (vs RVT-2 81.4%), 95.4% real at 3 trajs/task — the point-cloud→2D-projection bar; [[2601.17486\|EquiForm]] 66.6% avg over 16 sim benchmarks (+10.0% over Canonical Policy, +24.3% over DP3) — the noise-robust equivariant-point bar |

**Why it matters.**
- **The gap**: the dominant recipe ([[2501.15830|SpatialVLA]], OpenVLA-class) feeds the head RGB tokens from a 2D backbone and hopes the geometry needed for contact-precise control falls out of the semantic features, so metric 3D motion and contact geometry stay implicit.
- **Today's answers**: the geometry-over-RGB principle is *already established* at small scale — [[2402.02500|Point-Cloud-Matters]] (NeurIPS'24) swept graded lighting/noise/background shift across 125 tasks and found point-cloud policies beat RGB by up to 76.92% mean SR *and* generalize better, and [[2306.06799|Point-Cloud-RL-Study]] localized that advantage to agent-object relationship reasoning. At VLA scale: [[2606.03943|PointAction]] splits into an embodiment-agnostic video-to-point model + a small point-to-action decoder (43.0% zero-shot on an xArm7 it never trained on, 47.7% RoboCasa365 ID); [[2508.09071|GeoVLA]] runs a 3D point branch beside a frozen VLM (97.7% LIBERO) with height/scale/viewpoint robustness; [[2605.21414|PointACT]] couples points to actions at multiple scales (82.3% RLBench). All add geometry — none isolate the *minimal-sufficient-geometry knee* or the *representation-vs-decoder* driver of cross-embodiment transfer.
- **The opening**: [[2605.24642|GFM-VLA-Study]] linear-probes the deficit directly — GR00T-N1.5's VLM output carries 0.73 m depth RMSE vs a geometric model's 0.41 m — so the RGB backbone provably does not encode the geometry the head needs, and supplying it explicitly is the lever.

**First-principles framing.**
- **First principle**: A manipulation action is set by the metric 3D layout (where gripper, object, and contacts are), not by 2D appearance. Geometry is fixed under lighting, texture, viewpoint, and camera placement; appearance is not. [[2606.02274|Dexterity-BEV]] is the cleanest demonstration: its BEV 3D frame holds 89.9% on shifted LIBERO exactly where 2D collapses to <10%.
- **Assumption being challenged**: the OpenVLA / [[2501.15830|SpatialVLA]] view that a big 2D backbone already encodes whatever geometry the head needs, so an explicit 3D branch is wasted overhead. [[2508.09071|GeoVLA]] and [[2606.03943|PointAction]] show it breaks under the shifts (height, scale, viewpoint, embodiment) where geometry holds but pixels move — and the latent counterpoint [[2602.10098|VLA-JEPA]] (97.2% LIBERO) shows even a strong *implicit*-geometry latent must be beaten on the geometry-bound split, not the saturated ID one. (The *graded-shift-helps* and *advantage-is-relational* claims are settled prior — [[2402.02500|Point-Cloud-Matters]], [[2306.06799|Point-Cloud-RL-Study]] — so the contest moves to whether they hold *at 2D-pretrained-VLA scale* with *minimal* geometry.) [[2607.14228|SeeSE3]] complicates the assumption rather than settling it either way: frozen DINOv2/DINOv3 features do carry latent 3D structure (mutual k-NN ≈0.38 with camera-pose space, linearly-decoded 6D pose at R²≈0.65 via a lightweight adapter), so the "2D backbones encode nothing about geometry" strawman is false: R²≈0.65, weaker still on translation than rotation, is far short of the metric precision contact-precise manipulation needs, which is exactly the residual A1's explicit channel is betting it can supply.
- **The bet**: the geometry-over-RGB advantage [[2402.02500|Point-Cloud-Matters]] proved with simple encoders survives at 2D-pretrained-VLA scale *and* most of it is recoverable from sparse geometry — specifically, (i) the recovery-vs-density curve has a knee far below [[2508.09071|GeoVLA]]'s full branch (≥80% of the full-branch SR gain at [[2403.03954|DP3]]-level sparsity), and (ii) it is the point *representation*, not [[2606.03943|PointAction]]'s factorized decoder, that carries the 43.0% xArm7 zero-shot transfer — swapping points→RGB-features (decoder fixed) collapses most of it, swapping the decoder (points fixed) does not.

**Related research papers.** One table; every paper is a row on the axis the direction turns on — *how geometry enters the action head* — from sensed points through predicted points, parallel branches, and learned/distilled latent priors, with `Key result` and the gap it leaves. The strongest rows are the load-bearing proof papers; the RGB row is the consensus this inverts.

| System | Geometry interface | Key result | What's missing |
|---|---|---|---|
| [[2606.03943\|PointAction]] | predicted dynamic pointmaps (embodiment-agnostic) | **47.7%** RoboCasa365 ID, **43.0%** zero-shot xArm7 (2–2.5× over baselines) | never isolates whether the point *representation* or the *factorized decoder* carries the zero-shot win |
| [[2608.10860\|FLEX-π]] | joint RGB + predicted 3D pointmaps (frozen video-VAE encodes both, "almost losslessly") + DINO semantics, denoised together with actions by a Mixture-of-Transformers WAM | real bimanual **83%** vs ManiFlow **58%**, RoboTwin **94.6%** (matches a VLA pretrained on **76×** more data); ablation: adding DINO **+6.8** pts, pointmaps a further **+20** pts | a *modality*-dropout ablation isolating the pointmap channel's marginal contribution specifically (not just "3D helps") — directional H4 supporting evidence that the point representation, not architecture, carries the gain — but modality-dropout is a coarser cut than [[2503.04877\|Adapt3R]]'s representation-vs-decoder split, and not PointAction's exact cross-embodiment 2×2 swap |
| [[2606.02274\|Dexterity-BEV]] | BEV 3D frame + vertex maps aligning views and actions | **89.9%** shifted LIBERO where 2D fails (**<10%**) | the cleanest shift result, but on one BEV parameterization — no minimal-geometry sweep |
| [[2508.09071\|GeoVLA]] | parallel frozen-VLM + 3D point branch + 3D MoE expert | **97.7%** LIBERO, **77%** ManiSkill2, robust to height/scale/viewpoint | full branch is the expensive upper bound; doesn't say how much sparser geometry suffices |
| [[2605.21414\|PointACT]] | multi-scale point-action coupling | **96.0%** LIBERO, **82.3%** RLBench | coupling beats coarse injection but needs a full point cloud at inference |
| [[2506.06199\|3DFlowAction]] | 3D optical flow as an action interface (no action labels) | **70%** SR vs 2D-flow 25% / video-WM 20%; Franka↔XTrainer transfer | flow is a motion field, not a static state — limited on fine contact geometry |
| [[2403.03954\|DP3]] | sparse-point diffusion policy | compact point conditioning drives a precise policy | answers "is full 3D necessary" only at one sparse setting — no recovery-fraction curve |
| [[2402.02500\|Point-Cloud-Matters]] | sensed point cloud vs RGB/RGB-D, graded-shift sweep | point clouds beat RGB by up to **76.92%** mean SR across **125** tasks + best zero-shot to lighting/noise/background/view shift | runs H1's graded-shift sweep two years early, but on *simple* encoders/DP — never the 2D-pretrained-VLA backbone A1 inverts, nor a minimal-geometry knee |
| [[2306.06799\|Point-Cloud-RL-Study]] | sensed point cloud in RL, vs RGB/RGB-D | 3D agents win on tasks needing agent-object 3D *relationship* reasoning, near-parity on planar | localizes the advantage to relational/contact reasoning (H2), but RL-scale, no VLA backbone, no representation-vs-decoder isolation |
| [[2601.16212\|Point-Bridge]] | cross-domain point-cloud transfer interface | bridges sensed-point policies across domains | a transfer interface, not a minimal-geometry or representation-vs-decoder ablation |
| [[2505.18474\|Canonical-Policy]] | SE(3)-equivariant canonicalized point policy | equivariance buys viewpoint/pose robustness for free | proves *structure* (equivariance) helps, but doesn't separate it from raw point density on the recovery curve |
| [[2509.01819\|ManiFlow]] | flow-matching policy where appearance *still* helps | strong SR with appearance retained alongside geometry | the counterpoint — geometry isn't strictly dominant; bounds the bet's "appearance is noise" claim where texture carries task signal |
| [[2604.12908\|VGA]] | manipulation as vision-to-geometry $f(v)\to G$ over a 3D-WM backbone | **98.1%** LIBERO, **+6%** OOD camera viewpoint | the first-principle as an architecture, but geometry is the *output*, not a sensed input |
| [[2604.15281\|R3D]] | LayerNorm-only 3D encoder + 3D augmentation (fixes the "scaling paradox") | RoboTwin 2.0 **83.8%** Easy / **64.8%** Hard, xArm6 real **60.7%** | makes 3D policies *scale*, but the win is the recipe, not an isolated geometry term |
| [[2605.11832\|AML-VLA]] | multi-view latent action manifold with G³T depth priors | **98.6%** LIBERO, **85.7%** LIBERO-Plus zero-shot perturbation | a learned 3D *prior*, not sensed geometry — leaves the sensed-vs-learned question open |
| [[2606.04436\|3DThinkVLA]] | latent 3D priors distilled via 3D-thinking co-training, no inference sensor | **98.7%** LIBERO, **81.0%** LIBERO-Plus zero-shot | geometry-as-latent-prior — strong OOD, but no explicit point state the head reads |
| [[2605.29416\|3DVLA]] | plug-and-play multi-view 3D fusion + object-centric 3D instances | **86.0%** LIBERO-Plus (SOTA), no 3D annotation | injected geometry, but on top of the RGB head — not a native point-cloud policy |
| [[2603.24393\|3D-MIX]] | VGGT 3D features into any VLA via semantic-gated fusion | **98.05%** LIBERO, **+12.51%** OOD | geometry bolted on without re-architecting — the cheap end of A1's own frontier (feeds A3) |
| [[2602.00937\|CLAMP]] | contrastive 3D multi-view action-conditioned pretraining | up to **+30%** SR, higher sample efficiency | 3D pretraining tuned for the policy, but the head still acts on RGB at inference |
| [[2604.14089\|UMI-3D]] | LiDAR 3D demonstration interface | drift-resistant pose on textureless/deformable scenes where vision SLAM fails | geometry-over-appearance at the *data* layer, not the action head |
| [[2604.06168\|Action-Images]] | 7-DoF actions as projected multi-view 2D-Gaussian heatmaps (EE position/up/normal) | **60%** zero-shot RLBench reach / **45%** real close-drawer (vs **0–20%**), PSNR **23.48** vs TesserAct 20.83 | resolves 3D ambiguity by *projecting* geometry into views — not a sensed/predicted point state the head reads directly |
| [[2604.17880\|ST-π]] | structured spatio-temporal VLA (ST-VLM decomposition + dual-generator) | top SR across 4 LIBERO suites, gains widen on long-horizon | structured geometry, but no point-vs-RGB head ablation (feeds B-style structure) |
| [[2501.15830\|SpatialVLA]] | RGB-token spatial policy (consensus baseline) | the standard 2D head this direction inverts | leaves all metric 3D implicit — the row the bet is measured against |
| [[2506.07961\|BridgeVLA]] | raw point cloud → multi-view 2D orthographic projections + 2D-heatmap pretraining, back-projected to 3D target points | **88.2%** RLBench-18 (vs RVT-2 **81.4%**), **95.4%** real with only 3 trajectories/task, **64.0%** COLOSSEUM vs **56.7%** | feeds geometry through the 2D VLM in its native format, same projection trick as [[2604.06168\|Action-Images]], but on a full point cloud the head never reads directly; no minimal-geometry sweep. Its successor [[2608.05042\|BridgeVLA++]] adds spatio-temporal memory on top of this same projection interface (RLBench-18 90.5%→93.7% in its own re-run), but that extension is a C3 memory-compound story, not an A1 geometry-interface one — see C3's table |
| [[2410.01345\|GemBench]] (3D-LOTUS) | language-conditioned Point Cloud Transformer end-to-end head (classification-based pose) | **83.1%** RLBench-18 at 2.2 GPU-days; 3D-LOTUS++ **64.5%** novel-rigid / **41.5%** novel-articulated / **17.4%** novel-long-horizon (vs 3D-LOTUS **0.3%**) | a native point-cloud head that holds on graded cross-object generalization where [[2508.09071\|GeoVLA]]'s ID branch saturates — but full point cloud at inference, no representation-vs-decoder isolation |
| [[2601.17486\|EquiForm]] | SE(3)-equivariant point policy + geometric denoising (normal/tangent correction) → diffusion action | **66.6%** avg over 16 sim benchmarks (+**10.0%** over Canonical Policy, +**24.3%** over DP3); +**28.1%** real | the noise-robust successor to [[2505.18474\|Canonical-Policy]]'s equivariance — adds denoising for sensor noise, but still needs a clean sensed point cloud and full density |
| [[2602.10698\|AugVLA]] | sensor-free: VGGT monocular depth → back-projected point cloud → PointNet feature + Action-Assistant regularizer into the VLA action expert | RoboCasa-GR1 **54%** at 100 demos / **46%** at 30 (vs Gr00T **50%** / **43%**) | the predicted-pointmap path [[2606.03943\|PointAction]] takes, but as a side-feature bolted onto the RGB head (like [[2603.24393\|3D-MIX]]) — not a native point branch, and gains are modest |
| [[2602.00807\|Any3D-VLA]] | patch-level gated-residual fusion of compressed 3D point clouds with 2D features, trained on a *hybrid* of sim-GT / sensor / model-estimated depth sources | **62.5%** real zero-shot manipulation (**+29.2%** over [[2501.15830\|SpatialVLA]]), **+13.9%** LIBERO over GraspVLA; sensor-free at inference (estimated depth often beats commodity sensors) | the cleanest instrument for H7's sensing-tax fold — its hybrid-depth-source training is exactly the "charge the predicted-depth error on the contact tail" lever, and it lands the geometry-bolt-on on top of an RGB head (like [[2603.24393\|3D-MIX]]/[[2602.10698\|AugVLA]]) — but the depth-source-diversity ablation isn't decomposed into a recovery-fraction or representation-vs-decoder split |
| [[2507.00416\|Evo]] (Evo-0) | sensor-free implicit 3D: VGGT spatial encoder → 3D tokens cross-attended into VLM, no depth/point input | RLBench **56%** (+15pt over π0 **41%**), real **57.41%** (+28.88pt); up to +40pt under viewpoint/height/distractor shift | the learned-implicit-3D end of the frontier alongside [[2605.11832\|AML-VLA]] and [[2606.04436\|3DThinkVLA]] — strong OOD but no explicit point state, leaving the sensed-vs-learned question open |
| [[2606.10899\|MV-Actor]] | multi-view RGB-D → semantic-spatial tokens with Pi3X feed-forward spatial priors + metric-depth repair into pose head | PerAct2 bimanual **87.8%** (vs RGB-D 3DFA **85.1%**), real ARK-Lift **63.1%** staged (vs **58.1%**) on consumer depth cameras | resolves consumer-depth noise via feed-forward spatial priors (the sensor-noise risk [[2606.03943\|PointAction]] dodges by predicting points) — but fuses 3D into a 2D head, not a native point policy |
| [[2404.18926\|Point-Cloud-Robustness]] (PCWM) | model-based RL world model on partial point clouds (PointConv + RSSM, no reconstruction) | **76%** less viewpoint-induced performance drop than RGB-D on LiftCube; adapts to new camera in **25–32** episodes vs **70–94** for RGB-D-WM | the RL/WM-scale sibling of [[2306.06799\|Point-Cloud-RL-Study]] — localizes the win to viewpoint/FOV shift, but RL-scale, no VLA backbone, no minimal-geometry knee |
| [[2403.09631\|3D-VLA]] | 3D vision encoder + point clouds into an LLM backbone with a generative WM predicting goal point clouds | improved 3D grounding/localization over 2D VLM baselines; zero-shot manipulation planning to novel scenes (qualitative) | the historical root of "3D state into the action model" that [[2606.03943\|PointAction]] and [[2508.09071\|GeoVLA]] later sharpened — but geometry enters via a generative-WM coupling, not a clean point head the policy reads |
| [[2503.04877\|Adapt3R]] | decoupled encoder: 2D semantics (CLIP ResNet) + 3D used *only* for end-effector-frame localization (canonicalized points + Fourier PE + attention-pool → compact conditioning vector), modular into ACT/DP/BAKU | **<6%** SR drop on novel camera poses (vs **44.4%** RGB / **55.6%** 3D Diffuser Actor), zero-shot cross-embodiment to UR5e/Kinova3/Kuka IIWA, **93** LIBERO-90 + 6 real tasks at **44.1 Hz** | the cleanest published *representation-vs-decoder* split (3D representation separated from the semantic backbone by design — H4's "never isolated" driver) and a direct H5 OOD instrument — but a fixed end-effector-frame canonicalization, not a minimal-geometry-density sweep, and 3D collapsed to a localization medium rather than the full point branch [[2508.09071\|GeoVLA]]/[[2601.17486\|EquiForm]] keep |
| [[2503.08950\|FP3]] | native point-cloud foundation policy (multimodal diffusion transformer, DROID-pretrained, LoRA-finetuned) with a point-vs-RGB head ablation *at foundation scale* | **100%** across four household tasks (80 demos), full appearance-shift robustness (**100%** lighting / novel-object / background); 2D-image head collapses to **30%** avg | the foundation-scale 3D head H1's at-VLA-scale claim is about, and a clean geometry-over-RGB proof point — but reports the head ablation at one density, not a minimal-geometry knee or representation-vs-decoder isolation |
| [[2411.18623\|Lift3D-Foundation-Policy]] | lifts a 2D-pretrained backbone (DINOv2/CLIP) into an explicit 3D point head via a 2D-model-lifting strategy + task-aware depth-reconstruction MAE | **84.5%** MetaWorld (**+18.2%** over DP3), **88.0%** Adroit (**+21.3%** over DP3), **90%/85%** real with 30 demos | sits exactly on A1's challenged assumption (2D priors *do* carry into 3D); its lifting-step ablations probe *which* steps carry the gain (H4's driver) — but the lift is a fixed scheme, no recovery-vs-density sweep |
| [[2502.12320\|Fusing-Point-Cloud-Visual]] | FPV-Net fuses point cloud + RGB via AdaLN (RGB modulates points) on RoboCasa | **49.42–50.50%** RoboCasa vs DP3 **22.75%**; local RGB features add a separable **+2%** on fine-grained tasks | the appearance-still-helps counterpoint that bounds the bet's "appearance is noise" claim on the exact RoboCasa benchmark H2/H4 use — geometry isn't strictly dominant where local texture carries task signal, beside [[2509.01819\|ManiFlow]] |
| [[2410.17488\|GenDP]] | 3D semantic fields (point cloud + DINOv2 descriptors) into a diffusion policy, with a without-semantics ablation | **93%** unseen-instance SR vs vanilla DP **20%**; without-semantics ablation at **58%** isolates the geometry contribution from the semantic one | a clean geometry-vs-semantics decomposition on category-level OOD — but distinct from H4's representation-vs-decoder split ([[2503.04877\|Adapt3R]] holds that title): GenDP separates geometric from semantic *content*, not representation from decoder *architecture*, and the point state is varied, not its density, so no minimal-geometry knee either |
| [[2406.11740\|Imagination-Policy]] | generative point-cloud model imagines target points → SVD point-cloud registration infers the SE(3) transform (action factored into geometry-generation + transformation-estimation, bi-equivariant) | high-precision RLBench: Plug-Charger **26.67%** vs **0%** baselines (10 demos), Stack-Wine **97.33%** vs **32%** at 5 demos; real UR5 no sim pre-training | the root form of [[2606.03943\|PointAction]]'s predicted-point / factorized-action path — exactly the representation-vs-decoder factorization H4 interrogates, but never separates which half carries the win |
| [[2505.10105\|EmbodiedMAE]] | unified 3D MAE (RGB+depth+points) pretrained on DROID-3D, features fed to a fixed policy | xArm **95.4%** vs DINOv2-RGBD **84.6%**, SO-100 **65.4%** vs SPA **57.5%**; scales with model size | the sensed-3D-encoder counterpart to [[2606.03943\|PointAction]]'s predicted-point path — strengthens the representation side of the bet (the depth/point representation drives the gain) — but a pretrained encoder, not a minimal-geometry sweep |
| [[2509.18676\|3D-Flow-Diffusion-Policy]] | 3D scene-flow as the policy state (2D point-tracker + depth → lifted 3D flow) | real avg **56.9%** vs DP3 **27.5%**; succeeds where DP3 scores **0%** (Hang **30%**, Shelve **20%**, Non-prehensile **35%**) | concentrates its advantage exactly where H2 predicts — contact-rich/non-prehensile phases — lifting the [[2506.06199\|3DFlowAction]] interface into 3D; but flow is a motion field, not a static state, limited on fine contact geometry |
| [[2506.01196\|OG-VLA]] | point cloud from multi-view RGBD → canonical orthographic views → VLM emits image tokens decoded to 6-DoF pose (the BridgeVLA / Action-Images projection lane at VLA scale) | ARNOLD Novel-Object Pickup **90.0%** vs **50.0%** baselines; real **100%** novel pose / **80%** novel object with 3–5 demos | the projection-trick counterpoint to a head reading points directly — feeds geometry through a 2D VLM in native orthographic format, same trick as [[2604.06168\|Action-Images]]/[[2506.07961\|BridgeVLA]] but never reads a point state directly, no minimal-geometry sweep |
| [[2607.06564\|Lift3D-VLA]] | 2D-model-lifting so the VLA's 2D vision encoder processes point clouds via camera-aligned virtual projections + Geometry-Centric MAE (static-3D reconstruction + *future*-geometric prediction) + layer-wise temporal action decoding | **88.6%** MetaWorld single / **87.7%** multi-task, **82.8%** RLBench, **71%** real avg, degradation held to **6–8%** under unseen objects/lighting/backgrounds | the VLA-scale successor to [[2411.18623\|Lift3D-Foundation-Policy]]'s 2D-model-lifting: adds a *dynamics*-aware future-geometric-prediction objective and temporal action coherence, but a fixed lifting scheme over synthesized pseudo-3D, no minimal-geometry-density sweep and no representation-vs-decoder isolation |
| [[2605.15836\|GAP]] | action-free geometric-anchor *pretraining* of a spatial adapter (object-centric masks + centroid-alignment / geometric-spread / keypoint-diversity losses), decoupled from action-mapping | **62%** Can at 15 demos (vs AFA **46%**), **56–59%** StackCube; lifts R3M/DINOv2/VC-1 alike; prevents low-data "bottleneck collapse" | a geometric-prior *pretraining* regularizer for the data-scarce regime (beside [[2602.00937\|CLAMP]] contrastive-3D and [[2505.10105\|EmbodiedMAE]] MAE pretraining), but geometry is a train-time prior shaping the adapter, not a sensed/predicted 3D state the head reads, and no recovery-vs-density knee |
| [[2607.11498\|Robot-Centric Pointmaps]] | end-effector-centered pointmap (depth → base frame → EE-centered), preserving the H×W image grid and fused element-wise with RGB tokens via a pointmap encoder initialized from the RGB encoder | RoboCasa **+7.6 pp** (π0.5 55.3%→62.9%), real Franka **+11.7 pp** unseen-viewpoint (55.0%→66.7%) | keeps the image grid so it reuses 2D-pretrained weights, sidestepping the frame-mismatch [[2606.03943\|PointAction]]'s predicted path also avoids, but injects a fixed dense pointmap with no minimal-geometry-density sweep or representation-vs-decoder isolation |
| [[2607.12356\|VistaVLA]] | 3D Gaussian primitives carrying both geometry and distilled open-vocab semantics, compressed via a parameter-free Merge-then-Query mechanism into fixed-length tokens | **+22.8 pp** real-world avg SR over VLA-Adapter, LIBERO-Pro-Swap spatial-OOD **1.7%→12.2%** | the semantic-plus-geometric Gaussian analog of [[2410.17488\|GenDP]]'s 3D semantic fields, but a heavier two-stage RGB-D-render-then-distill pipeline, no recovery-fraction curve against a full point branch |
| [[2512.21970\|StereoVLA]] | stereo RGB pair → GeoSem encoder fusing a stereo-depth foundation model's filtered cost volume with SigLIP/DINOv2 semantics, plus interaction-region-depth and camera-parameter co-training tasks | **+33.4 pp** real avg SR (transparent/bar-shaped objects at 0% baseline), robust across camera-randomization ranges, strong zero-shot on LIBERO-MV-R | a *stereo* geometry interface distinct from every monocular-depth row in this table, but a 5M-trajectory synthetic-stereo pretraining cost, and no recovery-fraction curve against [[2508.09071\|GeoVLA]]'s full 3D branch |
| [[2602.10101\|Robo3R]] | feed-forward RGB + robot-state → metric-scale 3D reconstruction (masked-point head + keypoint/PnP canonical-frame alignment) | point error **0.006** / scale error **0.007** (order-of-magnitude over depth-camera baselines), **43.5 Hz** monocular; IL SR up to **15/16**, grasp SR **10/16** on transparent/reflective objects depth sensors fail on | sharpens H7's sensing-tax fold: *predicted* geometry beats sensed depth on exactly the textureless/reflective tail H7 worries about, so an "unpriced second network" may buy back more than it charges; a reconstruction backbone, not itself a policy head |
| [[2508.17230\|FVP]] | next-point-cloud-prediction diffusion pretext task, plugged in ahead of any 3D encoder/IL policy | **+16.9%** ID / **+24.7%** OOD SR (DP3, 12 sim tasks), **+15–55 pp** absolute across 12 real tasks | the temporal-dynamics twin of [[2605.15836\|GAP]]/[[2602.00937\|CLAMP]]/[[2505.10105\|EmbodiedMAE]]'s pretraining regularizers, predicting *future* geometry, not just present, but a pretext task bolted onto an existing 3D pipeline, no minimal-geometry knee of its own |
| [[2607.14228\|SeeSE3]] | none injected: probes what geometry a *frozen* 2D self-supervised backbone already encodes, then a lightweight Poincaré adapter linearly decodes it | DINOv2/DINOv3 topological alignment with camera-pose space (mutual k-NN ≈**0.38**); adapter recovers 6D twist vectors at **R²≈0.65**; **60%** success on training-free latent-space visual odometry | evidence *for* A1's challenged assumption, not against it: 2D SSL features do carry latent 3D structure, but R²≈0.65 (weaker on translation than rotation) is far from metric-precise, bounding how much of A1's bet a frozen 2D backbone alone can recover |
| [[2606.12759\|Sparse2Act]] | pretraining-encoder decoder-capacity ablation (representation vs decoder, at the encoder level) | establishes a representation-over-decoder finding: the pretrained representation, not decoder capacity, carries the transfer gain | H6's gate-deciding paper — but it ablates a *pretraining encoder's* decoder capacity, not [[2606.03943\|PointAction]]'s *cross-embodiment factorized predicted-pointmap decoder*, so it doesn't cover the representation-identity 2×2 swap H6 runs |

**Hypotheses & tests.** The FP bet — the small-scale geometry-over-RGB advantage survives at VLA scale *and* from sparse geometry — decomposed. H1/H2 are *confirmed prior* ([[2402.02500|Point-Cloud-Matters]], [[2306.06799|Point-Cloud-RL-Study]]) re-run at VLA scale to anchor the regime; the front-line contribution is H3 (minimal-geometry knee) + H4 (representation-vs-decoder) + H5 (beat the latent on OOD). **The primary gate for the whole cluster is the explicit-point-head vs distilled-latent comparison (H5 below, sharpened by H6): A2/A3's strategic weight is *contingent* on it** — only if explicit metric structure beats the latent on the geometry-bound OOD split does A1's native head stay load-bearing and A2/A3 become its cost-reduction frontier; if the latent matches, the cluster's gravity moves to A3. H7 then prices the sensing tax that decides whether the explicit-head recommendation survives once geometry is charged for.
1. **H1 — The graded-shift advantage replicates at 2D-pretrained-VLA scale (confirmed prior, extended).**
   - *Prediction*: [[2402.02500|Point-Cloud-Matters]]'s graded lighting/noise/background sweep — which already showed point > RGB on simple encoders — holds with a *2D-pretrained-VLA backbone*: holding the backbone fixed, the point-head-minus-RGB SR margin grows monotonically with shift magnitude, ≈0 at zero shift.
   - *Test*: re-run [[2402.02500|Point-Cloud-Matters]]'s sim shift sweep with a [[2508.09071|GeoVLA]]-style VLA point head vs an RGB head; check the small-scale monotone pattern survives the backbone swap, then confirm it physically on [[2509.18953|Eva-VLA]]'s real-world graded-shift suite (illumination + 3D object transforms + adversarial patches, per-variation failure rates — SOTA VLAs surge from **4.0–23.5%** to over **80%** under optimized shift).
   - *Row*: Point Cloud Matters / GeoVLA / SpatialVLA.
   - *Falsifier*: a flat/non-monotone margin at VLA scale → the small-scale result is encoder-specific, not a backbone-invariant geometry principle.
2. **H2 — The advantage concentrates in relational/contact sub-segments at VLA scale (confirmed prior, extended).**
   - *Prediction*: [[2306.06799|Point-Cloud-RL-Study]]'s localization of the win to agent-object *relationship* reasoning reproduces at VLA scale — decomposing SR by phase, the point-head margin concentrates in contact-rich segments and is near-zero in free-space transit.
   - *Test*: phase-label trajectories on [[2606.03943|PointAction]]'s RoboCasa365 tasks; report margin per phase.
   - *Row*: Point Cloud RL Study / PointAction.
   - *Falsifier*: a uniform margin across phases → the point state helps globally, not via relational contact geometry.
3. **H3 — Minimal sufficient geometry sits well below a full point branch.**
   - *Prediction*: most of the SR gain is recovered by sparse points ([[2403.03954|DP3]]-density), and the recovery-vs-density curve has a knee far below [[2508.09071|GeoVLA]]'s full branch.
   - *Test*: sweep point density/completeness on a fixed head; plot recovered fraction of the full-branch gain.
   - *Row*: DP3 / GeoVLA.
   - *Falsifier*: gain scales with density up to the full branch → cheap geometry doesn't suffice; A3's depth bridge is the only economical path.
4. **H4 — The point *representation*, not the factorized decoder, carries the zero-shot win.**
   - *Prediction*: swapping [[2606.03943|PointAction]]'s point representation for an RGB-feature representation (decoder architecture held fixed) collapses most of the 43.0% xArm7 transfer; swapping the decoder while keeping points does not.
   - *Test*: 2×2 representation-vs-decoder swap on the embodiment-free pretraining, scored on [[2505.14986|AnyBody]]'s controlled morphology-distance protocol (18 Isaac-Sim morphologies, explicit interpolation/extrapolation/composition splits) so the transfer is read off a suite that *isolates* morphological generalization from appearance/geometry confounds — though its reach/push tasks narrow rather than close the contact-precise side. [[2608.10860|FLEX-π]]'s modality-dropout ablation (pointmaps alone add +20 pts atop DINO's +6.8) is directional supporting evidence for the representation side, but a dropout ablation, not the representation-vs-decoder 2×2 this hypothesis needs.
   - *Row*: PointAction / FLEX-π.
   - *Falsifier*: the decoder swap costs as much as the representation swap → factorization, not geometry, is the driver.
5. **H5 — A native point head beats the strongest latent-geometry policy on the geometry-bound split, not on ID.**
   - *Prediction*: against [[2602.10098|VLA-JEPA]] (97.2% LIBERO) and [[2606.04436|3DThinkVLA]] (81.0% LIBERO-Plus), the point head ties on ID LIBERO but exceeds on LIBERO-Plus perturbation, where explicit metric structure beats distilled latent priors.
   - *Test*: head-to-head on LIBERO (ID) and LIBERO-Plus (OOD) at matched data.
   - *Row*: VLA-JEPA / 3DThinkVLA.
   - *Falsifier*: latent priors match the point head on OOD → explicit points add nothing the latent can't distill.
6. **H6 — The explicit-point-head vs distilled-latent contest is the PRIMARY gate, decided on a *cross-embodiment factorized-decoder* split, not saturated ID (and it decides A2/A3's weight).**
   - *Prediction*: this is H5 sharpened into the cluster's load-bearing experiment — on a genuinely geometry-bound, cross-embodiment split, a native point head retains a measurable margin over the strongest *sensor-free* latent ([[2507.00416|Evo]] +40pt under viewpoint/height shift with no point input, [[2606.04436|3DThinkVLA]] 81.0% LIBERO-Plus, [[2602.10098|VLA-JEPA]] 97.2% LIBERO) that survives [[2606.12759|Sparse2Act]]'s already-published representation-over-decoder finding — because Sparse2Act ablates a *pretraining encoder's decoder capacity*, not [[2606.03943|PointAction]]'s *cross-embodiment factorized predicted-pointmap decoder* in a representation-identity 2×2 swap. The margin's sign decides the cluster's gravity: positive → A1's native head is load-bearing and A2/A3 are its cost-reduction frontier; ≈0 → demote A1 to a confirmatory ablation citing Sparse2Act and shift weight to A3.
   - *Test*: run PointAction's representation-identity 2×2 (point-rep vs RGB-feature-rep) × (factorized vs monolithic decoder) on [[2505.14986|AnyBody]]'s morphology-distance splits, and in the *same* harness pit the winning point head against the sensor-free latent on LIBERO (ID) vs LIBERO-Plus (OOD) at matched data — the contest [[2606.12759|Sparse2Act]] does not run.
   - *Row*: PointAction / VLA-JEPA / Sparse2Act.
   - *Falsifier*: the sensor-free latent matches the point head on the cross-embodiment OOD split *and* Sparse2Act's encoder-level finding already covers the representation-identity swap → explicit points are recoverable from a latent, and the cluster's gravity rightly moves to A3.
7. **H7 — The sensing tax can invert the explicit-head recommendation once depth-source cost is charged (blind-spot fold).**
   - *Prediction*: the externalized-geometry recommendation assumes geometry arrives clean and free, but a sensed point head pays a depth sensor and a predicted-pointmap head ([[2606.03943|PointAction]], [[2602.10698|AugVLA]]) pays a *second network* whose error is worst exactly on the contact-rich / textureless / specular tail where H2 says geometry matters most — so on a *cost-charged* axis (added params + sensor requirement + estimated-depth-noise-at-the-hard-tail) the explicit head's net SR advantage over a sensor-free latent shrinks, and may invert below a measurable noise threshold.
   - *Test*: re-score H5/H6's explicit-vs-latent contest under realistic estimated-depth noise (not clean rendered depth), counting the depth-producing network's params/latency against the point head, and locate the noise level at which the latent's no-sensing-tax advantage overtakes the explicit head. The tempering counter-evidence to fold in: [[2602.00807|Any3D-VLA]] shows hybrid sim/sensor/estimated-depth training makes the estimated-depth path *robust* (estimated depth can beat commodity sensors), so the sensing tax may be payable rather than fatal — H7 should report the explicit head's margin both with and without the depth source trained Any3D-VLA-style.
   - *Row*: PointAction / AugVLA / Any3D-VLA / VLA-JEPA.
   - *Falsifier*: the explicit head keeps its margin even under hard-tail estimated-depth noise with the second network charged → the sensing tax is not the deciding variable and the externalized-geometry recommendation stands.

> [!warning] Risks
> - **Point clouds need depth sensing or reconstruction** that may be noisy or unavailable at deploy. → Lean on the predicted-pointmap path ([[2606.03943|PointAction]]) so geometry is *generated*, not sensed, removing the hard depth-sensor dependency.
> - **The unpriced geometry-source / sensing tax may decide the bet** — a sensed head needs a depth sensor and a predicted-pointmap head needs a *second network* whose error is worst on the contact-rich/textureless tail where geometry matters most, so "geometry is free" is the hidden variable behind every A-cluster recommendation. → H7 charges the sensing tax explicitly; gate the explicit-head recommendation on the *cost-charged* explicit-vs-latent margin under realistic estimated-depth noise, not the clean-depth number.
> - **The advantage may vanish on in-distribution benchmarks** where RGB backbones already saturate (LIBERO ~97%). → Design evaluation around the appearance-shift / cross-embodiment splits where H1/H5 predict the gap appears; treat ID parity as expected, not failure.
> - **Full 3D branches add latency and parameters.** → H3's minimal-sufficient-geometry sweep + A3's depth-token bridge as the lightweight fallback if full point branches don't pay their compute.

### A2 — Occupancy-Forecasting as the Policy's Forward Model

| | |
|---|---|
| **Cluster** | A — Geometry-Native Policies |
| **Thesis** | Planning needs to know *what space will be filled*, not *what the scene will look like* — a voxel grid answers that directly, while pixels make you re-infer geometry every step. Occupancy-as-forward-model for manipulation isn't new — [[2203.06856\|ACID]] and [[2505.16249\|3D-Occ-MPC]] already ran it, on an implicit field and a single-object grid — but nobody has taken a *dense explicit* voxel loop to RoboCasa scale and closed it into the policy's own action head. The bet is in First-principles below. |
| **Anchor papers** | [[2604.22748\|Agentic-World-Modeling-Survey]] (survey), [[2510.16732\|World-Models-for-Embodied-AI-Survey]] (survey), [[2506.20134\|3D-World-Models-Survey]] (survey), [[2603.28887\|OccSim]] (method), [[2510.10125\|CTRL-WORLD]] (method) |
| **Key targets** | Stable rollout ≥3,000 frames / ≥4 km ([[2603.28887\|OccSim]]); +22.1% relative mIoU vs asset-based sim; 67% zero-shot; manipulation port: beat a pixel-world-model baseline on geometry-bound [[2406.02523\|RoboCasa]] tasks, scored via [[2604.19092\|RoboWM-Bench]]'s WM→executable-action→step+final SR (more discriminative than perceptual-plausibility metrics) |

**Why it matters.**
- **The gap**: model-based control needs a forward model, and the default is a pixel-space video predictor the policy must re-parse into geometry every step — so drift piles up fast (pixel/occupancy WMs in driving were "limited to fewer than 50 frames," per [[2603.28887|OccSim]]).
- **Today's answers**: occupancy-as-manipulation-forward-model is *not* new — [[2203.06856|ACID]] (RSS'22) already rolled an implicit occupancy+flow forward model in a goal-conditioned manipulation loop (+30% SR over the strongest baseline, sim→real on plush toys), and [[2505.16249|3D-Occ-MPC]] (RA-L'25) ran explicit occupancy → learned dynamics → predictive control on elasto-plastic objects (occupancy-state EMD **28.6e-3** vs patch/crop baselines). What persists in pixels: [[2510.10125|CTRL-WORLD]] makes the pixel forward model controllable (38.7→83.4% on unseen objects); [[2506.23135|RoboScape]] augments it with temporal depth + keypoint dynamics (91% Robomimic on 200 synthetic trajectories). None of the occupancy prior runs the dense-voxel-vs-pixel-WM *horizon-to-divergence* head-to-head at RoboCasa scale.
- **The opening**: [[2603.28887|OccSim]] forecasts *occupancy* — an explicit voxel-semantic grid — with a Warp-DiT block that bounds geometric error via 3D rigid transforms, sustaining 3,000+ stable frames over 4+ km (**80×** the prior <50-frame ceiling) and lifting downstream forecasting **+22.1%** relative mIoU (67% zero-shot). The horizon ceiling the field treats as inherent is a *property of the pixel substrate*, removable.

**First-principles framing.**
- **First principle**: a planner needs to know which regions of space will be occupied (collision-freedom, contact, reachability) — a voxel grid answers that directly. Pixels are a lossy, view-dependent re-encoding of the same geometry; occupancy is the planning-native state.
- **Assumption being challenged**: this is a measurement gap, not a contested belief — no paper argues *for* pixel-space forward models over explicit occupancy on the merits. [[2510.10125|CTRL-WORLD]] builds on a pretrained video-diffusion backbone (Stable Video Diffusion) because that scale of pretraining exists for pixels and not for occupancy grids — its own paper motivates the choice as "a scalable and promising alternative" to hand-built simulators, never as a claim that pixel prediction forecasts *further* than explicit geometry. Where the two have been measured against each other, pixels already lose: [[2603.28887|OccSim]]'s 80× horizon gain and rigid-body forecasters like [[2605.09196|RigidFormer]] (8× faster than FIGNet, stable to 217 objects) both point toward explicit geometric state, not away from it. (That occupancy-is-the-planning-native-state is *settled* — [[2203.06856|ACID]], [[2505.16249|3D-Occ-MPC]] — so what's genuinely missing is not a rebuttal to overturn, it's the *dense-explicit-voxel-vs-pixel-WM* horizon-to-divergence measurement at RoboCasa scale, which nobody has run.)
- **The bet**: a *dense explicit* voxel-occupancy forward model — beyond [[2203.06856|ACID]]'s implicit field and [[2505.16249|3D-Occ-MPC]]'s single-object grid — holds geometric stability an order of magnitude longer than a *pixel*-WM baseline at RoboCasa scale (rollout-horizon-to-divergence ≥10× the pixel baseline), and its *forecast* occupancy can condition A1's point head to close a fully-geometric perceive-imagine-act loop that beats present-frame-only conditioning on long-horizon SR — none of which any single occupancy-manipulation paper has run. The distinct object is the *in-loop policy forward model vs a pixel forward model*; the occupancy-vs-*latent*-4D horizon comparison and the sub-cm Warp-DiT resolution sweep are ceded to sibling **C1** ([[2604.19092|RoboWM-Bench]], H1/H4), not re-run here.

**Related research papers.** One table on the axis the direction turns on — *the forward-model substrate the planner rolls forward* (occupancy-voxel / pixel-video / rigid-body / depth-augmented / latent-4D) — with `Key result` and what each leaves missing for a manipulation planner.

| System | Forward-model substrate | Key result | What's missing |
|---|---|---|---|
| [[2603.28887\|OccSim]] | voxel-semantic occupancy + Warp-DiT rigid-transform bound | **3,000+** stable frames, **4+ km**, **+22.1%** rel mIoU, **67%** zero-shot | a driving result — never ported to sub-cm tabletop occupancy |
| [[2203.06856\|ACID]] | implicit occupancy + flow forward model (goal-conditioned) | **55.6%** SR (**+30%** over best baseline) on unseen deformable manipulation, sim→real | establishes occupancy-as-forward-model for manipulation (2022), but *implicit* field, plush-deformable-specific, no horizon-to-divergence vs pixel WM |
| [[2505.16249\|3D-Occ-MPC]] | explicit occupancy → learned dynamics → MPC | occupancy state EMD **28.6e-3** / DCD **34.5e-4** (beats patch/crop), elasto-plastic sim+real | runs the in-loop explicit-occupancy control A2 proposes, but single-object/elasto-plastic, no RoboCasa-scale ≥10× horizon claim, no A1-head conditioning |
| [[2505.05512\|Occupancy-World-Model]] | autoregressive indoor occupancy forecasting | **+22.34** IoU / **+12.21** mIoU next-state, **+19.48**/**+11.61** autoregressive | ports occupancy WM driving→indoor (the scale move A2 needs), but a scene forecaster, not a manipulation control loop |
| [[2506.23126\|ParticleFormer]] | particle/point forward dynamics + MPC | superior MSE/CD dynamics, robust to hyperparams, lower MPC final-state error | particle (not dense-voxel-occupancy) substrate — the point-set alternative to A2's grid; no occupancy horizon-to-divergence |
| [[2508.17600\|GWM]] | feed-forward 3D-Gaussian-Splatting + 3D-Gaussian-VAE compact latent + DiT predicting future 3D states action-conditioned via cross-attention, rolled forward by the planner in model-based RL | **+10.5%** avg IL SR, real pick-place **35%→65%**, ~**2×** faster MBRL convergence on Meta-World | the geometry-native (3DGS) manipulation forward model the table lacks — sits between [[2604.26694\|X-WAM]]'s latent-4D and A2's dense-explicit-voxel, strengthening the geometry-beats-pixels claim — but a rendering-Gaussian substrate, not a planner-collision-queryable occupancy grid, and no occupancy horizon-to-divergence |
| [[2506.23135\|RoboScape]] | physics-informed RGB + temporal depth + keypoint dynamics | **91%** Robomimic (200 synthetic trajectories), **0.953** Pearson real-SR | depth-augmented but still pixel-anchored — geometry not the native state |
| [[2605.09196\|RigidFormer]] | transformer rigid-body dynamics forecaster | **0.161 m / 15.33°** MOVi-B error, **23.9** FPS (8× FIGNet), stable to 217 objects | object-level rigid bodies, not a dense occupancy field the planner queries |
| [[2604.22152\|dWorldEval]] | pixel-video drift-over-horizon evaluator (benchmark) | LPIPS **0.243** at 20-step round-trip horizon (beats drifting baselines), policy-ranking Pearson **~0.9–0.92** | standardizes the drift-over-horizon measurement A2 borrows from driving, but pixel substrate — cannot score *geometric* divergence; the metric vehicle for H1's frames-to-divergence axis |
| [[2504.20995\|TesserAct]] | 4D RGB-D-N world model | depth+normal channels sharpen action prediction | the depth-augmented midpoint — geometry is a channel, not the substrate |
| [[2604.16484\|DexWorldModel]] | O(1)-memory latent dexterous WM | constant-memory rollout over 2,000 steps | latent, not explicit — the efficiency budget the occupancy loop must respect |
| [[2604.26694\|X-WAM]] | latent-4D unified WAM (now C1's latent-4D comparator) | Chamfer **0.0049** vs 0.0680, 15 Hz | internal latent the planner can't read directly — A2's delta is *external* occupancy |
| [[2510.16732\|World-Models-for-Embodied-AI-Survey]] | latent → token → explicit-3D taxonomy | locates occupancy at the explicit end of the spectrum | a map, not a manipulation forward model |
| [[2406.02523\|RoboCasa]] | the proposed target suite | geometry-bound long-horizon kitchen tasks where pixel WMs drift | no occupancy-loop baseline exists on it yet — the gap A2 fills |
| [[2410.00425\|ManiSkill3]] | GPU contact-rich manipulation | the sub-cm contact-precision stress test | tests policies, not forward-model horizon stability |
| [[2506.03079\|ORV]] | 4D semantic occupancy as a geometric prior conditioning a pixel video FM (CogVideoX + adaptive Gaussian-splat render + chunk-level action conditioning) | **18.8%** lower FVD, **+3.5%** SR in visual planning on Robodesk, **+13.7%**/**+6.5%** policy-learning gain for RoboVLM/SpatialVLA | the occupancy-conditions-pixel hybrid the table otherwise lacks, but occupancy is an *input prior* here, not the substrate the planner rolls forward like [[2603.28887\|OccSim]] — so it inherits the pixel substrate's drift, no horizon-to-divergence claim; its ORV-S2R sim-occupancy path is exactly the Risk note's derive-occupancy-from-sim route |
| [[2604.28115\|FreeOcc]] | training-free online open-vocab occupancy *construction* (SLAM backbone + geometrically-anchored 3D Gaussians + VLM semantics → Gaussian-to-occupancy projection), not a forecaster | **2×+** IoU/mIoU over self-supervised baselines on EmbodiedOcc-ScanNet, zero-shot generalization on ReplicaOcc where learned baselines collapse | builds present-frame occupancy without any 3D annotation or pose GT, directly answers the Risks note's "occupancy GT is scarce" concern, but doesn't forecast, so it feeds the substrate rather than testing A2's forward-model claim |
| [[2510.16729\|IR-WM]] | vision-centric 4D occupancy forecasting via implicit *residual* prediction of dynamic-only changes + Feature Alignment Module bounding autoregressive error | IoU_c **40.8** / wIoU_f **38.2** occupancy forecast, planning L2 **0.53m** (vs 0.85m) / collision **0.17%** on nuScenes | a second occupancy-WM horizon-stabilizer alongside [[2603.28887\|OccSim]], but a different anti-drift route (residual + Feature Alignment vs Warp-DiT rigid-transform) — and like OccSim it's a driving forecaster-for-planning, never ported to sub-cm tabletop, not a manipulation control loop; its Semi-Coupled static/dynamic split is the driving analog of H3's decoupling |

**Hypotheses & tests.** The FP bet — a *dense explicit* voxel substrate beats *pixels* on horizon at RoboCasa scale, conditions A1's head, and closes the in-loop perceive-imagine-act loop — decomposed. (That occupancy-is-the-forward-model-state is settled by [[2203.06856|ACID]] / [[2505.16249|3D-Occ-MPC]]; the front-line is the in-loop policy-forward-model conjunction nobody has run. The occupancy-vs-*latent*-4D horizon comparison and the raw sub-cm Warp-DiT resolution sweep belong to sibling **C1**, not A2.)
1. **H1 — A dense voxel-occupancy inner loop beats the pixel inner loop on horizon-to-divergence at RoboCasa scale.**
   - *Prediction*: swapping a manipulation WM's pixel-prediction loop for a *dense-voxel* occupancy loop (backbone fixed) raises rollout-horizon-to-geometric-divergence ≥10× — beyond what [[2203.06856|ACID]]'s implicit field or [[2505.16249|3D-Occ-MPC]]'s single-object grid demonstrated — and the 80× driving gain partially survives the scale change.
   - *Test*: matched-backbone dense-occupancy-vs-pixel forward models on [[2406.02523|RoboCasa]], hosting both WM contestants on [[2604.19092|RoboWM-Bench]]'s shared WM→action→step+final SR scorecard (its discriminative-power finding — execution accuracy varies even at high perceptual scores — is exactly the fixed-horizon-SR-hides-stability claim) and comparing the two inner loops in genuine closed loop via [[2510.18135|World-in-World]] (counterfactual rollout → revision policy → task SR, manipulation in its suite); measure frames-to-divergence + downstream SR.
   - *Row*: OccSim / CTRL-WORLD / 3D-Occ-MPC.
   - *Falsifier*: occupancy ≤ pixel horizon at RoboCasa scale → the driving gain is scale-specific and ACID/3D-Occ-MPC's single-object result doesn't densify.
2. **H2 — Forecast-occupancy conditioning of A1's head degrades gracefully as voxel resolution coarsens.**
   - *Prediction*: the in-loop conditioning benefit (H4) survives resolution coarsening — the forecast-occupancy-conditioned A1 head keeps its long-horizon SR edge over present-frame conditioning down to a usable voxel resolution, rather than collapsing the moment the grid is coarser than contact scale. (The raw Warp-DiT geometric-error-bound sub-cm sweep is ceded to C1-H4; here the axis is action-head SR, not reconstruction error.)
   - *Test*: sweep voxel resolution on a contact task and report the resolution at which forecast-conditioning stops beating present-frame conditioning on A1's head SR.
   - *Row*: OccSim / 3D-Occ-MPC.
   - *Falsifier*: the conditioning benefit vanishes above contact scale → the in-loop occupancy loop needs a resolution the planner can't afford, and A2's value collapses back to C1's raw-substrate horizon claim.
3. **H3 — Decoupling the dynamic agent stabilizes object-motion forecasting.**
   - *Prediction*: applying [[2603.28887|OccSim]]'s static-scene / dynamic-agent decoupling to manipulation (object+gripper = dynamic agent) lowers object-motion forecast error vs a monolithic occupancy predictor.
   - *Test*: ablate the decoupling on a pick-place occupancy forecaster.
   - *Row*: OccSim / RigidFormer.
   - *Falsifier*: the monolithic predictor matches the decoupled one → the static/dynamic split is driving-specific.
4. **H4 — Predicted future-occupancy can condition A1's action head directly.**
   - *Prediction*: feeding A2's forecast occupancy into A1's point-cloud head closes a fully-geometric perceive-imagine-act loop that beats the same head on present-frame geometry alone on long-horizon tasks.
   - *Test*: condition the A1 head on forecast vs present occupancy; report long-horizon SR on [[2510.18135|World-in-World]]'s closed-loop embodied-utility protocol (WM counterfactual rollout → plan selection → task success), the standardized inner-loop substrate the perceive-imagine-act comparison otherwise lacks.
   - *Row*: OccSim / DexWorldModel.
   - *Falsifier*: forecast conditioning ≤ present-frame → imagination adds nothing for the action head.
5. **H5 — The occupancy forward model's in-loop counterfactual revision beats a pixel forward model's, beyond the raw horizon gain.**
   - *Prediction*: in a closed perceive-imagine-act loop (counterfactual rollout → revision policy → task SR), the occupancy forward model yields better action *revision* than a pixel forward model even after controlling for raw horizon-to-divergence (H1), because the planner can collision-query the forecast occupancy directly, where the pixel rollout must be re-parsed into geometry before it can revise. (This is A2's in-loop object; the occupancy-vs-*latent*-4D substrate comparison it used to make is ceded to C1-H1.)
   - *Test*: occupancy vs pixel forward model in [[2510.18135|World-in-World]]'s counterfactual-rollout → revision-policy → task-SR loop, at *matched* horizon-to-divergence; report revision-SR delta.
   - *Row*: OccSim / CTRL-WORLD.
   - *Falsifier*: occupancy revision ≤ pixel revision at matched horizon → occupancy's only value is raw horizon, adding nothing in-loop, and A2 reduces to the substrate-horizon claim C1 already owns.
6. **H6 — A dense-voxel occupancy loop is *deployable-latency-competitive* with the sparse-explicit substrates, scored as a hypothesis not asserted.**
   - *Prediction*: "latency is the deployable axis that favours a sparser substrate" is a *testable hypothesis*, not a settled fact — the sparse-explicit incumbents run at deployable rates (PointWorld, 2601.03782, ~0.12 s per 10-step prediction ≈ ~0.012 s/step in MPC, *not* 0.1 s/step), so a dense-voxel occupancy loop must be shown to fall within a usable multiple of that on the *same* hardware, or the horizon win (H1) is paid for in a latency the planner can't afford. The bet is that dense-voxel occupancy clears a usable latency bar *and* keeps H1's ≥10× horizon edge, not that voxels are free.
   - *Test*: matched-hardware action-latency-per-MPC-step for the dense-voxel occupancy loop vs the sparse-point/particle baselines (PointWorld point-flow, [[2506.23126|ParticleFormer]] particles) at the same horizon and task set, plotting horizon-to-divergence against latency so the deployability claim is read off a *frontier*, not a single number.
   - *Row*: OccSim / ParticleFormer / 3D-Occ-MPC.
   - *Falsifier*: dense-voxel latency is many multiples of the sparse-explicit baselines at equal horizon → "dense occupancy" loses the deployable-substrate race to sparse explicit geometry, and the voxel qualifier is a moat of one word.

> [!warning] Risks
> - **Cross-domain transfer may not hold** — driving occupancy is meter-scale and mostly-static; manipulation needs sub-cm dynamic occupancy. → H2's resolution sweep + H3's dynamic-agent analog are the explicit go/no-go; report the scale at which the Warp-DiT bound breaks.
> - **The "deployable axis" may favour a sparser explicit substrate** — sparse-point incumbents (PointWorld ~0.12 s/10-step, ParticleFormer particles) already run in real-time MPC, so dense voxels can win horizon yet lose latency. → H6 makes deployability a *measured frontier* (horizon-to-divergence vs matched-hardware latency), not an asserted advantage; report the latency multiple before claiming the dense-voxel substrate is deployable.
> - **Benchmark validity for the OOD/horizon claim** — frames-to-divergence and zero-shot transfer can be inflated by a suite that mixes geometric drift with appearance drift or scores perceptual plausibility instead of executable action. → Host H1 on [[2604.19092|RoboWM-Bench]]'s executability-discriminating step+final SR (it shows execution accuracy varies even at high perceptual scores) and report *geometric* divergence separately, so the horizon claim is not a perceptual-fidelity artifact.
> - **Occupancy ground truth is scarce in manipulation datasets.** → Derive occupancy from depth + known gripper geometry (as driving derives it from LiDAR), or pretrain the forecaster in sim where occupancy is free.
> - **Voxel grids are memory-heavy at sub-cm resolution.** → Sparse/hierarchical occupancy (octree) + bound the claim to the working-volume around the end-effector rather than the full scene.

### A3 — Depth-Token Bridges: 3D-Awareness into Pretrained 2D Policies Without Re-Training

| | |
|---|---|
| **Cluster** | A — Geometry-Native Policies |
| **Thesis** | A 2D policy's geometry deficit is a *missing channel*, not a *wrong backbone* — so a cheap depth-token side-input can supply it without disturbing the pretrained semantic alignment. The field assumes 3D-awareness needs a full parallel 3D branch and re-training, forcing a false choice between RGB-only (cheap, geometry-blind) and full-3D (capable, expensive). The bet is in First-principles below. |
| **Anchor papers** | [[2504.05786\|3D-Spatial-Reasoning-in-LLM-Survey]] (survey), [[2506.20134\|3D-World-Models-Survey]] (survey), [[2510.16732\|World-Models-for-Embodied-AI-Survey]] (survey), [[2306.03310\|LIBERO]] (benchmark), [[2510.14836\|QDepth-VLA]] (method), [[2508.09071\|GeoVLA]] (method) |
| **Key targets** | +8.8% LIBERO-Spatial, +29.7% long-horizon over open_pi_0, +10–20% real-robot ([[2510.14836\|QDepth-VLA]], single-view) on [[2306.03310\|LIBERO]]'s 4-suite SR (SPATIAL/OBJECT/GOAL/100); recover ≥80% of [[2508.09071\|GeoVLA]]'s full-3D-branch gain (97.7% LIBERO) at side-channel cost; alignment-preservation tracked via [[2505.05456\|SITE]]'s spatial-VQA→LIBERO-Spatial-SR correlation (**0.902** Pearson); [[2602.17951\|ROCKET]] 81.7% LIBERO-Plus (largest gains under Robot/Layout shift, 98.5% LIBERO at ~4% compute) — the train-time multi-layer-alignment robustness number A3's H4/H5 levers must match at zero inference cost |

**Why it matters.**
- **The gap**: A1 and A2 buy geometry by changing the architecture — a parallel point branch or an occupancy loop — but that strands the huge installed base of RGB-pretrained policies that already work and that nobody wants to re-train.
- **Today's answers**: the frozen-backbone 3D bolt-on is *already established* — [[2503.07511|PointVLA]] freezes the action expert and injects 3D via a lightweight modular block with no retraining (skip-block analysis to minimize disruption), explicitly motivated by not stranding the 2D-pretrained installed base; [[2510.13375|DepthVLA]] runs the twin depth-expert stream (94.9% LIBERO, 74.8% SimplerEnv); [[2408.05107|Depth-Helps]] (DI², IROS'24) already quantization-denoises depth on LIBERO (63.15% RGB-only, only **0.8%** below its RGB-D variant). At VLA scale: [[2510.14836|QDepth-VLA]] feeds depth as discrete VQ-VAE tokens to a depth-expert reading the VLM's vision features (+8.8% LIBERO-Spatial, +29.7% long-horizon over `open_pi_0`, single-view); [[2605.14950|Evo-Depth]] injects implicit depth via FiLM (95.4% LIBERO, 0.9B at 12.3 Hz); [[2606.03240|GeoAlign]] trains a depth encoder dropped at execution (99.0% LIBERO). None measure the *recovery-fraction frontier* against a full-3D branch on a common backbone, nor the *alignment-preservation* number.
- **The opening**: [[2510.12276|Spatial-Forcing]] aligns VLA features to a 3D foundation model with *no 3D at inference*, hitting 98.5% LIBERO at 3.8× faster training / 5.9× more data-efficiency — proof the geometry can be a frozen-backbone side-task, not a parallel stream.

**First-principles framing.**
- **First principle**: a 2D policy's spatial weakness is a *missing channel* (depth), not a corrupted representation. Adding the channel as discrete tokens through a decoupled expert supplies the metric cues — you don't rebuild the backbone to add a channel. The biology read: depth is layered on as an early, cheap channel, not a separate stream re-learned from scratch.
- **Assumption being challenged**: that a lightweight, frozen-backbone side-channel is *not enough* — held by [[2508.09071|GeoVLA]] itself, whose own paper argues the cheap alternatives fail and builds the expensive full dual-path branch because of it: injecting 3D directly into the VLM "disrupts the alignment between visual encoders and [language models]," and freezing the action expert while bolting on point features via a lightweight module — [[2503.07511|PointVLA]]'s own recipe — "hinders adaptation to the newly introduced point cloud modality." GeoVLA backs that verdict with real evidence, not just its architecture: state-of-the-art **97.7%** LIBERO, with ablations confirming the Point Embedding Network, end-effector token, and routing Mixture-of-Experts all contribute. A3 bets against that verdict, not around it: a depth-token side-channel — the exact class of recipe GeoVLA argues under-adapts — recovers most of GeoVLA's own full-3D-branch gain at a fraction of the cost, without the alignment disruption GeoVLA's critique of direct-VLM injection predicts.
- **The bet**: the deliverable is the *measurement* nobody has produced, not the existence claim PointVLA settled — specifically, (i) on one common backbone the recovery-fraction-vs-added-params curve for a depth-token bridge has a clear *knee* recovering ≥80% of [[2508.09071|GeoVLA]]'s full-3D-branch SR gain well below full-branch cost (no found paper plots this), and (ii) the frozen-backbone side-channel perturbs a held-out VQA semantic-alignment probe *measurably less* than full-branch fusion that backprops into the backbone — the alignment-preservation cost everyone assumed but [[2503.07511|PointVLA]] never numbered.

**Related research papers.** One table on the axis the direction turns on — *where and how the geometry is injected into a pretrained 2D policy* (single-view depth token / FiLM implicit depth / train-time-only alignment / full-3D branch upper bound / SSL-backbone pretext) — with `Key result` and what each leaves missing.

| System | Injection point | Key result | What's missing |
|---|---|---|---|
| [[2510.14836\|QDepth-VLA]] | quantized depth tokens → decoupled depth expert (single-view) | **+8.8%** LIBERO-Spatial, **+29.7%** long-horizon, **+10–20%** real | never measured head-to-head vs a full-3D branch on one backbone — no recovery-fraction curve |
| [[2503.07511\|PointVLA]] | freeze action expert + lightweight modular 3D injection block, no retraining (skip-block) | beats 2D IL across tasks, long-horizon from **20** demos/task, cross-embodiment | states A3's mechanism + installed-base assumption verbatim, but *sensed* point clouds (not single-view depth tokens) and never plots the recovery-fraction-vs-full-branch knee or the alignment-drift number |
| [[2510.13375\|DepthVLA]] | parallel depth-expert stream beside the VLA | **94.9%** LIBERO, **74.8%** SimplerEnv WidowX (vs π0 58.8%), 79% real progress | the twin depth-expert, but a heavier parallel stream — no recovery-fraction-vs-cost frontier on a common backbone |
| [[2408.05107\|Depth-Helps]] | depth-aware codebook (quantized depth), drop to RGB-only at inference | **63.15%** LIBERO RGB-only (only **0.8%** below RGB-D), 66.67% real | pre-answers H2 (quantization denoises depth) on LIBERO, but only one codebook setting — no capacity sweep, no full-branch ceiling |
| [[2605.14950\|Evo-Depth]] | implicit depth from multi-view RGB via FiLM (no sensor) | **95.4%** LIBERO, **90%** real, **0.9B** params at **12.3 Hz** | multi-view, and quantization-vs-FiLM contribution not isolated |
| [[2606.03240\|GeoAlign]] | depth encoder trained on RGB-D, dropped at execution | **99.0%** LIBERO, **78.8%** geometry-critical real (**+13.8 pp**) | geometry without sensing, but the alignment-preservation cost is unmeasured |
| [[2512.00903\|SwiftVLA]] | frozen-4D-VGGT spatiotemporal features distilled via mask-and-reconstruct, 4D branch dropped at inference | **94.7%** LIBERO, **0.53** RoboTwin 2.0 (vs SmolVLA 0.29), **18×** faster / **12×** less memory on Jetson Orin | train-time-only 4D, *spatiotemporal* not depth — recovery-fraction vs a full-3D branch unmeasured |
| [[2510.17439\|FALCON-Spatial-VLA]] | global 3D priors from RGB (optional depth/pose) into a Spatial-Enhanced Action Head, VLM alignment preserved | **70.0%** real cluttered (**+25.6%** over SpatialVLA), height-sensitive SR **60→80%** | injects into the head not the backbone (A3's alignment thesis), but on top of a full ESM, not a light side-channel |
| [[2510.12276\|Spatial-Forcing]] | feature-alignment to a 3D foundation model, no 3D at inference | **98.5%** LIBERO, **3.8×** faster training, **5.9×** data-efficient | train-time alignment, not a deployable depth channel — different lever |
| [[2605.10485\|VEGA]] | cosine-distill a 3D-aware DINOv2-FiT3D teacher, no inference overhead | RoboTwin 2.0 **67.5%/30.7%** | distillation, not an explicit side-channel the policy can attend to |
| [[2605.30350\|DynaFLIP]] | tri-modal-dynamics-guided encoder, no inference 3D | highest-SR frozen backbone on MetaWorld/RLBench; best LIBERO mean w/ DP | geometry baked into the encoder — not a modular bolt-on bridge |
| [[2508.09071\|GeoVLA]] | full parallel 3D point branch (the upper bound) | **97.7%** LIBERO | the *expensive* ceiling A3 measures against; risks disturbing semantic alignment |
| [[2606.03943\|PointAction]] | predicted-pointmap interface | 43.0% zero-shot xArm7 | generated geometry, full-head — the architecture-change A3 avoids |
| [[2504.20995\|TesserAct]] | depth-as-predicted-channel (RGB-D-N) | depth channel sharpens grounding | predicts depth, doesn't inject sensed/quantized depth into a frozen head |
| [[2502.13143\|SoFar]] | orientation grounding from 2D | a lightweight side-signal sharpens manipulation | orientation only — not a metric depth channel |
| [[2605.09963\|Spatial-Prediction-SP]] | spatial pretext tasks on MAE/MoCo/DINO | **+8.0%** ImageNet-1K, jigsaw 69.87%→90.24%, lower texture bias | adds the spatial channel at the SSL-backbone layer, not the policy head |
| [[2411.18623\|Lift3D-Foundation-Policy]] | two-stage lift of a *large-scale 2D-pretrained* model (DINOv2/CLIP): Stage-1 task-aware MAE *reconstructs depth* in affordance regions while distilling to preserve the 2D prior | **84.5%** MetaWorld (**+18.2%** over DP3), **88.0%** Adroit (**+21.3%** over DP3), **90%** real from 30 demos | the load-bearing 2024 precedent for A3's "missing channel, not wrong backbone" + "preserve semantic alignment" framing (H4) — a depth-reconstructing MAE plus 2D-prior distillation, distinct from the train-time-align-to-3D-FM lane ([[2510.12276\|Spatial-Forcing]]/[[2605.10485\|VEGA]]); but MetaWorld/Adroit not [[2306.03310\|LIBERO]], and no recovery-fraction-vs-full-branch curve |
| [[2605.24642\|GFM-VLA-Study]] | Early/Late Fusion + Spatial Forcing comparison | maps how to bolt geometric-foundation-model features on | a survey of integration strategies, not a single recovery-fraction result |
| [[2602.10098\|VLA-JEPA]] | implicit geometry in a JEPA latent | **97.2%** LIBERO | the latent alternative the bridge competes with on cost, not an explicit channel |
| [[2501.15830\|SpatialVLA]] | RGB-token spatial policy (the bolt-on host) | the frozen backbone the depth bridge attaches to | leaves all depth implicit — the baseline the bridge improves |
| [[2602.10698\|AugVLA]] | sensor-free monocular depth (VGGT) → back-projected point cloud → PointNet feature, regularized by a lightweight "Action Assistant" into the action expert | **54%** robocasa-gr1 (100 demos, vs Gr00T **50%**), beats Diffusion Policy/Gr00T on 5 real dexterous tasks | the same "non-disruptive bolt-on" claim as [[2503.07511\|PointVLA]] but via *estimated* depth → point feature (not single-view depth tokens like [[2510.14836\|QDepth-VLA]]); the Action-Assistant regularizer is asserted, never measured as alignment-preservation (H4) |
| [[2603.09079\|GST]] | anisotropic Gaussian spatial tokens (position + surface orientation + geometric confidence) + Depth-Aware-CoT, into a flow-matching action expert | **83.1%** LIBERO-Pro (vs SpatialVLA **76.8%**), **+9.2 pp** precision-insertion and **+5.4 pp** SimplerEnv over [[2510.13375\|DepthVLA]] | a *richer* geometry token than [[2510.14836\|QDepth-VLA]]'s scalar VQ depth (encodes orientation/confidence), and beats the [[2510.13375\|DepthVLA]] parallel stream — but still no recovery-fraction curve vs [[2508.09071\|GeoVLA]]'s full-3D ceiling, nor the alignment-drift number |
| [[2603.12772\|PVI]] | zero-initialized residual plug-in: auxiliary encoder (V-JEPA2 temporal / DINOv2) injected layer-wise into a *frozen* action expert via a trainable copy branch | **35.7%→59.7%** (**+24.0 pp**) on 20 bimanual sim tasks over fine-tuned GR00T N1.5; V-JEPA2 temporal **69.4%** vs DINOv2 **56.8%** | the encoder-agnostic frozen-expert side-channel A3's thesis names verbatim (the [[2503.07511\|PointVLA]] skip-block done generically), but the auxiliary signal is *temporal/semantic*, not a metric depth channel — so the geometry-specific recovery-fraction and alignment-cost stay open |
| [[2608.01826\|MVUCF]] | direct depth + cross-view correspondence supervision on the VLA's *own* upper hidden states (layers 8-15), no auxiliary branch, no depth/calibration at inference | depth MAE **4.9cm→0.44cm**, cross-view Hit@1 **0.4%→64%**, **+22.4 pp** LIBERO-Plus, **+23.3 pp** RoboTwin (38.6%→61.9%), real humanoid **81.7%** vs **66.7%** base | shapes the backbone's own action-facing tokens rather than a side-channel or auxiliary head like [[2603.09079\|GST]]/[[2603.12772\|PVI]]: the largest LIBERO-Plus jump in the table, but two objectives bundled (no depth-only vs cross-view-only recovery-fraction split against [[2508.09071\|GeoVLA]]'s full branch) |
| [[2602.17951\|ROCKET]] | residual *multi-layer* feature-alignment to a 3D vision foundation model (teacher) via a shared Matryoshka projector, train-time only — no 3D at inference | **98.5%** LIBERO at ~**4%** of prior-SOTA compute; **81.7%** LIBERO-Plus (largest gains under Robot/Layout shift); shared projector **80.0%→98.2%** | the multi-layer extension of [[2510.12276\|Spatial-Forcing]]'s single-layer alignment (and the LIBERO-Plus robustness number Spatial Forcing's row lacks), but still a train-time lever, not a deployable depth channel — recovery-fraction vs a full-3D branch on a common backbone unplotted |
| [[2607.25912\|SAM3D-VLA]] | object-level 3D priors distilled from a frozen SAM3D teacher into intermediate VLA visual features via a masked normalized-MSE alignment loss, train-time only | **99.1%** LIBERO, **71.6%** CALVIN 5-step, real occlusion SR **21.3%→44.3%** | a train-time-only distillation route like [[2510.12276\|Spatial-Forcing]]/[[2605.10485\|VEGA]]/[[2602.17951\|ROCKET]], but distilling *object-centric* 3D structure with open-vocab detection/segmentation masking, strengthens H5's train-time-matches-inference claim specifically on occlusion, still no recovery-fraction curve vs GeoVLA |
| [[2606.17040\|R2RDreamer]] | training-*data*-level geometry: lightweight 3D co-editing of object/EE pose → occlusion-aware projection → video-completion to synthesize geometrically-consistent RGB for 2D *and* VLA policies | **3.13%→21.9–40.6%** (Diffusion Policy) and **→21.9–46.9%** (π₀) real spatial-generalization; one source demo ≈ 5–15 human demos | injects geometry *before* the policy (augmentation), so it strands no backbone and adds nothing at inference — a different injection point than [[2510.14836\|QDepth-VLA]]'s feature channel; never compared head-to-head against an inference depth channel on matched data |
| [[2606.14048\|WAM4D]] | spatial register tokens distill geometry from a pretrained geometric head into a *frozen WAM backbone* (causal-mixture attention); entire 4D branch dropped at deployment | **91.8%** RoboTwin 2.0 (50 tasks), **90%** real sub-action SR on AstriBot S1, **525 ms** latency / **9.71 GiB** | the WAM analog of [[2512.00903\|SwiftVLA]]'s drop-at-inference distillation (and like it, *spatiotemporal/register* not a depth token) — extends H5 to world-action models, but measured on RoboTwin not [[2306.03310\|LIBERO]], so no shared-backbone recovery-fraction vs [[2508.09071\|GeoVLA]] |
| [[2608.08023\|Trajectory Field WAM]] | motion-alignment + destination-alignment losses distill a frozen 3D-trajectory-field teacher into a WAM's DiT features; both losses train-time-only, no 3D at inference | LIBERO-Plus avg **62.21%→71.01%** (**+8.8 pp**), camera perturbation **+17.26 pp**, ID LIBERO **98.2%→98.6%**, RoboTwin 2.0 **91.34%** vs **90.58%** | a second WAM-scale H5 witness beside [[2606.14048\|WAM4D]] — a *trajectory-field* teacher rather than register tokens, and the sharpest isolated camera-viewpoint OOD gain from a single train-time loss in the table — but like WAM4D no shared-backbone recovery-fraction vs [[2508.09071\|GeoVLA]]'s full branch |
| [[2608.03727\|Track4Action]] | frozen `Track4World` 3D tracker (trajectories, motion, visibility, camera params) distilled into learnable *track queries* via L2 alignment; tracker discarded at deployment | **82.3%** LIBERO-Plus (+3.0 pp over [[2603.25399\|LaMP]] 79.3%), RoboTwin 2.0 **80.44%/81.48%**, real bimanual **67.5%** (+20 pp over alignment-free) | distills a *motion/trajectory* teacher rather than depth or feature alignment like the rest of the table: a genuinely different training-only signal, but never measured against a depth-token bridge on a common backbone (no recovery-fraction vs QDepth-VLA/GeoVLA) |
| [[2604.21914\|VistaBot]] | geometry as view-normalizer: 4D geometry estimation (rel-pose + depth) → reproject to training-view → video-diffusion synthesizes view-aligned latent features the policy reads | View-Generalization-Score **0.24→0.67** (ACT, **2.79×**) and **0.33→0.87** (π₀) sim; **0.21→0.72** / **0.27→0.79** real at ~**3 Hz** | uses geometry to *cancel* viewpoint shift (H3's second-view axis) rather than inject a depth channel — heavier (VDM synthesis) than a token side-input, and the view-robustness gain is not decomposed into a depth-token-equivalent |
| [[2512.16811\|GeoPredict]] | future robot-keypoint trajectories + 3DGS future-scene-geometry prediction, both *training-time-only* supervision shaping the VLA's internal representations | **+10.1 pp** RoboCasa Human-50 (42.3%→52.4%), **96.5%** avg LIBERO (vs UniVLA 95.2%/π0 93.9%), real geometry-generalization **50%→95%**, spatial-generalization **60%→85%** | a second H5 train-time-only witness alongside Spatial Forcing/SwiftVLA/ROCKET, but 3DGS-*scene*-geometry refined by predicted kinematics rather than depth or register tokens — the sharpest real-world geometry-generalization number in the table, yet still no recovery-fraction curve vs [[2508.09071\|GeoVLA]]'s full branch |
| [[2608.10756\|Semantic-3DGS Mobile Manipulation]] | active multi-view 3DGS (VGGT geometry + CLIP/DINOv2 semantics) injected via zero-init adapters into only the *final five* of many frozen diffusion blocks | long-horizon **60%** vs PointVLA **40%**/DexVLA **28%**; cluttered **74%** vs PointVLA **46%**; late-block injection beats all-block **82%→75%** avg SR at **80 ms** vs **175 ms** latency | a direct *injection-depth* ablation (late-block vs all-block) the table lacks — evidence the side-channel doesn't need to touch every layer to work, sharpening H1's cost-frontier — but a 3DGS-scene source (heavier build than a depth token) and no recovery-fraction curve vs GeoVLA either |

**Hypotheses & tests.** The FP bet — the cost-efficiency frontier and alignment-cost nobody has measured, not "a depth bridge works" ([[2503.07511|PointVLA]] settled that) — decomposed. H1 (recovery-fraction knee) + H4 (alignment-preservation number) are the front-line; H2 is largely pre-answered by [[2408.05107|Depth-Helps]] and re-run only to extend it to a capacity sweep.
1. **H1 — The recovery-fraction curve has a knee well below full-branch cost.**
   - *Prediction*: sweeping depth-token capacity (codebook size, expert depth) on one common backbone, the recovered fraction of [[2508.09071|GeoVLA]]'s full-3D-branch SR gain reaches ≥80% at a small fraction of the added parameters, with a clear knee — the frontier neither [[2503.07511|PointVLA]] nor [[2510.13375|DepthVLA]] plotted.
   - *Test*: capacity sweep on a common backbone; plot recovered-fraction vs added params on [[2306.03310|LIBERO]]-Spatial / long-horizon SR (the named suite the sweep is read off, against GeoVLA's full-3D ceiling).
   - *Row*: QDepth-VLA / GeoVLA / PointVLA.
   - *Falsifier*: recovery scales linearly to full-branch cost → no cheap operating point exists.
2. **H2 — Quantization, not just the decoupled expert, drives the noise-robustness gain (extend pre-answered).**
   - *Prediction*: [[2408.05107|Depth-Helps]] already shows quantized depth costs only 0.8% to drop; extending to a 2×2 (quantized vs continuous depth) × (decoupled vs fused expert) ablation, quantization contributes a *separable* robustness gain beyond the expert across a capacity range.
   - *Test*: the 2×2 on [[2510.14836|QDepth-VLA]]'s setup under depth-estimator noise, sweeping codebook size.
   - *Row*: QDepth-VLA / Depth Helps / Evo-Depth.
   - *Falsifier*: continuous-depth matches quantized at equal expert across the sweep → quantization is incidental beyond Depth Helps' single setting.
3. **H3 — A second view's depth tokens close the residual gap to full-3D.**
   - *Prediction*: single-view depth-token gain saturates below the full-3D ceiling; adding a second view's depth tokens closes most of the residual to methods that consume sensed point clouds.
   - *Test*: single-vs-two-view depth tokens vs a sensed-point-cloud full-3D method.
   - *Row*: QDepth-VLA / GeoVLA.
   - *Falsifier*: a second view doesn't help → the bridge ceiling is intrinsic, not view-limited.
4. **H4 — The side-channel perturbs semantic alignment far less than full-branch fusion.**
   - *Prediction*: [[2505.05456|SITE]]'s spatial-VQA probe shows the frozen-backbone depth expert perturbs semantic alignment measurably less than full-branch fusion that backprops into the backbone — and because SITE's CAA tracks LIBERO-Spatial manipulation SR at **0.902** Pearson, the probe drift it measures demonstrably predicts the downstream SR the side-channel must not damage.
   - *Test*: [[2505.05456|SITE]] spatial-VQA-probe drift, depth-expert vs full-branch fusion, same data.
   - *Row*: QDepth-VLA / GeoVLA.
   - *Falsifier*: the side-channel perturbs alignment as much as fusion → the "non-disruptive" claim fails.
5. **H5 — Train-time-only distillation matches the inference depth channel on geometry-bound tasks.**
   - *Prediction*: [[2510.12276|Spatial-Forcing]]-style train-time alignment recovers a comparable fraction of the full-3D gain to QDepth-VLA's inference channel on geometry-critical tasks, at *zero* inference cost — so the cheapest deployable form needs no depth at test; [[2512.00903|SwiftVLA]]'s mask-and-reconstruct distillation (94.7% LIBERO with the 4D branch dropped) extends this to *spatiotemporal* (not just depth) features, predicting the dropped channel can be richer than depth without an inference cost.
   - *Test*: train-time-distill ([[2510.12276|Spatial-Forcing]] depth, [[2512.00903|SwiftVLA]] spatiotemporal) vs inference-depth-token on geometry-critical real tasks at matched backbone. [[2608.03727|Track4Action]] extends the family with a *tracker* teacher (motion/trajectory/visibility, not depth) also discarded at deployment (82.3% LIBERO-Plus, +20 pp real bimanual over its own alignment-free control), another train-time-only signal beating its no-distillation baseline, worth folding into the same recovery-fraction comparison once run. [[2512.16811|GeoPredict]] (3DGS future-geometry + kinematics, zero inference cost, 50%→95% real geometry-generalization) and [[2608.08023|Trajectory Field WAM]] (motion + destination trajectory-field alignment, zero inference cost, +8.8 pp LIBERO-Plus / +17.26 pp under camera perturbation) are two further train-time-only witnesses worth folding into the same matched-backbone comparison.
   - *Row*: Spatial Forcing / SwiftVLA / QDepth-VLA / Track4Action / GeoPredict / Trajectory Field WAM.
   - *Falsifier*: train-time alignment underperforms the inference channel materially → depth must be present at inference.
6. **H6 — The depth-source cost is the hidden variable in the recovery-fraction frontier, and pricing it can invert the bridge's verdict (blind-spot fold).**
   - *Prediction*: H1's recovery-fraction-vs-params curve is plotted as if the depth tokens arrive free, but the depth itself comes from either a sensor (a hardware tax most real cells don't carry) or a *second network* (a monocular/VGGT depth estimator, as in [[2510.14836|QDepth-VLA]]/[[2602.10698|AugVLA]]) whose params and error must be charged to the bridge — and that estimator is worst on the textureless/specular tail where the geometry channel is supposed to help most. Once the depth-producing network is counted into the x-axis and its hard-tail noise into the y-axis, the bridge's knee shifts right and may fall *below* the zero-sensing-tax train-time-distillation path (H5), inverting the "deployable depth channel beats train-time-only" recommendation.
   - *Test*: re-plot H1's recovery-fraction frontier with the depth estimator's params/latency added to the bridge's cost and with depth produced under realistic (not clean-rendered) noise on the geometry-critical tail; compare the cost-charged bridge knee against the zero-inference-cost train-time-distillation point ([[2510.12276|Spatial-Forcing]]).
   - *Row*: QDepth-VLA / AugVLA / Spatial Forcing.
   - *Falsifier*: the cost-charged bridge still beats the train-time-only path on the geometry-critical tail → the depth-source tax is affordable and the inference depth channel is the right deployable form.

> [!warning] Risks
> - **The bridge may plateau below the full-3D ceiling** on the hardest geometry-bound tasks. → H1's recovery-fraction curve sets an honest expectation — frame the contribution as a *cost-efficiency frontier*, not SR-SOTA; concede the ceiling where it appears.
> - **The depth source is an unpriced second network or sensor** — the "cheap" bridge hides a monocular/VGGT depth estimator (or a depth sensor) whose params and hard-tail error belong on the cost axis, and whose noise is worst exactly on the textureless/specular tasks the channel is meant to rescue. → H6 charges the depth-source cost into H1's frontier and re-plots the knee under realistic estimated-depth noise; gate the "deployable depth channel beats train-time-only" claim on the *cost-charged* comparison against the zero-inference-cost distillation path (H5).
> - **Depth-token quality depends on the depth estimator.** → [[2510.14836|QDepth-VLA]]'s quantization buffers estimator noise (H2); report sensitivity to estimator quality so the claim is bounded to realistic depth.
> - **Side-channel may still subtly perturb semantic alignment.** → H4's VQA-probe quantifies the perturbation; gate the "non-disruptive" claim on it rather than assuming the frozen backbone is untouched.

---

## Cluster B — Spatial Reasoning as a 3D-Grounded Cognition Layer

*Reasoning happens over explicit metric geometry, upstream of the action head — the policy acts on the output of a 3D-grounded cognition step, not on raw RGB. The two directions are the spatial and temporal halves of the same layer: a scene-graph that grounds *where* objects are (B1) and a 4D-consistency constraint that keeps that geometry coherent *over time* (B2), each feeding the metric structure the action heads downstream consume.*

### B1 — Explicit 3D Scene-Graph CoT for Metric Spatial Reasoning

| | |
|---|---|
| **Cluster** | B — 3D-Grounded Cognition |
| **Thesis** | Metric spatial relations are a *graph over geometric entities*; free-form language CoT only describes that graph lossily and hallucinates when ungrounded. The MLLM field trusts language CoT over RGB and assumes scale alone closes the spatial gap. The bet is in First-principles below. |
| **Anchor papers** | [[2504.05786\|3D-Spatial-Reasoning-in-LLM-Survey]] (survey), [[2506.20134\|3D-World-Models-Survey]] (survey), [[2604.22748\|Agentic-World-Modeling-Survey]] (survey), [[2507.13362\|VLM-Spatial-Reasoning-RL]] (method), [[2501.10074\|SpatialCoT]] (method), [[2601.13304\|CausalSpatial]] (benchmark) |
| **Key targets** | CVBench ≥77.69% ([[2507.13362\|VLM-Spatial-Reasoning-RL]]); per-relation-type metric-spatial-QA on [[2412.07825\|3DSRBench]] (12 question types / 4 categories, FlipEval+CircularEval bias controls, 6D-viewpoint OOD split, human **95.7%** vs LMM **52.0–60.3%**); manip SR ≥82.57% / nav SR ≥61.83% ([[2501.10074\|SpatialCoT]]); close part of the [[2601.13304\|CausalSpatial]] 54.17%→84.49% human gap; [[2511.07403\|SpatialThinker]] 78.2% CV-Bench (exceeds the 77.69% CVBench target, +12.1% over GPT-4o on 3DSRBench); [[2504.20024\|SpatialReasoner]] 60.3% 3DSRBench (the LMM upper bound of the 52.0–60.3% band the human-95.7% gap is measured against) |

**Why it matters.**
- **The gap**: [[2504.05786|3D-Spatial-Reasoning-in-LLM-Survey]] and [[2601.13304|CausalSpatial]] reach the same diagnosis — MLLMs don't reason about metric space, they *describe* it in language and hallucinate when ungrounded; on causal tasks (collision, occlusion, trajectory) GPT-5 scores 54.17% vs human 84.49% and is overconfident (low Not-Sure-Rate).
- **Today's answers**: the structure-first + RL + anti-shortcut-OOD recipe is *already demonstrated* — [[2601.01984|Thinking-with-Blueprints]] builds an explicit structured spatial representation, reasons over it, and adds RL rewards + anti-shortcut augmentation (92.7% SAT-val, **79.7%** SAT-test OOD, +16.4% over base), and [[2512.16909|MomaGraph]] predicts a scene-graph via RL with edge-level reward (71.6% MomaGraph-Bench, 70% real long-horizon SR). [[2507.13362|VLM-Spatial-Reasoning-RL]] decomposes the scene into a relational graph before answering (77.69% CVBench, +19.5% Depth-OOD where SFT degrades); [[2501.10074|SpatialCoT]] pushes it into action via coordinate alignment (82.57% manip); [[2606.24338|RoBoSR]] makes the 3D scene graph the *state space* of a manipulation reasoner and uses RFT to fix grounding/state-consistency errors (+11.7% on hard spatial tasks *by reducing hallucinations*) — circumstantial support that wrong-graph errors dominate (H3), but it never numbers graph-construction accuracy. None isolate whether the gain is the *metric* edges vs topological ordering, nor instrument graph-construction accuracy as the bottleneck.
- **The opening**: [[2604.24300|ReVSI]]'s dummy-video stress test shows fine-tuned models catastrophically hallucinate *absent* objects, and [[2605.30161|Why-Far-Looks-Up]] traces a "vertical-distance entanglement" shortcut (36.9 pp gap) — proof the deficit is a *missing metric representation* a scene-graph supplies, not a data shortage.

**First-principles framing.**
- **First principle**: spatial relations form a graph over geometric entities (objects with metric positions, pairwise relations, contacts). Reasoning correctly *is* operating on that graph; language is a lossy serialization that drops the metric structure the task needs. [[2503.11089|EmbodiedVSR]]'s dynamic scene-graph + physics-constrained CoT (+18.4% Arm Feasibility, 80% real reassembly) shows the graph, made explicit, is what carries the gain.
- **Assumption being challenged**: the scaling view that a big-enough multimodal model reasons about space implicitly is *already refuted* — [[2507.13362|VLM-Spatial-Reasoning-RL]] shows naive CoT can *hurt* and only *structured* scene-graph CoT helps; [[2601.13304|CausalSpatial]] shows even GPT-5 sits 30 points below humans. And the *structure+RL+OOD-generalizes* claim is now consensus too ([[2601.01984|Thinking-with-Blueprints]], [[2512.16909|MomaGraph]]). The open assumption B1 attacks: that adding *any* graph is what matters — when the untested question is *which property of the graph* (metric edges vs topological ordering) carries the gain, and *where* the graph fails (construction vs reasoning).
- **The bet**: the gain is carried by *metric* content, not graph topology, and bottlenecked by *construction*, not reasoning — neither isolated by [[2601.01984|Thinking-with-Blueprints]] or [[2512.16909|MomaGraph]]. Specifically, (i) a scene-graph with metric edge labels (distances, angles) beats a purely topological graph on CVBench, with the gap concentrated in metric-relation question types and ≈0 on object-naming; and (ii) when scene-graph CoT errs on the [[2601.13304|CausalSpatial]] 54.17%→84.49% causal slice, most errors trace to a *wrong graph* (hallucinated/missing entity), so graph-construction accuracy predicts answer accuracy.

**Related research papers.** One table on the axis the direction turns on — *how the spatial representation upstream of the answer is structured* (metric scene-graph / topological relations / depth-rationale / simulate-then-reason / symbolic state / 3D-proxy tokens / reconstructed-3D / diagnostic) — with `Key result` and what each leaves missing. The diagnostic rows prove the gap is metric; the method rows attack it with structure.

| System | Spatial representation | Key result | What's missing |
|---|---|---|---|
| [[2507.13362\|VLM-Spatial-Reasoning-RL]] | explicit metric scene-graph + GRPO | **77.69%** CVBench (+5–15%), **+19.5%** Depth-OOD where SFT degrades | doesn't separate metric-edge from topological-edge contribution |
| [[2601.01984\|Thinking-with-Blueprints]] | explicit structured spatial representation + RL reward + anti-shortcut aug | **92.7%** SAT-val, **79.7%** SAT-test OOD (+16.4% over base), 60.7% BLINK, 7B | records positions/sizes + RL-OOD (settles B1's H2), but never decomposes metric-vs-topological gain, nor instruments construction accuracy |
| [[2512.16909\|MomaGraph]] | scene-graph prediction via RL with edge-level reward | **71.6%** MomaGraph-Bench, **+4.8%** BLINK, **70%** real long-horizon SR | RL over a learned graph with edge reward, but no metric-edge isolation (H1) and no causal-task split (H3) |
| [[2603.22279\|3D-Layout-R1]] | RL-tuned 3D layout reasoning (IoU/collision reward) | **+15%** Mean IoU, **1.000** Collision-Free on Sorting, 7B/8B ≥ commercial | layout-as-RL-target, but layout-only — not a relational metric scene-graph with contact edges |
| [[2501.10074\|SpatialCoT]] | coordinate-aligned CoT (language→coords) | **82.57%** manip / **61.83%** nav SR | the B→A handoff, but never benchmarked vs a geometric action head |
| [[2503.11089\|EmbodiedVSR]] | dynamic scene-graph + physics-constrained CoT | **+18.4%** Arm Feasibility over GPT-4o, **80%** real reassembly | scene-graph for robots, but graph-construction accuracy not reported separately |
| [[2606.24338\|RoBoSR]] | structured 3D scene graph $M_{sg}=(O_t,E_t)$ as the *primary state space* (objects + relations), SFT for graph interpretation then RFT for grounding/state-consistency correction, on a 1.6M Manip-Cognition dataset | **+11.7%** absolute on hard spatial tasks (RFT reduces hallucination / commonsense / goal errors), GSR-bench SOTA across difficulty, real-world causally-constrained multi-step execution + autonomous state-consistency correction | the scene-graph-as-state-space form B1's first principle names verbatim — and RFT-for-state-consistency directly attacks H3's wrong-graph error mode (its 11.7% gain is *from reducing hallucinated/missing entities*) — but it never decomposes metric-edge vs topological gain (H1) nor reports graph-construction accuracy as a separate axis (H3's instrument), conflating construction with reasoning the way the cluster's other rows do |
| [[2601.11442\|Map2Thought]] | metric cognitive map (symbolic grid + continuous metric scale) + deterministic geometric Cog-CoT | **61.0%** VSI-Bench (top open-source), **59.9%** at half the data | a *map* not a relational graph — verifiable on metric queries, but no pairwise contact/relation edges |
| [[2505.20279\|VLM-3R]] | reasoning over reconstructed 3D | grounds reasoning in reconstructed geometry | reconstruction cost; not a lightweight graph |
| [[2608.02980\|Qwen-3D]] | voxel-merged multi-view tokens in 3D world-space + 3D RoPE + a *direct* token-to-mask decoder (no text-space/proposal bottleneck) | **62.8%** ScanRefer Acc@25 (+30% over 3D-LLM, +12% over LLaVA-3D), closing to specialist UniVLG (63.5%); OOD Locate-3D **55.7%/43.4%** beats UniVLG **32.3%/24.6%**; preserves 2D (RefCOCO 88.1% vs base 89.1%) | the sharpest LMM-vs-specialist gap closure yet, and names *decoding* (not scene-graph structure) as the lever, but it is pure VQA/grounding, no action head, no metric-vs-topological decomposition (H1), no manipulation SR |
| [[2506.04220\|Struct2D]] | structured 2D → spatial reasoning | cheap structure without full 3D | 2D structure — leaves the metric-vs-topological question open |
| [[2605.08064\|Proxy3D]] | compact semantically-clustered 3D proxy tokens | **700 vs 8000** tokens, efficient metric-3D interface | a token interface, not a reasoned relational graph |
| [[2605.06758\|R3L]] | relative-relation invariant decomposition + consistent imagination | **0.0%** collision/out-of-bound, **1.8×** faster convergence | metric scene-graph *construction*, but layout-only, not full CoT |
| [[2606.06076\|MGSD]] | symbolic-state self-distillation for visual planning | **11.2%→30.5%** on FrozenLake/Maze/MiniBehaviour | symbolic state for planning — not metric 3D relations |
| [[2507.12508\|MindJourney]] | simulate-then-reason via controllable video WM | SAT-Real o1 **74.6%→84.7%** | imagination at test time; gain depends on the WM, not a graph |
| [[2604.26934\|World2VLM]] | distilled WM imagination into the VLM (GRPO) | **+15.44–15.98 pp** across 4 spatial benchmarks | internalizes simulate-then-reason — but no explicit relational graph |
| [[2606.03988\|Imaginative-Perception-Tokens]] | flow-matching tokens simulate unobserved views | **67.3%** Multiview Counting, beats textual CoT; gains persist w/o inference gen | view simulation, not metric relational structure |
| [[2605.18162\|SAGE-Spatial-VLM]] | self-evolving geometric-logic duality + GRPO | **+15.0** MindCube at **<37%** of the data | targets "pseudo-understanding" via RL, but not a metric graph per se |
| [[2604.14144\|SpatialEvo]] | deterministic geometric environment for RL labels | **54.7** avg (7B), **−5.1 pt** without DGE GT | structure-as-supervision, but the graph isn't the reasoning substrate |
| [[2505.12448\|SSR]] | raw depth → structured rationales → compact latents | **+13.6%** spatial accuracy, CoT cost **23.16s→0.32s** | depth-grounded rationale (B1×A3 tie) — not a relational scene-graph |
| [[2506.03135\|OmniSpatial]] | comprehensive spatial-reasoning benchmark + built-in PointGraph scene-graph baseline | **>56%** top VLM vs human **92.6%** (30-pt gap); Complex-Spatial-Logic **30–40%**; PointGraph + SpatialCoT boost, SFT **+7.82** avg | a standardized suite where B1's metric-vs-structure comparison runs against a *built-in* scene-graph baseline, but reports answer accuracy, not graph-construction accuracy (H5) |
| [[2605.29074\|Embodied3DBench]] | metric-3D perception suite (3D bbox / object point / grasp point) + View-Augmented-CoT | 13 VLMs, none robust on low-level metric 3D; score correlates with downstream LIBERO SR | the load-bearing metric-3D substrate for the reasoning-vs-perception split (H3), but a diagnostic, not a deployable graph layer |
| [[2601.13304\|CausalSpatial]] | causal-spatial diagnostic + COW visual simulation | GPT-5 **54.17%** vs human **84.49%**; visual sim suppresses hallucination | measures the gap; the COW is a probe, not a deployable graph layer |
| [[2604.24300\|ReVSI]] | dummy-video hallucination stress test | fine-tuned models hallucinate absent objects catastrophically | a diagnostic — exposes shortcut-vs-metric, prescribes no fix |
| [[2605.30161\|Why-Far-Looks-Up]] | vertical-distance-entanglement probe | **36.9 pp** shortcut gap (consistent vs counter) | proves reasoning is shortcut, not metric — no remedy |
| [[2605.30557\|SpatialUncertain]] | unanswerable-question calibration probe | overconfident: **~30%** occlusion, **<10%** perspective ambiguity | the calibration half of the gap; no grounding mechanism |
| [[2607.21072\|ProVisE]] (SpatialGen-Bench) | spatial cognition scored through *generated pixels*, not text | top text model **61.04%** vs top visual model **54.49%** vs human **87.79%**; **88.03%** of visual-model errors are genuine spatial-judgment failures | a second, orthogonal diagnostic instrument for the same metric gap [[2601.13304\|CausalSpatial]]/[[2605.29074\|Embodied3DBench]] measure through text output — still a diagnostic, not a deployable graph layer |
| [[2511.07403\|SpatialThinker]] | scene-graph-supervised CoT + dense lexicographic spatial reward (CIoU) + GRPO | **78.2%** CV-Bench (+12.1% over GPT-4o on 3DSRBench), 1.8× the gain of sparse-reward RL, on only **7K** synthetic samples | a *denser* sibling of [[2507.13362\|VLM-Spatial-Reasoning-RL]] — the reward rewards CIoU box-alignment (perceptual grounding), not metric-edge labels, so still doesn't isolate metric-vs-topological content (H1) |
| [[2504.20024\|SpatialReasoner]] | explicit 3D location+orientation representation + SFT→RL composite reward | **60.3%** 3DSRBench (beats Gemini 2.0 Flash 49.8%); ablation: **51.9%→58.3%** when explicit-3D removed; states perception (3D-property estimation) is the bottleneck, not computation | echoes B1's H5 (construction-bound) and beats MomaGraph/[[2507.13362\|VLM-Spatial-Reasoning-RL]] on 3DSRBench, but its representation is a *flat* property list (locations/orientations), not a relational graph with contact/pairwise edges |
| [[2607.10879\|BRO Scene Graph Prediction]] | top-down Building-Room-Object hierarchical scene-graph diffusion, with an inference-time masking strategy that completes unknown regions from partial observation | outperforms LLM-based and occupancy-based baselines on 3D-FRONT partial graphs; strong OOD generalization on Matterport3D | generates/completes the graph structure itself rather than reasoning over it, a construction method upstream of B1's CoT rows, and a candidate H5 construction-accuracy instrument, but never coupled to a downstream reasoning or action head |
| [[2607.00889\|DeWorldSG]] | probabilistic 3D Gaussian nodes (sample mean + covariance of back-projected inliers, not a lifted 2D box) + video-world-model predicate priors (frozen V-JEPA 2 class-pair prior fused into temporally-aggregated visual logits) | **77.4%** relative gain on 3DSSG relationship recall, **23.2%** predicate recall over prior best; ablation ladder 28.5%→40.3% (masking)→47.8% (refinement)→50.2% (video prior) | the sharpest construction-accuracy *mechanism* yet — an explicit ablation ladder showing which component buys how much recall — but scores graph-construction quality alone, still not regressed against downstream answer accuracy the way H5 needs |
| [[2608.06170\|Prior-SG]] | reconciles a noisy observed Instance Graph against an LLM-synthesized task-conditioned Prior Graph via graph cuts on a Markov Random Field, rather than reasoning over a graph already assumed correct | **68.0%** F1 / **61.8%** mIoU region segmentation on HM3D (SOTA over Clio/HOV-SG), zero-shot ontological flexibility across task prompts | a third H5 construction-accuracy instrument beside [[2607.10879\|BRO Scene Graph Prediction]] and [[2601.11644\|Trust-Spatial]] — but it scores region-segmentation quality of the graph itself, not answer accuracy downstream, so it evidences *how* to build a robust graph without yet closing the construction-accuracy-predicts-answer-accuracy regression H5 needs |
| [[2608.08036\|RoboGraph]] (RoboGraphBench) | compiles tasks into executable symbolic scene graphs and scores agents on task-state horizon (TSH) — how far an agent must maintain/explore/update task-relevant graph state | GPT-5.5 **78.4%** tabletop / **82.5%** indoor, open-source models collapse to **9.3%**/**0%**; state-management metrics (AER/RAR) correlate **0.69–0.74** with success, beating conventional proxies | a benchmark, not a reasoning method, but the first suite to make the *scene-graph-as-state* first principle itself the difficulty axis (maintenance/exploration/update spans) rather than a single-shot QA probe — the metric substrate a future construction-accuracy-vs-TSH regression could use, and a demand-side sibling to C3's RoboMemArena on the reasoning side |
| [[2607.21595\|VLM-IE3D]] | implicit geometry tokens (coarse, from AnySplat) + explicit geometry tokens (reconstructed depth attributes), fused into a VLM via an Implicit-Explicit Attention adapter, RGB-video-only input | **+22.4 pp** 3D dense captioning (C@0.5), **+9.2 pp** 3D visual grounding (Acc@0.25) over Qwen2.5-VL-3B | fuses two geometry granularities rather than building a relational graph, a token-interface sibling to [[2605.08064\|Proxy3D]]/[[2505.20279\|VLM-3R]], sharpening H1's metric-vs-topological question with an explicit-token control but not itself a scene-graph |
| [[2601.11644\|Trust-Spatial]] | learned external-geometric *confidence estimator* (gradient-boosting over geometric-alignment + spatial-ambiguity + detection-quality + VLM-internal-uncertainty) predicting *when to trust* a VLM spatial answer; scores pairwise scene-graph *edge-construction precision* and prunes by confidence | **0.674** BLIP-2 AUROC (+34% over text-confidence); vision signals **87.4%** of feature importance vs **12.7%** VLM self-confidence; confidence-pruning lifts edge precision **52.1%→78.3%**; 61.9% selective-prediction coverage at 60% target accuracy | the deployable *construction-quality predictor* H5 needs and the cited H5 rows all lack — [[2603.15386\|RieMind]] is a *GT-graph* upper bound (injects perfect annotations rather than scoring construction), [[2605.30557\|SpatialUncertain]] is a calibration probe with no grounding mechanism, [[2504.20024\|SpatialReasoner]] only *states* construction is the bottleneck without a predictor; Trust-Spatial regresses learned graph-construction quality against answer accuracy — exactly the predictive-verifier-over-edge-construction cell, the H5 instrument the Benchmark-Gaps row names |
| [[2511.16160\|Video2Layout]] | metric-grounded cognitive map via *continuous* BEV-bbox coordinates + numerical CoT | **47.46%** avg over spatial benchmarks, **+4.92%** over grid-map-trained counterpart, **61.32%** map-accuracy at 4 frames | the continuous-vs-discrete upgrade to [[2601.11442\|Map2Thought]]'s symbolic grid (settles *grid-vs-continuous* for a map), but still a *map* of positions — no pairwise relation/contact edges, and reports map accuracy only diagnostically, not as the predictor of answer accuracy (H5) |
| [[2603.15386\|RieMind]] | GT 3D scene-graph (3DSG) + deterministic LLM toolbox querying it, perception decoupled from reasoning | **89.5%** VSI-Bench static (GPT-4.1, ~+16% over best fine-tuned); agentic GPT-4o **+49.9%** avg over base; 7B Qwen reaches 64.1% without training | the clean-graph upper bound that *isolates reasoning from perceptual error* — exactly the H5 probe [[2503.11089\|EmbodiedVSR]]/[[2512.16909\|MomaGraph]] lack — but uses ground-truth annotations, so it measures reasoning-given-a-perfect-graph, not construction accuracy itself |
| [[2602.06037\|GeoThinker]] | active/gated geometry integration (Spatial-Grounded Fusion: frame constraints + importance gating + strategic layers) | **62.23** avg / **67.4** VSTI-Bench (+29.2 pt over GPT-4o), <5% added FLOPs; +1.66% RoboRefer, +3.7% driving DAC | selective fusion of geometric cues, the *active* counterpart to [[2505.12448\|SSR]]'s passive depth-rationale — but fuses a uniform geometric stream into latents, never materializing the explicit relational graph H1 tests |
| [[2602.21186\|Spa3R]] | self-supervised view-invariant predictive spatial field (latent), injected via residual cross-attention adapter | **58.6%** VSI-Bench (+1.3 pt over Cambrian-S-3B); SPAR-Bench **58.4%** vs SpaceR-7B 37.6%; **+7.5%** adapter over token-append | a *learned latent* spatial interface like [[2605.08064\|Proxy3D]]'s proxy tokens and [[2505.20279\|VLM-3R]]'s reconstruction, cheaper than explicit 3D — but the field is opaque, leaving the metric-vs-topological question (H1) unanswerable inside the latent |
| [[2603.23404\|TRACE]] | training-free structured *textual* allocentric cache (room context + camera trajectory + entity registry) | **+7.54%** VSI-Bench (Gemini 3 Pro), beats Cognitive-Map / CoT / ToT prompting; removing entity registry **−5.24%**, removing trajectory **−1.92%** | a symbolic-*textual* structure (the serialized middle ground between [[2601.11442\|Map2Thought]]'s metric map and free-form CoT) — proves structure beats prose, but the registry stores qualitative relations, not metric edges, so it can't dissociate metric content (H1) |
| [[2506.09965\|VILASR]] | visual-drawing CoT — boxes + auxiliary lines drawn *onto the image* as a spatial scratchpad (GRPO) | **+18.4%** avg over 5 benchmarks; **98.2%** MAZE, **45.4%** VSI-Bench, **30.2%** MMSI-Bench | a distinct representation locus — the scratchpad lives *on the pixels*, not in a graph/coords/map — but the drawn marks encode topological layout, never labeled metric distances/angles, so the H1 metric-edge question stays open |
| [[2512.07733\|SpatialDreamer]] | RL-grounded active mental imagery — think-imagine-answer loop over an external WM + Geometric Policy Optimization | **93.9%** SAT-Real, **84.9%** MindCube-Tiny (+55.6% over base), GeoPO **+5.5%** over GRPO | the strongest simulate-then-reason result — RL over imagined views, sibling to [[2507.12508\|MindJourney]] / [[2604.26934\|World2VLM]] — but the gain rides on the WM's geometric fidelity, not an explicit relational graph; imagery, not metric structure |
| [[2602.08236\|AVIC]] | adaptive *when/how-much-to-imagine* — policy gatekeeper + trajectory-level verification over a visual WM | SAT-Real **74.0%→79.3%** with **9×** fewer tokens and **17×** fewer WM calls than always-on; improves R2R navigation | adds the cost-control knob the simulate-then-reason rows ([[2507.12508\|MindJourney]] / [[2512.07733\|SpatialDreamer]]) lack — but it gates *whether* to imagine, not *what structure* to build, so it optimizes imagery budget, not the metric representation B1 bets on |
| [[2602.21619\|VSR-Information-Injection-Analysis]] | diagnostic — black-box VLMs with varied injected spatial context (verbalized vs numerical, single vs aggregated) | targeted single-context injection **+9.6%**, but aggregating contexts degrades; *verbalized* beats *numerical* (numerical hurts up to **−9.2%**) | a foil for the metric bet: shows *naive numerical/metric text injection* into a frozen model backfires — sharpening that B1's win must come from a *learned structured metric graph*, not free-form metric prose; like [[2604.24300\|ReVSI]] it diagnoses, prescribes no graph |

**Hypotheses & tests.** The FP bet — explicit metric scene-graph CoT + RL grounding closes the causal slice of the human gap — decomposed.
1. **H1 — Metric edges, not topological relations, carry the gain.**
   - *Prediction*: a scene-graph with metric edge labels (distances, angles) beats a purely topological graph (left-of, on-top-of) on CVBench, with the gap concentrated in metric-relation question types.
   - *Test*: ablate metric-vs-topological edges on [[2507.13362|VLM-Spatial-Reasoning-RL]]; decompose gains by relation type over [[2412.07825|3DSRBench]]'s 12-question-type / 4-category split (its FlipEval directly tests the left/right topological shortcut to dissociate from metric content, and its 6D-viewpoint OOD split doubles as the H2 OOD substrate CVBench lacks).
   - *Row*: VLM Spatial Reasoning RL.
   - *Falsifier*: topological edges match metric edges → the win is structure, not metric content.
2. **H2 — RL grounding transfers the scene-graph habit OOD better than SFT (settled prior, re-confirm on causal-OOD).**
   - *Prediction*: GRPO-grounded scene-graph CoT generalizes to [[2601.13304|CausalSpatial]]'s causal tasks where SFT-trained CoT degrades, replicating the +19.5% Depth-OOD pattern — already demonstrated in general OOD by [[2601.01984|Thinking-with-Blueprints]] (79.7% SAT-test) and [[2512.16909|MomaGraph]]; B1 only re-confirms it on the *causal* slice those papers didn't isolate.
   - *Test*: RL vs SFT on the same scene-graph backbone, evaluated on held-out causal tasks.
   - *Row*: VLM Spatial Reasoning RL / Thinking with Blueprints / CausalSpatial.
   - *Falsifier*: SFT matches RL on causal-OOD → grounding is prompt-tuning, not a transferable habit.
3. **H3 — The residual human gap is reasoning-bound on trajectory, perception-bound on occlusion.**
   - *Prediction*: decomposing the 30-point gap by task, scene-graph CoT closes most of the *trajectory* slice (reasoning) but little of the *occlusion* slice (perception/depth).
   - *Test*: per-task (collision/occlusion/trajectory) gain measurement on CausalSpatial, extended by [[2506.03135|OmniSpatial]]'s per-dimension split (Complex-Spatial-Logic 30–40% = reasoning-bound vs Perspective-Taking/perception = perception-bound) for a standardized reasoning-vs-perception decomposition beyond CausalSpatial's slice.
   - *Row*: CausalSpatial / Embodied3DBench / OmniSpatial.
   - *Falsifier*: uniform closure across tasks → the gap isn't split reasoning-vs-perception.
4. **H4 — Upstream scene-graph grounding partially substitutes for downstream geometry.**
   - *Prediction*: feeding [[2501.10074|SpatialCoT]]'s reasoned coordinates into a simpler RGB action head recovers a measurable fraction of A1's point-cloud-head SR on geometry-bound tasks — but not all of it, leaving a residual only sensed geometry closes.
   - *Test*: scene-graph-coords + RGB head vs A1 point head on matched tasks.
   - *Row*: SpatialCoT.
   - *Falsifier*: upstream grounding fully matches the point head → downstream geometry is redundant (collapses A1 into B1); or recovers ≈0 → reasoning doesn't substitute at all.
5. **H5 — Graph-construction accuracy, not answer accuracy, is the failure bottleneck.**
   - *Prediction*: when scene-graph CoT errs, most errors trace to a wrong graph (hallucinated/missing entity), not wrong reasoning over a correct graph — so reporting graph-construction accuracy separately predicts answer accuracy.
   - *Test*: instrument graph-construction vs reasoning errors on [[2503.11089|EmbodiedVSR]] / [[2604.24300|ReVSI]] stress sets, using [[2601.11644|Trust-Spatial]]'s learned edge-construction-precision estimator as the deployable construction-quality predictor (it lifts edge precision 52.1%→78.3% by confidence-pruning and regresses vision-signal confidence against answer accuracy) — the H5 instrument [[2603.15386|RieMind]] (GT-graph upper bound) and [[2605.30557|SpatialUncertain]] (calibration-only) lack. [[2608.06170|Prior-SG]]'s prior-vs-observation alignment (68.0% F1 region segmentation, SOTA over Clio/HOV-SG) is a fourth construction-quality mechanism, but scores the graph itself, not the downstream-answer-accuracy regression H5 needs.
   - *Row*: EmbodiedVSR / ReVSI / Trust-Spatial (edge-construction-quality predictor) / Prior-SG / DeWorldSG (construction quality, unregressed against answer accuracy).
   - *Falsifier*: errors are evenly split construction-vs-reasoning → graph quality isn't the dominant lever; or Trust-Spatial's construction-confidence fails to predict answer accuracy → construction quality is not the measurable bottleneck.

> [!warning] Risks
> - **Scene-graph construction can itself hallucinate** — a wrong graph poisons downstream reasoning. → Ground the graph in B2's 4D-consistency / depth ([[2601.13304|CausalSpatial]]'s COW visual-simulation evidence), and report graph-construction accuracy separately from answer accuracy (H5).
> - **Gains may be benchmark-specific** (CVBench-tuned prompts don't transfer). → H2's RL-OOD protocol tests cross-benchmark transfer explicitly; treat GRPO as the generalization mechanism, not prompt-tuning.
> - **The human gap may be perception-bound, not reasoning-bound** — scene-graph CoT can't fix bad depth. → H3 separates perception from reasoning failure; if perception-bound, route to A3's depth bridge as the upstream fix.

### B2 — 4D-Consistent Policies: Spatio-Temporal Geometry as a Reasoning Constraint

| | |
|---|---|
| **Cluster** | B — 3D-Grounded Cognition |
| **Thesis** | For an action to be planned over a horizon, an object's geometry and identity must stay coherent across time and viewpoint — 4D consistency. The field forces a choice: stay 2D (cheap, inconsistent) or generate explicit future frames (expensive). The bet is in First-principles below. |
| **Anchor papers** | [[2506.20134\|3D-World-Models-Survey]] (survey), [[2504.05786\|3D-Spatial-Reasoning-in-LLM-Survey]] (survey), [[2510.16732\|World-Models-for-Embodied-AI-Survey]] (survey), [[2603.22078\|WAM-vs-VLA-Robustness]] (benchmark), [[2605.13632\|GTA-VLA]] (benchmark), [[2605.05126\|ConsisVLA-4D]] (method), [[2508.07917\|MolmoAct]] (method) |
| **Key targets** | LIBERO ≥98.1% ([[2605.05126\|ConsisVLA-4D]]); factor-decomposed OOD on [[2510.13626\|LIBERO-Plus]] (7 axes, isolated camera-viewpoint where SR drops 95%→<30%); real bimanual ≥70.0% (vs OpenVLA 28.5%) on [[2506.18088\|RoboTwin-2.0]] (5-dim DR, +24.4% real few-shot); SimplerEnv-Plus unseen-object ≥58.3% (vs 36.2% baseline) with trace-guidance recovering ~20% of failures ([[2605.13632\|GTA-VLA]]) — the named OOD-SimplerEnv target for H3's traces-help-OOD arm; 2.31× inference speedup / 1.36× training-cost cut; OOD SR ≥72.1% ([[2508.07917\|MolmoAct]]); [[2606.17046\|GAM]] LIBERO-Plus camera-perturbation 83.1% (only 12.1 pp total drop, vs 16.1–60.9% for baselines) — the sharpest viewpoint-OOD retention target for H1's isolated camera-viewpoint slice |

**Why it matters.**
- **The gap**: a policy that plans over a horizon must keep an object's geometry and identity coherent across time and viewpoint — else the action it commits at step 1 is invalidated by a hallucinated scene at step 5; the field's two answers (projection-biased 2D, or expensive explicit future-frame generation) both fall short.
- **Today's answers**: each flank of the bet is already fenced. Viewpoint-OOD: [[2509.14117|GeoAware]] states B2's hypothesis verbatim — "generalization across views is tied to the geometric acuity of the visual encoder" — with an implicit/no-reconstruction route (**+35 pp** zero-shot unseen-viewpoint SR on LIBERO, 96.8% ID), but *spatial-only*, no temporal 4D. Temporal-consistency-as-in-policy-constraint: [[2605.21862|EvoScene-VLA]] (recurrent scene-prior + depth/3D anchors, 89.1% RoboTwin) and [[2602.20200|OptimusVLA]] (98.6% LIBERO, 2.9× speedup) already enforce it. [[2605.05126|ConsisVLA-4D]] enforces 4D coherence with *implicit* consistency attention (98.1% LIBERO, 70.0% real bimanual vs OpenVLA 28.5%, 2.31× faster); [[2508.07917|MolmoAct]] makes it *explicit and steerable* (72.1% OOD). None runs the consistency-vs-perception ablation (perception held fixed) over *full* space+time 4D.
- **The opening**: [[2603.25399|LaMP]] shows a dense 3D-scene-flow latent motion prior lifts LIBERO-Plus +9.7 pp over OpenVLA-OFT (79.3%) — a temporal-geometry constraint, not explicit frames, driving exactly the OOD robustness this direction predicts.

**First-principles framing.**
- **First principle**: for an action to be planned over a horizon, the imagined geometry must be *temporally and cross-view consistent* — the same object on a coherent 4D trajectory. Consistency is a constraint the representation must satisfy; it is not the same as rendering every intermediate frame. [[2604.08532|SelfEvo]] shows consistency can even be a *self-supervision* target (−19.7% depth AbsRel, +20.1% AUC@30 on new domains, no labels).
- **Assumption being challenged**: the either/or that you accept projection-biased 2D (OpenVLA-class) or pay for explicit future-frame generation ([[2604.26694|X-WAM]]-class) to get 4D consistency. [[2605.05126|ConsisVLA-4D]] gets explicit-3D accuracy *and* a 2.31× speedup with *implicit* consistency attention — so the tradeoff is breakable. (That implicit consistency *helps OOD* is now consensus — [[2509.14117|GeoAware]] for viewpoint, [[2605.21862|EvoScene-VLA]] / [[2602.20200|OptimusVLA]] for time — so the contest is *which* of consistency-vs-perception drives it, and whether it holds over *full* space+time 4D, not viewpoint alone.)
- **The bet**: it is the consistency *constraint*, not raw 3D or viewpoint-geometry alone, that drives the OOD gain — and it must be shown over *full* space+time 4D, not the viewpoint-only slice [[2509.14117|GeoAware]] fenced. Specifically, ablating [[2605.05126|ConsisVLA-4D]]'s consistency attention with *perception held fixed* collapses OOD SR (toward MolmoAct's 72.1% baseline) more than ablating any single perceptual feature, and more than it dents ID SR — the consistency-vs-perception isolation no paper has run ([[2511.17199|VLA-4D]] varied coordinate-chaos, [[2509.14117|GeoAware]] varied VGGT-layer selection; neither isolated consistency-as-driver).

**Related research papers.** One table on the axis the direction turns on — *the 4D-consistency mechanism* (implicit attention / explicit frame generation / latent-JEPA / scene-flow prior / multi-view diffusion / kinematic tuning / self-distillation) — with `Key result` and what each leaves missing. The contrast that defines the direction is implicit (cheap, in-model) vs explicit (generated, external — the C2 boundary).

| System | Consistency mechanism | Key result | What's missing |
|---|---|---|---|
| [[2605.05126\|ConsisVLA-4D]] | implicit spatio-temporal consistency attention (no frame gen) | **98.1%** LIBERO, **70.0%** real bimanual vs OpenVLA 28.5%, **2.31×** faster | single-source cost claim; consistency-vs-3D not ablated on OOD |
| [[2508.07917\|MolmoAct]] | explicit depth-aware tokens + steerable visual reasoning traces | **86.6%** LIBERO, **72.1%** OOD (+7.8% over RT-2-X), 75% steering | explicit traces aid OOD, but heavier than implicit attention |
| [[2605.13632\|GTA-VLA]] | steerable human spatial guidance (points/boxes/traces) + the SimplerEnv-Plus OOD suite (benchmark) | **98.6%** LIBERO, **81.2%** SimplerEnv; SimplerEnv-Plus unseen-object **58.3%** vs **36.2%** baseline; trace-guidance recovers ~**20%** of failures (**81.2%→86.1%** SimplerEnv-Bridge) | MolmoAct already holds the steerable-traces method slot — so GTA-VLA's unfilled cell is the *named OOD-SimplerEnv suite* H3's traces-help-OOD arm needs, not a new consistency mechanism |
| [[2506.22242\|4D-VLA]] | 3D coord embeddings + multi-frame history | **+12.1%** LIBERO over OpenVLA, **+25.4%** LIBERO-LONG, real **85.63%** vs 27.70% | spatiotemporal pretraining, but consistency not enforced as a constraint |
| [[2509.14117\|GeoAware]] | geometric-acuity encoder for viewpoint generalization (no reconstruction) | **+35 pp** zero-shot unseen-viewpoint SR on LIBERO, **96.8%** ID, real-robot | states B2's hypothesis verbatim but *spatial-only* (viewpoint), no temporal 4D; ablation is VGGT-layer selection, not consistency-vs-perception |
| [[2608.01066\|OC-VLA++]] | geometry-guided synthetic paired views (offline monocular reconstruction, MoGe-2) + explicit cross-view *action*-equivariance loss on top of camera-space action grounding | real max-shift **48.3%** vs OC-VLA **40.8%** vs robot-frame **31.7%**; multi-view-trained sim **+4.4 pp** (52.4%→56.8%) | sharpens [[2509.14117\|GeoAware]]'s viewpoint flank with an explicit *action-level* equivariance objective rather than an encoder-acuity fix, but single-camera demonstrations only, and the equivariance target is viewpoint, not *temporal* 4D, so it doesn't touch H1's consistency-vs-perception isolation |
| [[2605.21862\|EvoScene-VLA]] | recurrent scene-prior + local depth + global 3D-foundation anchor | **89.1%** RoboTwin Clean / 88.5% Rand, **42.0%** real (+4.7%) | enforces temporal-consistency-as-in-policy-constraint, but never isolates consistency vs the depth/3D perceptual anchors on OOD |
| [[2602.20200\|OptimusVLA]] | temporal-consistency constraint + fast inference | **98.6%** LIBERO, **38%** RoboTwin-2.0-Hard, **2.9×** real speedup | the temporal half of the constraint, but no consistency-vs-perception ablation and no SR-vs-latency frontier vs explicit methods |
| [[2511.17199\|VLA-4D]] | full space+time 4D embeddings (coordinate-chaos ablation) | **97.4%** LIBERO, smooth global + stable local trajectories, zero-shot | runs the *closest* full-4D ablation (3D-vs-no-3D coordinate chaos), but tests 3D presence, not the consistency-vs-perception isolation B2's H1 needs |
| [[2603.25399\|LaMP]] | dense 3D-scene-flow latent motion prior | **98.3%** LIBERO, **79.3%** LIBERO-Plus (+9.7 pp over OpenVLA-OFT) | a temporal-geometry prior — strong OOD, but not a full 4D head |
| [[2604.26848\|STARRY]] | action-centric WM jointly denoising ST latents + actions | RoboTwin 2.0 **93.82%**, real bimanual **70.8%** (+31.7 pp over π0.5) | explicit ST-coherence — the heavier sibling of implicit attention |
| [[2604.03181\|MV-VDP]] | multi-view video diffusion policy w/ 3D + temporal priors | Meta-World **89.1%** at 5 demos, ~89% at one denoising step (5 Hz) | explicit multi-view generation — the cost B2 avoids |
| [[2602.09878\|MVISTA-4D]] | view-consistent 4D WM (spherical embed + deformable cross-view attn) | **72.6%** RLBench, lower depth/Chamfer | explicit 4D-consistency, externally read (C2 boundary), not in-policy |
| [[2507.01099\|Geometry-aware-4D-Robot-Video]] | explicit RGB+pointmap 4D generation, cross-view consistent | the externally-read pose pipeline (C2's anchor) | generates geometry for a tracker — the explicit counterpart B2 internalizes |
| [[2504.20995\|TesserAct]] | RGB-D-N temporal-depth channel | depth+normal sharpen action prediction | a channel, not an enforced consistency constraint |
| [[2503.19355\|ST-VLM]] | kinematic instruction tuning for ST reasoning | STKit-Bench **59.8%** vs GPT-4V 28.5% | the reasoning side of temporal geometry, not a policy constraint |
| [[2602.10098\|VLA-JEPA]] | implicit consistency in a JEPA latent | **97.2%** LIBERO | latent consistency without explicit geometry — the latent counterpoint |
| [[2601.05248\|LaST0]] | latent **spatio-temporal chain-of-thought** — a compact latent CoT decomposed into future-visual-dynamics + 3D-structure + robot-proprioception, extended *across time* into temporally-consistent implicit reasoning trajectories (Mixture-of-Transformers dual-system: slow latent-reasoning expert + fast acting expert) | **98.1%** LIBERO, **82%** RLBench; **15.4 Hz** inference (**14×** over explicit CoT-VLA at 1.1 Hz) | the latent-ST-CoT form of implicit consistency — the *non-verbalized* counterpart to [[2508.07917\|MolmoAct]]'s explicit visual/textual traces, distinct from [[2602.10098\|VLA-JEPA]]'s pure-opaque JEPA latent (no ST decomposition) and [[2602.20200\|OptimusVLA]]'s dual-memory action-trajectory smoothness; it substantiates H3's *constraint-vs-auditability* framing (a latent, non-verbal consistency CoT pitted against verbal traces) — but like every method here it does not run the H1 consistency-vs-perception isolation (that is the bet, not an add) |
| [[2604.08532\|SelfEvo]] | consistency-as-self-supervision (self-distillation, no labels) | **−19.7%** depth AbsRel, **+20.1%** AUC@30 on new domains | proves consistency is learnable label-free — not yet a deployed policy |
| [[2604.16484\|DexWorldModel]] | O(1)-memory latent WM | constant-memory rollout | the efficiency budget the consistency mechanism must fit |
| [[2505.05800\|3D-CAVLA]] | 3D context-aware scene-level conditioning over time | scene-level 3D conditioning | conditions on 3D, doesn't enforce cross-time consistency |
| [[2603.22078\|WAM-vs-VLA-Robustness]] | implicit-VLA-vs-explicit-WAM SR×latency frontier (benchmark) | shared 21-sub-dim / 7-category perturbation taxonomy over LIBERO-Plus + RoboTwin-2.0-Plus; WAMs **≥4.8×** slower than π0.5's 63 ms/chunk; π0.5 **85.7%** LIBERO-Plus | the only suite plotting OOD-SR *and* implicit-vs-explicit cost jointly, but compares architecture *families* — doesn't hold perception fixed to isolate consistency-as-driver (H1) |
| [[2506.18088\|RoboTwin-2.0]] | standardized bimanual domain-randomized suite (benchmark) | 5-dim DR; **+24.4%** real few-shot / **+21.0%** zero-shot unseen-background; auto expert-code **71.3%** | the named substrate behind B2's real-bimanual ≥70% target (and the suite EvoScene-VLA/OptimusVLA's RoboTwin numbers come from), but a DR suite, not a consistency mechanism itself |
| [[2606.14048\|WAM4D]] | implicit 4D-geometry distillation via spatial register tokens (training-only, dropped at deploy) + causal mixture attention | **91.8%** sim avg (50 RoboTwin 2.0 tasks), **90%** real sub-action SR (AstriBot S1), **525.43** ms latency / **9.71** GiB VRAM | names B2's H1 risk verbatim (explicit 4D objectives refocus on reconstruction over the causal video→action link), but distils geometry as a training prior, never ablating consistency-vs-perception on OOD the way H1 demands |
| [[2606.17046\|GAM]] | causal future-predictor inside a GFM latent — jointly forecasts future latent geometric state + action in one pass | **97.6%** LIBERO, only **12.1 pp** LIBERO-Plus drop (vs 16.1–60.9% baselines), **83.1%** under camera perturbation, **~6.9** ms/pass (55× faster than diffusion) | the GFM-native temporal-4D variant ConsisVLA-4D's attention isn't, and beats GeoAware's *spatial-only* viewpoint route on time too — but reports robustness as an architecture win, not the consistency-vs-perception isolation H1 needs |
| [[2608.08023\|Trajectory Field WAM]] | train-time-only motion-alignment (frame-difference cosine loss) + destination-alignment (source→destination KL) distilled from a frozen 3D-trajectory-field teacher into a WAM's DiT features, no 3D at inference | LIBERO-Plus avg **62.21%→71.01%** (**+8.8 pp**), camera perturbation **+17.26 pp**, ID LIBERO **98.2%→98.6%**, RoboTwin 2.0 **91.34%** vs **90.58%** | a temporal-*trajectory* constraint distinct from GAM's latent forecaster and ConsisVLA-4D's attention — the sharpest *isolated* camera-viewpoint OOD gain from a single train-time auxiliary loss in the table, strengthening H1's consistency-drives-OOD reading, but the ablation removes each loss term (LIBERO 98.2%→98.0%) rather than ablating consistency against a held-fixed perceptual arm |
| [[2506.19816\|CronusVLA]] | temporal-consistency-in-policy via FIFO feature-chunking + DiT cross-frame decoder (+ SimplerEnv-OR temporal-OOD suite) | **97.0%** LIBERO (+40.3% long-horizon over OpenVLA), **70.9%** SimplerEnv, **96.2 R-Score** on sparse temporal disturbance, **8.7 Hz** | supplies the *temporal*-OOD substrate H1/H3 lack (LIBERO-Plus/WAM-vs-VLA perturb spatial/visual factors only), but enforces temporal consistency without explicit 4D geometry — the consistency-vs-perception arm stays unmeasured |
| [[2606.13769\|μ0]] | query-conditioned 3D trace-space consistency — future semantic-point trajectories under a semantic-rigidity constraint (flow-matching + B-splines) | best ADE/FDE across horizons (**2.9×** faster than 2D baseline), real UR3 **91.7%** (+18.4 pp over VLM+expert), RoboCasa365 **30.25%** (+5.0 pp over π0) | a trace-space temporal-geometry constraint distinct from frame-gen, occupancy and the JEPA/scene-flow rows — but the constraint lives in a motion prior, not run as the consistency-vs-perception OOD ablation B2 turns on |
| [[2602.17951\|ROCKET]] | residual multi-layer 3D-representation alignment to a 3D foundation teacher via a shared projector (perception-arm of H1) | **98.5%** LIBERO at ~**4%** of prior compute, **81.7%** LIBERO-Plus (gains under Robot/Layout shift), shared projector lifts naive multi-layer **80.0%→98.2%** | the perceptual-feature-alignment counter-arm H1 must hold fixed (layer-choice sensitivity is its variable) — strengthens perception, but is *not* a consistency mechanism, so it foils rather than tests the consistency-as-driver claim |
| [[2512.02902\|VLA-Generalizability-Study]] | decomposes VLA into Spatial-Modeling (visual encoder) vs Physical-Modeling, isolating which drives viewpoint/lighting/background OOD (FTM/FLA) | FTM **48.5%→87.1%** on novel viewpoints at **4K** params; FLA **94.8%** across visual perturbations (Libero-V) at **99×** fewer params | the closest existing perception-vs-X OOD isolation — pins brittleness to visual-representation misalignment, the perception driver H1 contrasts against — but never isolates the *consistency* arm, only the perceptual one |
| [[2602.10109\|ST4VLA]] | dual-system gradient-decay querying that prevents spatial-representation *collapse* under joint perception+action training (perception-prior preservation) | **95.9%** LIBERO, **84.6%** SimplerEnv VM, **68%** unseen-object / **78%** unseen-instruction sim, higher Projection-Space Similarity vs collapse | the spatial-prior-preservation machinery H1 must hold fixed (perception arm, distinct from ROCKET's layer alignment) — measures the perception-vs-action gradient conflict, not consistency-as-OOD-driver |
| [[2605.06192\|EA-WM]] | explicit robot-geometry-preserving video generation — Kinematic-to-Visual Action Fields project 3D robot geometry into the camera plane + event-aware bidirectional fusion | **P3CScore 76.60** on WorldArena (+5.52 over CogVideoX), interaction **0.682** / trajectory **0.430**, KVAF ablation **−5.63** | the explicit cost-side B2 contrasts against (heavier sibling of STARRY/MV-VDP) — preserves geometry in *generated frames*, externally read, so it pays the generation cost implicit attention avoids |
| [[2605.22882\|GEM-4D]] | geometry-enhanced velocity-alignment distillation of dense 4D correspondence from a *frozen* 4D-geometry FM (PAGE-4D) into a Video-DiT's intermediate features + a parallel Geometry-DiT flow-matching branch → AIDS inverse-dynamics to 6-DoF | **58%→75%** Droid, **65%→83%** CLVR, **59%→87%** RAIL over TesserAct; lowest FVD/AbsRel/Chamfer | a distinct explicit-4D mechanism (4D-FM velocity distillation) none of the cited STARRY/MV-VDP/EA-WM rows use — enforces 4D consistency in *generated* frames, externally read; its ablations are geometry-flow-branch vs depth-supervision vs VGGT-features, so it strengthens the explicit row without running the H1 consistency-vs-perception isolation |
| [[2512.06628\|MIND-V]] | long-horizon temporal coherence via a hierarchical WM with a Physical-Foresight-Coherence RL reward (GRPO) | **+9.0%** PFC Score, **+76.7%** task SR, **+172.2%** user preference on long-horizon; lifts OpenVLA-OFT **27.7%→43.5%** as guidance | substrates H4's horizon crossover (coherence-via-reward, a variant absent among the policy/WM rows) — but coherence is enforced on *generated video* (explicit), not as an in-policy constraint, and never benchmarked against implicit attention at matched horizon |
| [[2604.17887\|StableIDM]] | causal-TCN temporal-dynamics-refinement enforcing kinematic consistency under manipulator truncation (a camera-viewpoint OOD) | AgiBot heavy-truncation **30.7% Acc** (vs Vidar 18.6%), real replay **47.4%** (vs 37.7%), VLA augmentation **35.3%→52.9%** (+17.6%) | a 'temporal-consistency-recovers-viewpoint' variant on exactly H1's isolated camera-viewpoint slice (recovers lost geometry from history) — but operates in an inverse-dynamics annotator, not a deployed policy, so the consistency gain isn't read as an OOD-SR driver |
| [[2602.06556\|LIBERO-X]] | graded 5-level progressive-difficulty OOD protocol perturbing spatial layout / object properties / instruction semantics, with multi-label diagnostics (benchmark) | VLA avg only **39.4%** at Level 1, **−31.2%** Level-1→5 decline (biggest **−12.7%** at Level 3 = scene-topology reconstruction), near-zero at **3+** steps | adds the *graded-difficulty* OOD axis LIBERO-Plus (7 isolated factor axes) and WAM-vs-VLA (21 sub-dims) lack, so consistency-vs-perception deltas can be traced across rising difficulty — but a benchmark, not a consistency mechanism itself |

**Hypotheses & tests.** The FP bet — implicit 4D-consistency matches explicit-3D at far lower cost, and consistency (not raw 3D) drives OOD — decomposed.
1. **H1 — Over full space+time 4D, ablating consistency collapses OOD more than ablating perception.**
   - *Prediction*: removing [[2605.05126|ConsisVLA-4D]]'s consistency attention (perception held fixed) collapses OOD SR more than removing any single perceptual feature, and more than it dents ID SR — the consistency-vs-perception isolation that [[2509.14117|GeoAware]] (VGGT-layer ablation, viewpoint-only) and [[2511.17199|VLA-4D]] (coordinate-chaos, 3D-presence) did *not* run, and over *time* not just viewpoint.
   - *Test*: consistency-ablation vs perceptual-feature-ablation on [[2510.13626|LIBERO-Plus]]'s decomposed factor axes (the isolated camera-viewpoint slice vs background/lighting/layout) and [[2603.22078|WAM-vs-VLA-Robustness]]'s per-category SR breakdown over LIBERO-Plus + RoboTwin-2.0-Plus; report ID-vs-OOD delta for each. [[2608.08023|Trajectory Field WAM]]'s per-loss ablation (removing motion- or destination-alignment costs only 0.2 pp ID but the joint loss buys +8.8 pp OOD) is directional evidence the temporal-consistency term, not raw perception, is doing the OOD work — but it ablates *which loss*, not consistency-vs-a-held-fixed-perceptual-arm, so H1's isolation stays open.
   - *Row*: ConsisVLA-4D / GeoAware / VLA-4D / Trajectory Field WAM.
   - *Falsifier*: a perceptual-feature ablation hurts OOD as much → raw 3D / viewpoint-geometry, not consistency, drives robustness.
2. **H2 — Implicit 4D is Pareto-dominant on the SR-vs-latency plane, except on a task sub-class.**
   - *Prediction*: plotting implicit ([[2605.05126|ConsisVLA-4D]]) vs explicit ([[2507.01099|Geometry-aware-4D-Robot-Video]], [[2604.26848|STARRY]]) on SR-vs-latency, implicit dominates except where externally-readable geometry is required (debuggability, tracker handoff).
   - *Test*: build the frontier across implicit and explicit methods on matched tasks — [[2603.22078|WAM-vs-VLA-Robustness]] already plots exactly this SR×latency frontier (implicit-VLA vs explicit-generation-WAM, WAMs ≥4.8× slower than π0.5's 63 ms/chunk) on a shared perturbation suite, the substrate this test otherwise lacks.
   - *Row*: ConsisVLA-4D / Geometry-aware 4D Robot Video.
   - *Falsifier*: explicit dominates broadly → the cost advantage is illusory.
3. **H3 — Implicit consistency + explicit traces compound, not redundant.**
   - *Prediction*: pairing implicit consistency (B2) with [[2508.07917|MolmoAct]]'s steerable visual traces raises OOD SR beyond either alone — the two ground different things (constraint vs auditability); the sharpest version of this contrast is [[2601.05248|LaST0]]'s *latent* (non-verbal) spatio-temporal CoT vs MolmoAct's *verbal/visual* traces — same consistency target, opposite auditability, so the pairing tests whether the non-verbal constraint and the verbal trace are redundant or complementary.
   - *Test*: implicit-only vs traces-only vs both on OOD SimplerEnv, including the latent-ST-CoT ([[2601.05248|LaST0]]) vs explicit-trace ([[2508.07917|MolmoAct]]) head-to-head at matched horizon.
   - *Row*: ConsisVLA-4D / MolmoAct / LaST0 (latent-ST-CoT, non-verbal consistency).
   - *Falsifier*: both ≈ max(either) → the signals are redundant; or the latent-ST-CoT matches explicit traces on OOD at lower latency → auditability is a free add, not a separate lever.
4. **H4 — The implicit advantage has a horizon crossover.**
   - *Prediction*: implicit consistency beats 2D above a horizon threshold and loses to explicit-generation above a longer one — there is a measurable crossover window where implicit is optimal.
   - *Test*: sweep task horizon; locate the two crossovers.
   - *Row*: ConsisVLA-4D / STARRY.
   - *Falsifier*: no crossover (implicit always or never wins) → horizon isn't the operating variable; beyond the upper crossover, route to C3.
5. **H5 — A scene-flow prior recovers most of the consistency gain at lower cost.**
   - *Prediction*: [[2603.25399|LaMP]]'s dense 3D-scene-flow prior recovers a large fraction of ConsisVLA-4D's OOD gain (toward 79.3% [[2510.13626|LIBERO-Plus]]) at lower architectural cost than full consistency attention — flow is the cheap form of the constraint.
   - *Test*: scene-flow prior vs full consistency attention on [[2510.13626|LIBERO-Plus]] at matched backbone.
   - *Row*: LaMP / ConsisVLA-4D.
   - *Falsifier*: the flow prior underperforms materially → the full attention mechanism is load-bearing, not just the temporal-geometry signal.

> [!warning] Risks
> - **Implicit consistency may not be inspectable** — you can't see what 4D structure the attention learned, hurting debuggability. → Pair with [[2508.07917|MolmoAct]]'s explicit visual reasoning traces (H3) so the temporal reasoning is steerable and auditable.
> - **Single-source cost claim** — the 2.31× speedup rests on [[2605.05126|ConsisVLA-4D]] alone. → H2's full cost/accuracy frontier across implicit and explicit methods is the go/no-go before generalizing the efficiency claim.
> - **Implicit 4D may silently fail on the longest horizons** where drift accumulates invisibly. → H4's horizon-scaling crossover bounds the regime; beyond it, route to **C3**'s explicit persistent geometric memory rather than stretching implicit consistency.

---

## Cluster C — Geometry-Native World Models & Memory

*The world model's representation is geometry, not appearance — a model-agnostic substrate any policy or model can use. The three directions are ordered along the decodability axis: occupancy a planner reads (C1, which also charts the latency-fidelity frontier of its latent-4D comparator [[2604.26694|X-WAM]]) and pointmaps a tracker reads (C2) keep geometry explicit and external; a world-frame memory (C3) makes whichever of them persist over long horizons.*

### C1 — Occupancy World Models as the Manipulation Rollout Substrate

| | |
|---|---|
| **Cluster** | C — Geometry-Native World Models |
| **Thesis** | A world model's long-horizon stability is bounded by how fast *geometric* error piles up — and an explicit occupancy grid with rigid-transform constraints bounds that error where a latent substrate doesn't. The manipulation-WM field assumes the rollout substrate should be the same RGB-D latent the policy sees, and nobody has run the occupancy-vs-latent horizon comparison at tabletop scale. The bet is in First-principles below. |
| **Anchor papers** | [[2510.16732\|World-Models-for-Embodied-AI-Survey]] (survey), [[2604.22748\|Agentic-World-Modeling-Survey]] (survey), [[2506.20134\|3D-World-Models-Survey]] (survey), [[2604.19092\|RoboWM-Bench]] (benchmark), [[2603.28887\|OccSim]] (method), [[2604.26694\|X-WAM]] (method) |
| **Key targets** | Stable rollout ≥3,000 frames / ≥4 km ([[2603.28887\|OccSim]]); +22.1% rel mIoU vs asset-based sim; 80× horizon over prior <50-frame WMs; manipulation port: minute-scale geometric coherence vs latent-world-model drift, scored on [[2604.19092\|RoboWM-Bench]]'s step+final manipulation SR (rigid/articulated/deformable/long-horizon/bimanual, more discriminative than perceptual-plausibility metrics); [[2509.05314\|ManipDreamer3D]] occupancy-planner trajectory error EE 15.38 / object 16.59 (the manipulation-occupancy planner-readout H2 asserts nobody ran); the latent-4D comparator's own latency-fidelity frontier — [[2604.26694\|X-WAM]]'s 4.5× action-latency speedup (4665→1033 ms) at 5 denoising steps → 15 Hz real-time, Chamfer 0.0049 vs 0.0680 two-stage — the baseline H6 asks whether Asynchronous Noise Sampling can push to 1–4 steps without degrading; [[2606.17046\|GAM]]'s ~6.9 ms/pass (~145 Hz), 55× faster than diffusion baselines, 12.1 pp LIBERO-Plus drop under camera perturbation — the sharper online-geometry latency-vs-robustness point H6 must clear |

**Why it matters.**
- **The gap**: [[2510.16732|World-Models-for-Embodied-AI-Survey]] traces the WM spatial axis from latent → token → explicit 3D, but the manipulation default is a latent/pixel substrate whose geometric error compounds — the rollout drifts within tens of frames over long horizons.
- **Today's answers**: occupancy-as-the-manipulation-rollout-substrate is *not* novel and *not* driving-only — [[2505.16249|3D-Occ-MPC]] runs dense voxel occupancy → learned dynamics → MPC sim+real, and [[2011.01968|DSR-Net]] built occupancy + MPC manipulation back in 2020 (planar-pushing voxel IoU **0.72** vs SE3-Net 0.31), with [[2505.05512|Occupancy-World-Model]] porting occupancy WMs to indoor scenes. [[2604.26694|X-WAM]] — this card's own latent-4D comparator — pushes the *latent-4D* corner (15 Hz, Chamfer 0.0049, per-frame-excellent); [[2510.10125|CTRL-WORLD]] makes the pixel substrate controllable. None runs the explicit-occupancy-vs-latent-4D *horizon-to-divergence* head-to-head at tabletop scale.
- **The opening**: [[2603.28887|OccSim]] proves explicit occupancy + Warp-DiT rigid-transform constraints bound per-step geometric error to sustain 3,000+ frames over 4+ km (**80×** the prior <50-frame ceiling), with data lifting downstream forecasting **+22.1%** rel mIoU — the long-horizon stability the field treats as unattainable.

**First-principles framing.**
- **First principle**: a WM's long-horizon stability is set by how fast *geometric* error grows from one rollout step to the next. An occupancy grid with rigid-transform constraints ([[2603.28887|OccSim]]'s Warp-DiT) keeps that per-step error in check; a latent substrate has no such limit, so the error compounds and the rollout drifts.
- **Assumption being challenged**: that the rollout substrate should be the same RGB-D latent the policy sees (the [[2510.10125|CTRL-WORLD]] / [[2604.26694|X-WAM]]-latent convention). The card's old "field skips occupancy because it was built only in driving" framing is *empirically false* — [[2011.01968|DSR-Net]] (2020) and [[2505.05512|Occupancy-World-Model]] already did occupancy in manipulation/indoor robots, and [[2505.16249|3D-Occ-MPC]] instantiates the occupancy-rollout principle. The real open assumption: that *because* occupancy and latent both "work," the choice doesn't bound horizon — when [[2603.28887|OccSim]]'s 80× horizon gain says the substrate *is* the drift source.
- **The bet**: a voxel-semantic occupancy WM beats a latent-4D baseline ([[2604.26694|X-WAM]]) by ≥1 order of magnitude on *horizon-to-divergence* at tabletop scale — the head-to-head [[2505.16249|3D-Occ-MPC]] and [[2011.01968|DSR-Net]] never ran — while *losing* on per-frame Chamfer (latent-4D's 0.0049), making the two complementary not competitive, and its Warp-DiT rigid-transform error bound survives to sub-cm. Because X-WAM is the card's toughest latent comparator, this card also charts how far that comparator itself can be pushed — whether Asynchronous Noise Sampling step-distills to 1–4 action steps without degrading read-out geometry (H6), and whether its RGB-D appearance channel earns its keep over a geometry-only point-flow (H7) — sharpening the baseline H1 must clear, not softening the occupancy-beats-latent claim.

**Related research papers.** One table on the axis the direction turns on — *the rollout substrate's decodability* (explicit-occupancy / latent-4D / pixel-video / depth-augmented / O(1)-latent) — with `Key result` and what each leaves missing for a long-horizon planner.

| System | Rollout substrate | Key result | What's missing |
|---|---|---|---|
| [[2603.28887\|OccSim]] | explicit voxel-semantic occupancy + Warp-DiT | **3,000+** stable frames, **4+ km**, **+22.1%** rel mIoU, **80×** horizon, **67%** zero-shot | a driving result — never ported to sub-cm manipulation occupancy |
| [[2505.16249\|3D-Occ-MPC]] | explicit occupancy → learned dynamics → MPC (the rollout substrate) | occupancy EMD **28.6e-3** / DCD **34.5e-4** beats patch/crop, elasto-plastic sim+real | instantiates C1's principle (occupancy-as-rollout-substrate) but object-centric, no full-scene occupancy-vs-latent horizon comparison |
| [[2011.01968\|DSR-Net]] | 3D scene-flow voxel state + MPC (occupancy in manipulation, 2020) | planar-push voxel IoU **0.72** vs SE3-Net 0.31, surface MSE **5.54 cm**, IoU **0.772** under occlusion | falsifies "occupancy is driving-only," but no rigid-transform horizon bound and no latent comparator |
| [[2505.05512\|Occupancy-World-Model]] | autoregressive indoor-scene occupancy forecasting | **+22.34** IoU / **+12.21** mIoU next-state, **+19.48** autoregressive IoU | proves occupancy WMs port driving→indoor (C1's scale move), but a scene forecaster, not a planner-readable manipulation rollout |
| [[2506.23126\|ParticleFormer]] | particle/point forward dynamics + MPC | superior MSE/CD, robust to hyperparams, lower MPC final-state error | the point-set rollout alternative — externally inspectable but not a dense occupancy grid a planner collision-checks |
| [[2604.26694\|X-WAM]] | latent-4D, decoded internally — this card's own comparator, whose deployable-latency and appearance-channel frontier are H6/H7 below | Chamfer **0.0049** vs 0.0680, **15 Hz**, 79.2% RoboCasa | internal latent the planner can't read; fails on *horizon*, excels per-frame |
| [[2601.03782\|PointWorld]] | explicit-3D point-flow, online real-time, model-agnostic transfer | **0.12 s** 10-step at **1B** params, zero-shot Franka push/fold/microwave, transfers across embodiments | a leaner explicit substrate than dense voxel occupancy — real-time and cross-embodiment, but point-flow not a collision-checkable grid, and never run head-to-head against OccSim's occupancy on horizon-to-divergence |
| [[2606.10040\|Efficient-WAM]] | asymmetric video-action denoising (many steps to action, far fewer to video), 1B Mixture-of-Transformers | **98 ms**/action-chunk (**32×** over Motus 3215 ms), real Astribot-S1 **66.25%** SR | the generalization of X-WAM's Asynchronous Noise Sampling H6 calls for, pushing async denoising to its extreme on a *pixel/video* substrate — H6's step-distillation latency anchor and falsifier, not itself a native-4D test |
| [[2603.03485\|Phys4D]] | three-tier 4D-geometric-fidelity evaluator (benchmark) | per-frame AbsRel **0.2711** → world-level 4D-CD **0.4626** + Worldline-L2 over horizon | the geometry-over-horizon instrument H6's step-distillation sweep reads its fidelity off — but doesn't itself run native-vs-recovered at matched latency |
| [[2606.17046\|GAM]] | native-online latent geometry on a repurposed Geometric Foundation Model backbone | **~6.9 ms/pass (~145 Hz)**, **55×** faster than diffusion baselines, only **12.1 pp** LIBERO-Plus drop | a faster architectural instance of X-WAM's online-4D-at-real-time pillar — sharpens H6's latency ceiling, but no async-denoising sweep or native-vs-recovered Chamfer head-to-head |
| [[2605.08279\|LaWM]] | latent WM with a **latent variational integrator** — a learned discrete Lagrangian (Principle of Least Action) *defines* the latent transition rule itself (structure is the transition operator, not a score/aux-loss) | acceleration PIS **0.6568→0.8964**; LPIPS **0.1259→0.1138**, AbsRel **0.3600→0.3284** on embodied robot interaction; beats video-gen + WM baselines on long-horizon geometric metrics | the one *latent* cell C1's table lacked: a latent substrate that **structurally bounds long-horizon geometric + energy drift via the transition rule** (not occupancy's explicit grid) — distinct from [[2604.26694\|X-WAM]] (per-frame-excellent, horizon-weak), [[2604.16484\|DexWorldModel]] (O(1)-memory budget), [[2603.18336\|ManiDreams]] (uncertainty-bound, not horizon-bound), [[2504.20995\|TesserAct]] (depth channel), [[2606.09813\|iMaC]] (contact channel); it is the *toughest latent comparator* for H1's falsifier — if a physics-structured latent already bounds geometric drift, the occupancy substrate must clear a higher bar — but its baselines are video-gen/WM, never an occupancy-vs-latent substrate comparison, so it sharpens H1 without preempting the bet |
| [[2606.16533\|Kairos]] | "A Native World Model Stack for Physical AI" — memory-structured latent with persistent global memory that geometrically damps error accumulation via hybrid linear-temporal attention | **90.8** LIBERO-Plus after fine-tuning, strong WorldModelBench-robot | the toughest latent-memory comparator for H1's falsifier alongside [[2605.08279\|LaWM]] — but its reported wins are on appearance/robustness suites, not a contact-rich/geometry-bound horizon suite, and it carries *no* necessity ablation, so it shows only *sufficiency*, not necessity |
| [[2510.10125\|CTRL-WORLD]] | controllable pixel video | **38.7→83.4%** on unseen objects | pixel substrate re-parses geometry; drifts long-horizon, not planner-readable |
| [[2504.20995\|TesserAct]] | RGB-D-N (depth-augmented) | depth+normal channels sharpen prediction | midpoint between latent and occupancy — geometry is a channel, not the state |
| [[2604.16484\|DexWorldModel]] | O(1)-memory latent | constant-memory rollout over 2,000 steps | latent, not explicit — the efficiency budget occupancy must respect |
| [[2604.22152\|dWorldEval]] | pixel-video horizon-drift evaluator (benchmark) | round-trip LPIPS **0.243** at 20-step horizon, policy-ranking Pearson **~0.9–0.92** | the manipulation drift-over-horizon metric vehicle for C1's central horizon claim, but pixel substrate — cannot itself score *occupancy-geometric* divergence |
| [[2406.02523\|RoboCasa]] | the proposed target suite | geometry-bound long-horizon manipulation (X-WAM 79.2% on 24 tasks) | no occupancy-substrate baseline on it yet — the gap C1 fills |
| [[2510.16732\|World-Models-for-Embodied-AI-Survey]] | latent → token → explicit-3D taxonomy | locates occupancy at the explicit end | a map, not a manipulation WM |
| [[2604.22748\|Agentic-World-Modeling-Survey]] | L2-Simulator domain-law rollout requirement | names the stability occupancy supplies | requirement, not mechanism |
| [[2506.20134\|3D-World-Models-Survey]] | 2D→3D-cognition transition | motivates explicit-geometry WMs | survey framing only |
| [[2509.05314\|ManipDreamer3D]] | reconstructed 64³ occupancy grid → off-the-shelf A*+CHOMP planner (occupancy-as-rollout-substrate, **in manipulation**) | FVD **93.98** (DiT), end-effector trajectory error **15.38** / object **16.59**, collision-free after CHOMP optimization | the closest existing instance of H2's claim — a planner *does* consume a manipulation occupancy grid without a learned decoder — but it occupancy-plans for *video synthesis* off a single image, never closing an occupancy rollout loop nor running the occupancy-vs-latent horizon comparison [[2505.16249\|3D-Occ-MPC]] and [[2011.01968\|DSR-Net]] also skip |
| [[2506.03079\|ORV]] | 4D semantic occupancy as geometric prior → conditions a pixel-video foundation model (occupancy-*guides*-pixel hybrid) | **+3.5%** visual-planning SR on Robodesk, **18.8%** lower FVD, sim-to-real via simulator-derived occupancy, RoboVLM **+13.7%** / SpatialVLA **+6.5%** | bridges [[2510.10125\|CTRL-WORLD]]'s pure-pixel and OccSim's pure-occupancy corners, but occupancy steers the pixel decoder rather than *being* the planner-readable state — so it inherits the pixel substrate's long-horizon drift the explicit-occupancy rows bound |
| [[2605.22882\|GEM-4D]] | dense 4D-correspondence distilled from a frozen geometry FM (PAGE-4D) into a Video-DiT's intermediate features at train time → Adaptive Inverse Dynamic System reads 6-DoF from the geometry-sharpened pixel rollout | **58%→75%** Droid AUTOLab, **65%→83%** CLVR, **59%→87%** RAIL over TesserAct; lowest FVD/AbsRel/Chamfer | the sharpest *manipulation* instance of the geometry-steers-pixel-decoder corner (alongside [[2504.20995\|TesserAct]]/[[2606.09813\|iMaC]]) — the toughest geometry-on-drifting-pixel foil the explicit-occupancy substrate must beat on *horizon*: GEM-4D wins per-frame geometry yet geometry is a train-time feature distillation, not a planner-readable grid, so the pixel rollout still drifts (sharpens H1 without preempting the occupancy-vs-latent horizon bet) |
| [[2510.16729\|IR-WM]] | explicit residual-occupancy forecast → BEV planning (dynamic-only residual + Feature Alignment Module) | planning L2 **0.53m** (from 0.85m), collision **0.17%** (from 0.29%), occupancy IoU_c **40.8** on nuScenes | a *distinct* per-step error-bounding mechanism from [[2603.28887\|OccSim]]'s Warp-DiT — residual-only prediction caps autoregressive geometric drift — but, like OccSim, a driving result; never ported to sub-cm manipulation, the H4 transfer gap |
| [[2603.18336\|ManiDreams]] | distributional-state forward dynamics (DRIS instance set) + declarative caging constraints (sample-predict-constrain) | outperforms PPO under perceptual noise / delay / physics randomization, zero-shot sim-to-real Franka pushing+picking, saturates beyond m=16/N=16 | the *uncertainty-propagation-bound* corner the geometric rows lack — sibling to [[2506.23126\|ParticleFormer]]'s point-set, externally-inspectable but a distributional state, not a dense occupancy grid a planner collision-checks; bounds uncertainty rather than horizon-to-divergence |
| [[1909.04915\|Hybrid-GP-Contact-Model]] | hybrid-automaton low-D forward dynamics + reset maps, UT/MC uncertainty propagation | superior NLL/RMSE on 1D-slide and **7-DOF YuMi** contact tasks, robust at **15 trials**, predicts velocity-jump discontinuities baselines miss | the classical low-dimensional-state substrate point: explicitly bounds contact-discontinuity prediction error (C1's central horizon concern) but on hand-engineered modes, no dense geometry and no rigid-transform bound to scale to full-scene occupancy |
| [[2606.09813\|iMaC]] | pixel WM + pointcloud-derived contact-distance-field & motion-image channels (WM-as-evaluator) | WM-success vs real-SR correlation **0.833–0.956** on 6/8 tasks, best task-averaged FID/PSNR/SSIM/FVD | the contact-geometry-channel variant of [[2504.20995\|TesserAct]]'s depth-as-channel row — contact-distance-fields sharpen the pixel substrate and score policies, but geometry is still a *channel* on a drifting pixel rollout, not the planner-readable state, and it ranks policies rather than bounding horizon |

**Hypotheses & tests.** The FP bet — explicit occupancy beats latent on horizon-to-divergence, complementary on per-frame fidelity — decomposed. H6 and H7 chart how far the latent-4D comparator ([[2604.26694|X-WAM]]) itself can be pushed — the deployable-latency frontier and whether its RGB-D appearance channel earns its keep — sharpening the baseline H1 must beat, not softening the occupancy-beats-latent bet.
1. **H1 — Occupancy beats latent on horizon-to-divergence at tabletop scale (the head-to-head nobody ran).**
   - *Prediction*: on matched manipulation tasks, frames-to-geometric-divergence for an occupancy WM exceeds [[2604.26694|X-WAM]]-class latent by ≥one order of magnitude — the explicit-vs-latent horizon comparison [[2505.16249|3D-Occ-MPC]] and [[2011.01968|DSR-Net]] never ran — partially surviving the driving→tabletop scale change.
   - *Test*: matched-task occupancy vs X-WAM latent on [[2604.19092|RoboWM-Bench]]'s long-horizon manipulation tasks (step+final SR, the shared substrate the occupancy-vs-latent head-to-head otherwise lacks), measuring frames-to-divergence via [[2604.22152|dWorldEval]]'s 20-step round-trip-consistency LPIPS (the manipulation analog of OccSim's driving stable-frame-count); critically, run the comparison against [[2605.08279|LaWM]]'s *physics-structured* latent (Least-Action transition rule that already bounds long-horizon geometric drift) **and against a [[2606.16533|Kairos]]-class structured-latent stack with persistent memory** as the toughest latent baselines — if occupancy still beats a drift-bounded *and* a memory-structured latent on horizon, the substrate claim survives; if either closes the horizon gap, the driving gain was the *structure*, not the explicit grid. (Whether Kairos shows geometry is *necessary* — falsifying the substrate claim — or merely that a structured latent is *sufficient* and competitive is now **resolved as sufficiency, not necessity** (Kairos = "A Native World Model Stack for Physical AI" — persistent global memory that geometrically damps error accumulation via hybrid linear-temporal attention, but with *no* necessity ablation; its reported wins (LIBERO-Plus 90.8 after fine-tuning, WorldModelBench-robot) are on appearance/robustness suites, *not* a contact-rich/geometry-bound horizon suite), so it is **not** a necessity falsifier — the necessity test must still be run on this contact-bound head-to-head.)
   - *Row*: OccSim / X-WAM / LaWM (physics-structured latent) / Kairos (memory-structured latent — *sufficiency* witness, **not** a necessity falsifier) / 3D-Occ-MPC.
   - *Falsifier*: occupancy ≤ latent horizon → the driving gain is scale-specific; in particular, if LaWM's Least-Action latent or a [[2606.16533|Kairos]]-class structured-latent matches occupancy's horizon-to-divergence *on the contact-bound suite*, the win is a structure-preserving transition rule (achievable in either substrate), not occupancy per se — and "geometry is THE substrate" collapses to "geometry is *one* sufficient substrate."
2. **H2 — Externally-readable occupancy beats a decoded latent for collision-checking.**
   - *Prediction*: an off-the-shelf planner consumes the occupancy grid for collision-checking without a learned decoder, and this beats decoding X-WAM's latent into a usable state on planning latency and accuracy.
   - *Test*: planner-on-occupancy vs planner-on-decoded-latent on collision-check tasks, hosted on [[2510.18135|World-in-World]]'s closed-loop WM-in-control-loop protocol (counterfactual rollout → unified planning → downstream SR, with Robotic Manipulation in its suite) — the planner-on-WM-state substrate H2 otherwise lacks.
   - *Row*: OccSim / X-WAM.
   - *Falsifier*: decoding the latent matches direct occupancy → externality buys nothing.
3. **H3 — A hybrid substrate beats either alone on (horizon × fidelity).**
   - *Prediction*: running occupancy for long-horizon planner-readable rollout and switching to [[2604.26694|X-WAM]]'s latent for per-step high-fidelity imagine beats either substrate alone on the joint metric.
   - *Test*: hybrid vs occupancy-only vs latent-only on (horizon × Chamfer).
   - *Row*: OccSim / X-WAM.
   - *Falsifier*: one substrate dominates both axes → the complementarity claim fails.
4. **H4 — The Warp-DiT error bound survives at sub-cm.**
   - *Prediction*: [[2603.28887|OccSim]]'s rigid-transform error-bounding holds at manipulation resolution, degrading gracefully rather than collapsing below meter-scale.
   - *Test*: sweep occupancy resolution; report the resolution at which the bound loosens.
   - *Row*: OccSim.
   - *Falsifier*: the bound collapses below meter-scale → occupancy doesn't transfer to manipulation precision.
5. **H5 — Occupancy-grid drift, not Chamfer, predicts downstream planning failure — and the drift axis is where occupancy's *necessity* (vs mere sufficiency) is decided.**
   - *Prediction*: downstream long-horizon planning SR correlates with horizon-to-divergence (mIoU-over-horizon), not per-frame Chamfer — confirming the right failure axis is horizon length. The same drift-vs-SR regression is the instrument that settles whether explicit occupancy is *necessary*: a structured latent that already bounds drift ([[2605.08279|LaWM]], a [[2606.16533|Kairos]]-class memory stack) would show *low* drift and *high* SR without an explicit grid, demoting "geometry is THE substrate" to "geometry is *one* sufficient substrate." Kairos itself shows only *sufficiency* (persistent memory that geometrically damps error accumulation, no necessity ablation), so whether it achieves low drift on the *contact-bound* manipulation suite (not its reported appearance/robustness suites) remains **un-run** — it is a *candidate* necessity falsifier only once measured on that suite.
   - *Test*: regress planning SR on both metrics across rollouts — [[2510.18135|World-in-World]] supplies the downstream closed-loop SR (e.g. Image-Goal Nav 38.19→45.14%, manipulation) to regress against, and [[2604.22152|dWorldEval]] the complementary policy-ranking correlation (Pearson ~0.9 of WM-proxy vs real SR) tying drift to outcome — running the structured-latent baselines ([[2605.08279|LaWM]] / [[2606.16533|Kairos]]) in the same regression so the necessity question is read off the drift axis, not asserted.
   - *Row*: OccSim / X-WAM / Kairos (sufficiency only — verified; necessity still un-run).
   - *Falsifier*: per-frame Chamfer predicts SR as well → occupancy and latent-4D measure the same thing; *or* a structured latent matches occupancy on both drift and SR → occupancy is sufficient, not necessary.
6. **H6 — The async action schedule shrinks to 1–4 steps without degrading the read-out geometry.**
   - *Prediction*: generalizing Asynchronous Noise Sampling, [[2604.26694|X-WAM]]'s action denoising schedule shrinks to 1–4 steps (step-distillation) without degrading the 4D geometry a consumer reads.
   - *Test*: sweep action denoising steps; report geometry fidelity vs steps on [[2603.03485|Phys4D]]'s short-term-consistency + world-level-4D-Chamfer + Worldline-L2 tiers (the over-horizon geometric-degradation axis the step-distillation sweep needs, not just per-frame Chamfer); cross-check against [[2606.10040|Efficient-WAM]]'s pixel/video-substrate asymmetric denoising, which already pushes async denoising to its extreme (98 ms/action-chunk) — if a pixel substrate matches native-4D's geometry fidelity at that speed, the speedup doesn't specifically need the native-4D substrate.
   - *Row*: X-WAM / Phys4D / Efficient-WAM.
   - *Falsifier*: fewer steps degrade geometry → the speedup trades away the substrate's value; or a pixel/video substrate ([[2606.10040|Efficient-WAM]]) matches native-4D's geometry fidelity at equal or faster latency → the async-schedule gain is a denoising-architecture trick, not specific to keeping 4D geometry native.
7. **H7 — Whether the *RGB-D appearance channel pays its way* is open, not pre-decided — a native-4D-RGB-D-video substrate transfers WM→policy *at least* as well as [[2601.03782|PointWorld]]'s point-flow only if the appearance channel is not pure noise.**
   - *Prediction*: this is the one place the doc's "appearance is noise" thesis is *put on trial rather than assumed* — [[2601.03782|PointWorld]] already shows model-agnostic cross-embodiment transfer for a *geometry-only* point-flow substrate, so the open question is whether keeping the **appearance channel intact** (X-WAM's RGB-D) adds anything a point-flow substrate lacks. The thesis default says it should be inert (geometry carries the action signal); the counterpoints in the cluster ([[2509.01819|ManiFlow]], [[2502.12320|Fusing-Point-Cloud-Visual]] +2% from local texture) say it can carry separable task-semantic signal on appearance-informative tasks. H7 does *not* assert RGB-D beats point-flow; it asserts the RGB-D substrate transfers *no worse* than point-flow, and measures whether the appearance channel is a net asset, inert, or a liability — resolving the doc-internal tension between "appearance is noise" (thesis) and "keep the appearance channel" (this hypothesis).
   - *Test*: 4D-plan vs latent-plan on spatial tasks via [[2602.08971|WorldArena]]'s action-planner + policy-evaluator tracks; then swap the RGB-D substrate into an action head and score closed-loop executability on [[2604.19092|RoboWM-Bench]]'s step+final SR, *with an appearance-channel-ablated (geometry-only) variant* run alongside so the appearance channel's marginal contribution is read directly against PointWorld's point-flow transfer. [[2608.04996|DreamWAM]] sharpens this from a different angle: decomposing the future-state target into RGB/motion/geometry/semantics and ablating each shows *motion*, not raw RGB pixel appearance, is the most consequential non-geometry channel: evidence that H7's binary "RGB-D-appearance vs geometry-only" framing collapses too much, since the informative part of "appearance" may be the *motion field*, not the pixels, predicting a motion-plus-geometry substrate (cheaper than full RGB-D) could match X-WAM's transfer as well as it matches PointWorld's geometry-only.
   - *Row*: X-WAM / PointWorld.
   - *Falsifier*: the geometry-only (appearance-ablated) substrate matches the full RGB-D one → the appearance channel is the noise the thesis says it is, and the RGB-D form adds nothing point-flow didn't; conversely, if appearance-ablation *hurts*, the "appearance is noise" thesis is too strong for the WM-substrate layer and must be qualified to task-type.

> [!warning] Risks
> - **Driving→manipulation scale gap** — occupancy validated at meter-scale, manipulation needs sub-cm. → H4's sub-cm Warp-DiT test is the go/no-go; report the resolution at which the error bound breaks rather than assuming transfer.
> - **Two bets living in one card can blur.** H1–H5 argue occupancy beats latent, H6–H7 optimize the latent comparator itself; a reader could mistake the latter for undermining the former. → Keep them explicitly separated: H6/H7 sharpen X-WAM as the toughest possible latent baseline for H1's comparison, they do not soften C1's core occupancy-beats-latent bet.
> - **Occupancy supervision scarcity in manipulation data.** → Derive occupancy in sim (free) and from depth + gripper geometry on real data; bound real-world claims to where occupancy GT is recoverable.

### C2 — 4D-Geometric-Consistent Video Prediction for 6-DoF Pose Extraction

| | |
|---|---|
| **Cluster** | C — Geometry-Native World Models |
| **Thesis** | A 6-DoF pose is a geometric quantity — cross-view-consistent pointmaps expose it directly to any tracker, while RGB-only frames leave it ambiguous. The field predicts RGB-only frames and re-estimates 3D after the fact, assuming a pixel video model plus a downstream pose estimator suffices. The bet is in First-principles below. |
| **Anchor papers** | [[2506.20134\|3D-World-Models-Survey]] (survey), [[2510.16732\|World-Models-for-Embodied-AI-Survey]] (survey), [[2604.22748\|Agentic-World-Modeling-Survey]] (survey), [[2604.19092\|RoboWM-Bench]] (benchmark), [[2507.01099\|Geometry-aware-4D-Robot-Video]] (method), [[2507.13347\|Pi3]] (method), [[2507.00990\|RIGVid]] (method) |
| **Key targets** | Avg task SR ≥0.64 across three sim tasks ([[2507.01099\|Geometry-aware-4D-Robot-Video]]) vs Dreamitate 0.12 / Diffusion Policy 0.12 (≈5× baseline), scaled out on [[2604.19092\|RoboWM-Bench]]'s predicted-video→executable-action step+final SR (more discriminative than perceptual plausibility); cross-view geometric consistency (higher mIoU) + lower FVD/AbsRel; generalize to novel viewpoints without retraining |

**Why it matters.**
- **The gap**: video-prediction-for-action methods (Dreamitate-class) predict future RGB frames and then bolt on a pose estimator — but RGB-only frames leave 6-DoF pose ambiguous, so the policies are brittle (Dreamitate and Diffusion Policy both at 0.12 avg task SR in [[2507.01099|Geometry-aware-4D-Robot-Video]]'s evaluation).
- **Today's answers**: [[2507.01099|Geometry-aware-4D-Robot-Video]] makes geometry a *predicted output* — SVD predicting future multi-view RGB *and* aligned 3D pointmaps (pointmap VAE + cross-view pointmap diffusion loss), no explicit camera poses at inference — so off-the-shelf trackers read 6-DoF straight off (0.64 avg task SR, ≈5× the 0.12 baselines). The nearest neighbor [[2509.00361|Gen-Visual-Foresight-Pose]] (GVF-TAPE) predicts in-model RGB-D and extracts 6-DoF in a closed loop (83.0% LIBERO, +11.56% over action-labeled IL) — but from a *single* view with a *learned* pose estimator, not cross-view-consistent points read by an off-the-shelf geometric tracker. [[2602.09878|MVISTA-4D]] reaches cross-view-consistent geometry but routes it to an end-to-end head, not a tracker. The cross-view-consistency-for-tracker mechanism + the H1 consistency-vs-fidelity ablation remain untaken.
- **The opening**: [[2604.27106|RecGen]]'s reconstruction-by-generation recovers explicit shape + 6-DoF pose at 92.7% ADD-SB@0.1 and +38.2 pp under occlusion vs SAM3D — proof the predicted-geometry → tracker-readable-pose pipeline holds where post-hoc estimation collapses.

**First-principles framing.**
- **First principle**: a 6-DoF pose is geometric. If the pointmaps agree across views, any tracker reads the pose straight off them — the pose is just the rigid transform between matching 3D points. RGB-only frames hide the pose: it is left implicit and changes with the viewpoint, so a downstream estimator re-solves it from scratch every frame with too little to go on. The pointmap machinery [[2507.13347|Pi3]] makes the consistent 3D the readout depends on.
- **Assumption being challenged**: the Dreamitate-class convention that a pixel video model + a downstream pose estimator suffices — held by [[2508.20840|Primitive-Embodied-WM]] (PEWM), which extracts 6-DoF from generated video *without explicit geometric supervision* (Gen6D, RGB-only). And the *softer* convention that geometry-from-a-single-view is enough: [[2509.00361|Gen-Visual-Foresight-Pose]] predicts RGB-D but reads pose with a *learned* estimator, not off cross-view-consistent points. [[2507.01099|Geometry-aware-4D-Robot-Video]]'s ≈5× SR gap shows the post-hoc/single-view estimator is the bottleneck.
- **The bet**: the discriminator that survives the whole field is *cross-view pointmap consistency read by an off-the-shelf geometric tracker* — not single-view RGB-D ([[2509.00361|Gen-Visual-Foresight-Pose]]) and not a learned estimator or RGB-only Gen6D ([[2508.20840|Primitive-Embodied-WM]]). [[2507.01099|Geometry-aware-4D-Robot-Video]]'s ≥0.64 avg task SR vs ~0.12 RGB-plus-estimator baselines (≈5×) is the only measurement of this magnitude anywhere in the literature — one paper's unreplicated three-task eval, not an established constant. So the falsifiable claim this direction actually stakes is narrower than "hit ≈5×": that the magnitude *replicates* when cross-view consistency is isolated on an independent benchmark, not just the anchor's own tasks. H1's ablation, run against [[2604.19092|RoboWM-Bench]]'s predicted-video→action SR rather than the anchor's three-task suite, is that replication gate — no neighbor has run it. If the consistency ablation collapses SR on RoboWM-Bench by a margin comparable to the anchor's ≈5× gap, the magnitude replicates and the bet holds; if the collapse is far smaller, ≈5× was anchor-specific and the honest headline shrinks to "consistency helps" with no stated multiplier.

**Related research papers.** One table on the axis the direction turns on — *the source of the geometry a tracker reads pose from* (predicted cross-view pointmaps / sensed depth / reconstruction-by-generation / latent-4D / implicit-in-policy) — with `Key result` and what each leaves missing. The contrast that defines the direction is externalized geometry (C2) vs internalized consistency (B2's [[2605.05126|ConsisVLA-4D]]).

| System | Pose-readout source | Key result | What's missing |
|---|---|---|---|
| [[2507.01099\|Geometry-aware-4D-Robot-Video]] | predicted cross-view-consistent pointmaps (SVD) | **0.64** avg task SR vs 0.12 baselines (≈5×), novel-viewpoint | three-task base; consistency-vs-RGB not ablated against the SR gain |
| [[2509.00361\|Gen-Visual-Foresight-Pose]] | predicted *single-view* RGB-D → *learned* 6-DoF pose estimator (closed loop) | **83.0%** LIBERO (+11.56% over action-labeled IL, +26.9% over video-pred), 56→86% real w/ human-video transfer | the nearest neighbor, but single-view (no cross-view pointmap consistency) + learned estimator, not an off-the-shelf tracker reading consistent points |
| [[2508.20840\|Primitive-Embodied-WM]] | generated video → 6-DoF *without* geometric supervision (Gen6D, RGB-only) | **93%** RLBench close-box, 16/20 real vs OpenVLA 0/20, 75× faster, 12 FPS | the Dreamitate-class convention C2 inverts — RGB-only pose makes it the baseline, not a solution |
| [[2601.05237\|ObjectForesight]] | predicted 3D object trajectories from egocentric video (DiT) | ADE **0.016 m** / ARE **2.30°** on EPIC-Kitchens, 2M-traj dataset | forecasts geometric trajectories, but offline from human video, not a tracker-readable in-policy rollout |
| [[2608.00486\|DreamTraj]] | 6-DoF trajectory read directly from an *early denoising step* of a frozen video-diffusion model's latents (7.1M-param flow-matching decoder), no video ever rendered, no external tracker | ADE **3.04 cm** / **7.9°** rotation error over 2.0s (beats privileged [[2601.05237\|ObjectForesight]]-class baseline's 7.52 cm / 28.6°); **4.6×** faster than generate-then-extract ([[2507.00990\|RIGVid]]/[[2602.20119\|NovaPlan]]-class) at 76s/49-frame trajectory | a genuinely cheaper source than every row in this table: skips rendering the video *and* the external tracker by reading pose off unrendered latents directly, closing the cost gap H2 asks about from the other direction, but scale-free (needs monocular depth to restore metric scale) and single-object trajectories, not cross-view-consistent scene pointmaps |
| [[2601.18323\|TC-IDM]] | post-hoc inverse-dynamics over generated video (motion prior) | **61.11%** real avg, novel-viewpoint + deformable + dual-arm/dexterous transfer | extracts action from generated video, but post-hoc IDM, not pose read off cross-view-consistent points |
| [[2511.12882\|MTV-World]] | multi-view video WM with object-mask prior (no explicit pointmaps) | J **54.9/45.0** dual-arm (vs Ctrl-World 35.3), zero-shot novel-view | multi-view consistency for *interaction* but RGB/mask, not tracker-readable 3D pointmaps |
| [[2502.10028\|3D-Foresight-Manipulation]] | predicted future depth/3D foresight (in-policy) | LIBERO **95.3%**, depth-critical real 70% vs 35% 2D, +6 ms latency | predicts 3D for *action*, not 6-DoF pose extraction by a tracker; single-view depth, no cross-view consistency |
| [[2602.09878\|MVISTA-4D]] | cross-view-consistent RGB-D 4D prediction | lower Chamfer/depth than baselines, 72.6% RLBench | routes consistency to an end-to-end head, not an external tracker — and its own w/o-cross-view-attention ablation drops only 3.8–5.0 pp (RLBench 72.6%→68.8%, RoboTwin 43.0%→38.0%), nowhere near the anchor's ≈5×; the closest existing calibration point for how large a real replication would need to be |
| [[2606.03943\|PointAction]] | predicted dynamic pointmaps (action interface) | 43.0% zero-shot xArm7 | predicts points for *action*, not pose extraction by a tracker |
| [[2605.05126\|ConsisVLA-4D]] | implicit consistency, internal (B2 boundary) | 98.1% LIBERO, 2.31× faster | geometry never leaves the model — the internalized contrast |
| [[2605.22882\|GEM-4D]] | dense 4D-correspondence distilled into the Video-DiT's *intermediate features* (geometry-velocity alignment + parallel Geometry-DiT flow-matching), read by AIDS — a learned confidence-gated tracker + geometry-kinematics pose fallback over "still imperfect" rollouts | **58%→75%** Droid-AUTOLab, **65%→83%** CLVR, **59%→87%** RAIL over TesserAct | the nearest NEW *internalized-distillation* foil that sharpens the externalized contrast (vs the anchor [[2507.01099\|Geometry-aware-4D-Robot-Video]]) — consistency lives in features, not output-space cross-view pointmaps, and pose comes from a learned gated tracker, not an off-the-shelf tracker reading consistent points. **Its deltas are over a *TesserAct* base on Droid/CLVR/RAIL, not the anchor's Dreamitate base on the three-task suite (0.12→0.64), so they are *not* directly commensurable with C2's ≈5× headline — GEM-4D shows an internalized route also lifts SR, not that it delivers "the same class of SR gain" the externalized pipeline does; the head-to-head (H4) is the only way to compare them on one base** |
| [[2504.20995\|TesserAct]] | explicit geometric channels alongside RGB | depth+normal sharpen action extraction | a channel, not tracker-readable cross-view pointmaps |
| [[2604.26694\|X-WAM]] | latent-4D, end-effector-derived camera poses | Chamfer 0.0049, 15 Hz | latent substrate — pose is internal, not externally read (now C1's latent-4D comparator) |
| [[2505.20279\|VLM-3R]] | reading geometry out of reconstructed 3D | grounds reasoning in reconstructed 3D | reconstruction for reasoning, not 6-DoF trajectory readout |
| [[2507.13347\|Pi3]] | pointmap/3D-reconstruction model | the consistent-pointmap machinery | a reconstruction backbone, not a predictive rollout |
| [[2604.19092\|RoboWM-Bench]] | predicted-video→executable-action SR (benchmark) | step+final SR across rigid/articulated/deformable/long-horizon, real-to-sim-validated; separates executable from merely-plausible where perceptual metrics saturate | the discriminative SR substrate the ≈5× headline scales onto (its perceptual-vs-executability split *is* H1's "consistency not RGB fidelity drives action"), but doesn't itself isolate 6-DoF readout from predicted vs sensed geometry |
| [[2510.16732\|World-Models-for-Embodied-AI-Survey]] | explicit-geometry WM + pose-extraction framing | frames the use case | survey only |
| [[2507.00990\|RIGVid]] | generated RGB video (Kling) → off-the-shelf 6-DoF object-pose *tracker* (FoundationPose) → retarget to end-effector, closed loop | **85%** avg SR across 4 tasks on xArm7 (matches real human demos), +35 pp over ReKep VLM-trajectory (50%) | the canonical Dreamitate-class instance with a *real* off-the-shelf geometric tracker — but RGB-only video, single-view, *monocular depth is the stated bottleneck*: it never predicts the cross-view-consistent pointmaps [[2507.01099\|Geometry-aware-4D-Robot-Video]] reads pose off, so it's the H1/H2 baseline (RGB+tracker, no cross-view consistency, no sensed/predicted depth) the cross-view claim must beat |
| [[2602.20119\|NovaPlan]] | generated future video → TAPIP3D dense 3D point-flow + Kabsch rigid-transform for 6-DoF object pose, + HaMeR hand-flow fallback under occlusion, VLM-switched (single-view, MoGe2 monocular-depth-grounded), closed loop | **70%** four-layer block stacking (vs NovaFlow 30% / π0.5 fails), **90%** block insertion, zero-shot FMB + assembly with non-prehensile recovery | a *sharper* single-view/monocular-depth baseline than [[2507.00990\|RIGVid]] (dense-flow+Kabsch object-pose readout, plus an occlusion hand-flow fallback no row has) — but its monocular-depth grounding is exactly the single-view bottleneck the cross-view loss must out-perform (H1), and its occlusion handling is a foil for [[2604.27106\|RecGen]]'s occlusion-gain claim (H5), not cross-view-consistent points an off-the-shelf tracker reads |
| [[2605.22272\|Imagine2Real]] | video generative prior → unified 4D *point trajectory* (robot **and** object) → BFM keypoints tracker → humanoid (Unitree G1) | Carry Box **0.00% → 82.65%**, Push Box 29.82% → 64.91% (sim2sim), zero-shot real HOI 0.5–2 kg | predicts sparse interaction-critical 4D points read by a tracker like [[2606.03943\|PointAction]] and [[2507.01099\|Geometry-aware-4D-Robot-Video]], but covers the *joint robot+object* HOI case no row does — sparse keypoints for whole-body action, not dense 6-DoF object pose, and latent-constrained (BFM) rather than externally tracker-readable cross-view pointmaps |

**Hypotheses & tests.** The FP bet — cross-view-consistent predicted pointmaps make pose tracker-readable, and consistency (not RGB fidelity) drives the action gain — decomposed.
1. **H1 — Cross-view consistency, not RGB fidelity or single-view depth, drives the action gain — and does so at a magnitude comparable to the anchor's ≈5×, not just directionally.**
   - *Prediction*: removing the cross-view pointmap diffusion loss (RGB quality held fixed) collapses trajectory-extraction accuracy and downstream SR more than degrading RGB FVD does — *and* a single-view-depth + learned-estimator variant ([[2509.00361|Gen-Visual-Foresight-Pose]]-style) underperforms the cross-view-consistent tracker readout on the same tasks, isolating consistency-for-tracker as the lever neither neighbor tested — *and* the resulting SR collapse on RoboWM-Bench is comparable in scale to the anchor's ≈5× gap, not a small dent.
   - *Test*: ablate the cross-view loss vs degrade RGB vs swap to single-view-depth + learned estimator; report SR delta for each on [[2604.19092|RoboWM-Bench]]'s predicted-video→action SR (its perceptual-vs-executability discrimination is exactly H1's axis — execution accuracy varies even at high perceptual scores) and against [[2602.08971|WorldArena]]'s measured *perception–functionality gap* (high FVD/visual quality ≠ downstream SR) at the world-model level.
   - *Row*: Geometry-aware 4D Robot Video / Gen Visual Foresight Pose / MVISTA-4D.
   - *Falsifier*: degrading RGB or single-view depth hurts SR as much → appearance / single-view geometry, not cross-view consistency, carries the gain. Separately, on magnitude: if the cross-view-ablation SR delta on RoboWM-Bench is only on the order of [[2602.09878|MVISTA-4D]]'s own cross-view-attention ablation (a 3.8–5.0 pp drop — RLBench 72.6%→68.8%, RoboTwin 43.0%→38.0% — not a multiplier) rather than comparable to the anchor's ≈5× gap, ≈5× was anchor-specific, not a replicated constant.
2. **H2 — Predicted pointmaps cost little vs sensed depth on pose readout.**
   - *Prediction*: 6-DoF extraction from predicted pointmaps loses only a small margin to extraction from sensed depth on the same tasks — small enough to justify predicting where sensing is unavailable.
   - *Test*: predicted-pointmap vs sensed-depth pose readout, matched tasks.
   - *Row*: Geometry-aware 4D Robot Video / RecGen.
   - *Falsifier*: predicted pointmaps lose a large margin → C2 only applies where depth sensing is impossible.
3. **H3 — Novel-viewpoint generalization has a measurable extrapolation envelope.**
   - *Prediction*: pose-extraction accuracy holds out to a bounded viewpoint-extrapolation range before degrading — the operational envelope of the externally-readable claim.
   - *Test*: sweep test viewpoint distance from training; locate the degradation point.
   - *Row*: Geometry-aware 4D Robot Video.
   - *Falsifier*: accuracy degrades immediately off-training-views → "no retraining" is overclaimed.
4. **H4 — Externalized geometry beats internalized on debuggability-critical tasks (and the internalized SR foil must be compared on one base, not on its own).**
   - *Prediction*: on the same task and *the same base*, C2's tracker-readout pipeline beats an internalized route — [[2605.05126|ConsisVLA-4D]]'s end-to-end implicit head, and the sharper internalized-distillation foil [[2605.22882|GEM-4D]] — where the geometry must be inspected/reused (multi-tracker, failure diagnosis), and loses where only cost matters. GEM-4D's reported 58→75 / 59→87 deltas are over a *TesserAct* base on Droid/CLVR/RAIL, not the anchor's Dreamitate base on the three-task suite, so they do **not** establish that internalization yields "the same class of SR gain" as externalization — only a matched-base head-to-head decides that, and it is exactly the test H4 runs.
   - *Test*: C2 vs ConsisVLA-4D **and** vs GEM-4D on matched tasks at one shared base; split by debuggability requirement (multi-tracker reuse, failure diagnosis) vs cost-only.
   - *Row*: Geometry-aware 4D Robot Video / ConsisVLA-4D / GEM-4D.
   - *Falsifier*: the internalized head matches externalization *on the same base* everywhere → externality buys nothing, and the internalized route's SR gain is genuinely equivalent.
5. **H5 — Occlusion is where predicted-geometry pose readout wins most.**
   - *Prediction*: the predicted-pointmap → pose pipeline's advantage over post-hoc estimation is largest under occlusion, mirroring [[2604.27106|RecGen]]'s +38.2 pp occlusion gain.
   - *Test*: stratify pose-readout accuracy by occlusion level.
   - *Row*: RecGen / Geometry-aware 4D Robot Video.
   - *Falsifier*: the advantage is flat across occlusion → predicting geometry doesn't specifically help the hard case.

> [!warning] Risks
> - **Single-anchor magnitude** — the ≥0.64 vs ~0.12 (≈5×) figure is [[2507.01099|Geometry-aware-4D-Robot-Video]]'s alone, unreplicated elsewhere. The nearest existing calibration point, [[2602.09878|MVISTA-4D]]'s own cross-view-attention ablation, clears only 3.8–5.0 pp — not proof either way (different comparison: one attention module inside an already-multiview pipeline, not full cross-view-geometry vs an RGB-only baseline), but a hint the true magnitude elsewhere could be far smaller. → Treat it as provisional, not established: H1's ablation on [[2604.19092|RoboWM-Bench]] (an independent benchmark, not the anchor's own three tasks) is the replication gate the bet's number must clear before it is cited elsewhere as a fact.
> - **Predicted pointmaps may be noisier than sensed depth**, degrading pose readout. → H2 quantifies the predict-vs-sense gap directly; if large, gate C2 to settings where sensing is unavailable (novel viewpoints, no depth sensor).
> - **Even a replicated magnitude may not transfer beyond simulation** — the anchor's three tasks and H1's RoboWM-Bench replication check are both sim-only. → Frame the contribution as *the mechanism* (explicit cross-view pointmaps → tracker-readable pose), with sim-to-real transfer as the explicit next step; do not extend the ≈5× claim to real-robot deployment without a dedicated real-world test.

### C3 — Persistent Geometric Memory as a Substrate-Agnostic Persistence Layer

| | |
|---|---|
| **Cluster** | C — Geometry-Native World Models & Memory |
| **Thesis** | That explicit geometric memory holds long-horizon coherence where attention-only models drift is now *settled* — [[2505.05495\|3D-Persistent-Embodied-WM]] showed it for an action-conditioned consumer. The unsolved question is whether one world-frame memory can serve as a *reusable persistence layer* over the doc's three geometric substrates — occupancy and latent-4D (both C1), and C2's pointmaps — and whether geometric + episodic memory *compound* on action-conditioned memory-required tasks. The field builds memory once, per substrate, and never benchmarks the two memory types together. The bet is in First-principles below. |
| **Anchor papers** | [[2604.22748\|Agentic-World-Modeling-Survey]] (survey), [[2504.21853\|Interactive-Generative-Video-Survey]] (survey), [[2602.04411\|Self-evolving-Embodied-AI]] (survey), [[2505.05495\|3D-Persistent-Embodied-WM]] (method), [[2603.17117\|MosaicMem]] (method), [[2603.24576\|Chameleon-Episodic-Memory]] (method), [[2605.10921\|RoboMemArena]] (benchmark), [[2606.09803\|Echo-Memory]] (benchmark), [[2603.04639\|RoboMME]] (benchmark) |
| **Key targets** | Substrate-agnostic persistence: each of occupancy / pointmaps / latent-4D (C1, C2) + the C3 layer beats the bare substrate on minute-scale coherence, scored on [[2602.08025\|MIND-Bench]]'s closed-loop revisit memory-consistency + action-control metric (>**4%** context-memory gain on the long-context-memory metric, substrate-level not per-method); episodic+geometric compound > max(either) on [[2605.10921\|RoboMemArena]] (68.9% of subtasks need history); per-representation decomposition read against [[2603.04639\|RoboMME]] (perceptual 44.51% / symbolic-Oracle 84.08% / human 90.5% over Counting/Permanence/Reference/Imitation — the layer-reuse and per-subtask splits for H1/H3); anchored to [[2505.05495\|3D-Persistent-Embodied-WM]] SRC 81.7% / FVD 91.9 (vs no-memory 63.4 / 194), [[2603.17117\|MosaicMem]] RotErr 0.51° vs 1.42°/4.65° at 16 FPS, [[2603.24576\|Chameleon-Episodic-Memory]] 100% episodic-recall / 73.5% spatial-tracking; [[2606.09828\|Mirage-LSM]] 10.57× faster / 55× lower 3D-cache GPU memory than RGB-point-cloud memory at WorldScore 70.36 (3D-Consistency 92.21) — the latent-3D efficiency frontier H5's footprint budget must beat; [[2606.09803\|Echo-Memory]] block-wise State-Space recurrence 69.00 open-domain VLM consistency vs raw-Context 58.63 vs Spatial-memory 17.12 — the memory-isolated coherence ranking H1's substrate-agnostic comparison is read against |

**Why it matters.**
- **The gap**: the headline "explicit geometric memory beats attention-only on action-conditioned long-horizon coherence" is *already taken* — [[2505.05495|3D-Persistent-Embodied-WM]] (PEWM) builds a volumetric world-frame memory, conditions an *action*-conditioned RGB-D WM on it, and beats the no-memory baseline (Scene-Revisit-Consistency **81.7%** vs **63.4%**, FVD **91.9** vs **194**) with downstream MPC/policy gains. So the open gap is not "does it help" but two things nobody has done: a memory layer that serves *any* of the three geometric substrates (occupancy, pointmaps, latent-4D), and a benchmark of geometric + episodic memory *together*.
- **Today's answers**: each memory type is built in isolation. Geometric: [[2603.17117|MosaicMem]] lifts 2D patches to 3D, RotErr **0.51°** vs 1.42°/4.65° at 16 FPS, but camera-controlled video not action-conditioned manipulation. Episodic: [[2603.24576|Chameleon-Episodic-Memory]] disambiguates indexable events (100% episodic-recall) but no persistent geometry. [[2506.05284|Long-Term-Spatial-Memory-WM]] already combines geometric + episodic in *one* WM (PSNR **19.10** vs 11.71 baselines on view-recall) — but for camera-video, not benchmarked on action-conditioned memory-required tasks.
- **The opening**: [[2605.10921|RoboMemArena]] proves the demand is real and *action-conditioned* — **68.9%** of its subtasks genuinely need history and reactive policies fail them — giving the substrate-agnostic + compound bet a benchmark target that PEWM's revisit-coherence metric does not cover.

**First-principles framing.**
- **First principle**: a world-frame geometric memory is *substrate-orthogonal* — object permanence (where things are when they leave view and return) is a property of the metric frame, not of the representation that fills it, so the *same* persistence mechanism should pin occupancy (C1), pointmaps (C2), or latent-4D ([[2604.26694|X-WAM]]). [[2505.05495|3D-Persistent-Embodied-WM]] proves the mechanism on an RGB-D substrate; the principle says it is not tied to that substrate. And geometric and episodic memory are *complementary* primitives — one fixes *where*, the other fixes *which event* — so they should compound.
- **Assumption being challenged**: that a shared memory design can generalize at all — held by [[2603.04639|RoboMME]] itself. Its own controlled study — 14 memory-representation × integration-strategy variants on one shared π0.5 backbone, across 16 memory-required manipulation tasks — found "no single memory representation or integration strategy universally outperforms others" (perceptual best overall **44.51%**, symbolic-Oracle **84.08%** on counting/grounding, human **90.5%**); task-dependence, not a reusable layer, is the field's best-evidenced conclusion. [[2505.05495|3D-Persistent-Embodied-WM]] and [[2603.17117|MosaicMem]] answer that by each building a *bespoke* geometric memory for their own model rather than betting on a shared layer; [[2603.24576|Chameleon-Episodic-Memory]] likewise builds episodic memory alone.
- **The bet**: (i) a single world-frame memory layer, dropped over each of occupancy, pointmaps, and latent-4D, raises minute-scale coherence above the bare substrate by a margin tracking [[2505.05495|3D-Persistent-Embodied-WM]]'s SRC 81.7% vs 63.4% no-memory gap — i.e. the layer is substrate-agnostic, not substrate-bespoke; and (ii) geometric ([[2603.17117|MosaicMem]]) + episodic ([[2603.24576|Chameleon-Episodic-Memory]]) memory *compound* on [[2605.10921|RoboMemArena]] (combined > max(either) on the 68.9% history-required subtasks), because they address different failure modes — neither demonstrated by any single paper. This runs directly against [[2603.04639|RoboMME]]'s task-dependence verdict, but at a narrower axis than RoboMME tested: *geometric persistence across representation substrates*, not RoboMME's symbolic-vs-perceptual-vs-recurrent comparison — the bet is that world-frame geometry is the one axis where a shared design does generalize.

**Related research papers.** One table on the axis the direction turns on — *the memory representation* (geometric-3D-patch / episodic-events / world-frame-semantic / evolving-3D / tiered-consolidation / O(1)-TTT / controllable-no-memory) — with `Key result` and what each leaves missing for action-conditioned long-horizon coherence.

| System | Memory representation | Key result | What's missing |
|---|---|---|---|
| [[2505.05495\|3D-Persistent-Embodied-WM]] | volumetric world-frame 3D-feature memory, *action*-conditioned RGB-D WM | SRC **81.7%** vs no-memory **63.4%**, FVD **91.9** vs **194**, 112-frame, MPC/policy gains | takes the bet's headline (action-conditioned geometric memory beats attention-only), but a *bespoke* memory for its own WM, indoor-nav, no substrate-agnostic layer or episodic compound |
| [[2603.17117\|MosaicMem]] | lifted-3D patches + Warped RoPE/Latent (hybrid) | RotErr **0.51°** vs 1.42°/4.65°, **16 FPS**, minute-level | camera-controlled video, not action-conditioned manipulation — the geometric half of the compound |
| [[2603.24576\|Chameleon-Episodic-Memory]] | disambiguated indexable episodic events + latent imagination | **100%** episodic-recall / **73.5%** spatial-tracking / **72.2%** sequential DSR | events, not persistent geometry — the episodic half of the compound |
| [[2506.05284\|Long-Term-Spatial-Memory-WM]] | combined geometric + episodic memory in one WM | view-recall PSNR **19.10** vs 11.71 baselines, SSIM 0.647 vs 0.44 | pre-empts the compound (H2) but camera-video only — never run on action-conditioned RoboMemArena tasks |
| [[2608.05042\|BridgeVLA++]] | disjoint temporal memory (anchor + keyframes + adaptive sub-goal keyframes, "what to do") and spatial memory (initial-scene point cloud, occlusion-robust re-rendering, "where to act"), each injected at a different stage of one action-conditioned manipulation policy | RMBench (memory-required) **18.9%→96.0%** (+13 pts over MemoryWAM), MemoryBench **11.3%→99.7%**, real Dobot **20.0%→93.3%** (3× over SAM2Act+); ablation: removing temporal memory collapses RMBench **96.0%→21.3%**, removing spatial memory costs occlusion tasks specifically | the action-conditioned manipulation-policy answer [[2506.05284\|Long-Term-Spatial-Memory-WM]]'s camera-video compound lacked: its own within-model ablation is close to H2's compound test (both memories necessary, different failure modes) but the two types are temporal-history and scene-geometry, not [[2603.24576\|Chameleon-Episodic-Memory]]'s disambiguated *events*, so it is evidence for the compound, not H2's exact instrument |
| [[2509.20297\|mindmap]] | world-frame metric-semantic 3D reconstruction memory in an action policy | **76%** avg SR (+56% over 3D Diffuser Actor) on out-of-view spatial-memory tasks, 97% Mug-in-Drawer | closes the action-policy flank for *one* bespoke memory, but a single manipulation policy — not a substrate-agnostic layer, no episodic compound |
| [[2603.03482\|PERSIST]] | persistent 3D world-frame memory, generative WM | lower FVD/FID over **600-step** episodes, mid-generation 3D editing, off-screen dynamics | persistent-3D coherence, but camera/generation-side — not pinned to an action-conditioned policy or used as a cross-substrate layer |
| [[2605.22283\|SOMA]] | world-frame spatial-semantic (2D detections lifted to 3D) | cuts target-localization time **40–59%** | semantic memory, geometric-coherence drift not reported |
| [[2604.11302\|3D-ALP]] | persistent 3D memory + world-model MCTS | **0.650** SR on memory-required steps vs 0.006 reactive; memory = **82%** of gain | the action-conditioned object-permanence result; planner-coupled |
| [[2510.01183\|EvoWorld]] | explicit evolving 3D memory in a generative WM | FVD **106.81** vs GenEx 199.76, **93.3%** long-horizon target-reaching | camera-video memory, not pinned to an action-conditioned policy |
| [[2606.03374\|eMEM]] | tiered spatio-temporal memory + R-tree 3D index | flat recall to **1-year** simulated delay | consolidation architecture, not geometric-coherence-under-action |
| [[2605.22814\|Remember-to-be-Curious]] | persistent 3DGS WM + long-term episodic memory | higher scene completeness HM3D/Gibson, RGB-only at test | exploration, not manipulation — episodic+geometry combined (H2 in one system) |
| [[2603.23497\|WildWorld]] | 108M-frame state-action long-horizon substrate | Action Following + State Alignment | a dataset/substrate, not a memory mechanism |
| [[2604.16484\|DexWorldModel]] | dual-state O(1) TTT memory | constant memory over 2,000 steps | efficiency, not geometric permanence — the contrast |
| [[2603.12255\|Spatial-TTT]] | hybrid Test-Time-Training layers (fast weights) + sliding-window attention, with depth-wise 3D spatiotemporal convolutions injecting a geometric-correspondence inductive bias into the fast-weight update | VSI-Bench **64.4** (SOTA), MindCube-Tiny **76.2%** (+12.3 pp over Gemini-3-pro), robust object-counting over **120-min** streams at ≈linear cost | the streaming, ≈linear-cost spatial-memory variant of [[2604.16484\|DexWorldModel]]'s O(1)-TTT, but with an *explicit geometric* inductive bias the dual-state TTT lacks — a fresh point on H5's coherence-vs-footprint axis; yet it is visual-QA over video (no action conditioning, no world-frame geometry persisted), so it backs the efficient-streaming-memory claim, not the action-conditioned geometric-vs-episodic compound (H2) |
| [[2510.10125\|CTRL-WORLD]] | controllable video, no persistent memory | **38.7→83.4%** unseen objects | controllability without memory — the no-memory baseline |
| [[2503.14489\|SEVA]] | implicit-attention camera memory | RotErr 1.42° (the explicit baseline MosaicMem beats) | drifts vs hybrid geometric memory |
| [[2506.03141\|CaM]] | static explicit-3D cache | RotErr 4.65° | breaks on dynamic scenes |
| [[2606.09803\|Echo-Memory]] | memory-isolated study: fixes VDiT backbone/data/training, swaps four memory families (Context / Compression / Spatial / State-Space) | block-wise State-Space recurrence tops open-domain VLM consistency **69.00** vs raw-Context **58.63** vs Spatial-memory max **17.12** on camera-revisit return probes | the controlled memory-vs-memory head-to-head H1's substrate-agnostic claim needs, but camera-revisit only (no action-conditioned RoboMemArena run) and scores semantic identity, not the geometric coherence the explicit-geometric rows (3D Persistent Embodied WM / MosaicMem) report |
| [[2603.04639\|RoboMME]] | memory-representation-vs-representation benchmark on the action-conditioned manipulation flank: 14 symbolic/perceptual/recurrent variants on a π0.5 backbone over a 4-way taxonomy (Counting / Permanence / Reference / Imitation), 16 tasks × 1,600 demos (benchmark) | perceptual best overall **44.51%**, symbolic-Oracle **84.08%** on counting/grounding, human **90.5%**; "no single memory representation dominates" | the representation-decomposed head-to-head H1 (which memory representation wins — the layer-reuse question) and H3 (its Permanence/Counting/Reference splits *are* the occlusion/counting/revisit splits) both need, *and* on action-conditioned manipulation where [[2605.10921\|RoboMemArena]] only proves demand (68.9% need history) without decomposing by representation — but scores per-representation SR, not the geometric-vs-episodic *compound* H2 turns on |
| [[2606.09828\|Mirage-LSM]] | latent-attributed 3D points stored directly in diffusion latent space (no rasterize-and-encode round-trip) | WorldScore **70.36**, 3D-Consistency **92.21**, closed-loop SSIM_C **0.825**, **10.57×** faster / **55×** lower 3D-cache GPU memory than RGB-point-cloud memory | a new point on the coherence-vs-cost axis (latent-3D vs MosaicMem's lifted-3D-patch / explicit RGB cache) that sharpens H5's footprint budget, but camera-controlled video, not action-conditioned, and filters dynamic objects out of the cache — the moved-object permanence STAR/3D-Belief target |
| [[2504.12369\|WorldMem]] | token-level retrievable memory bank (latent frames + 5D pose + timestamp) with state-aware attention + FOV-overlap retrieval | **PSNR 23.98 / rFID 15.37** on Minecraft hundreds of frames past context, PSNR 23.34 RealEstate10K loop-closure, holds user-placed objects over wide camera moves | the retrievable memory-unit representation the camera rows (MosaicMem / EvoWorld / PERSIST) don't carry, and explicitly frames the geometric-vs-implicit tradeoff C3 names — but camera/generation-side, not pinned to an action-conditioned policy or used as a cross-substrate layer |
| [[2605.18813\|CoME]] | composition of memory experts (STM local + LTM episodic-via-LoRA-TTT + SLTM geometric) fused by Product-of-Contrastive-Experts | LPIPS **0.097** / SSIM **0.892** Memory-Maze (vs base DiT 0.209 / 0.771), RECON ATE **0.96**, matches full-attention at **60×** less training compute | a memory-architecture representation that decomposes geometric vs episodic into separate experts (the H2 compound built into one model) and serves H5's footprint axis alongside DexWorldModel's O(1)-TTT — but diffusion camera/navigation WM, never run on action-conditioned RoboMemArena |
| [[2510.20328\|MemER]] | experience-retrieval visual memory (1D single-linkage clustering of VLM-nominated keyframes) over a generalist policy | **59/60** object retrievals + **1** wrong scoop on minute-scale partial-observability tasks, on par with human-subtask oracle, ~1-2 Hz real closed-loop | the scalable retrieval mechanism on the action-conditioned manipulation flank that RoboMemArena's demand calls for and no cited C3 row provides — but visual-only retrieval, no persistent world-frame geometry, so it leaves the geometric half of the compound to MosaicMem/3D Persistent Embodied WM |
| [[2602.15010\|BPP]] | selective key-history-frame memory: VLM (1 Hz) filters raw history to task-relevant keyframes, latency-masked | **+70%** avg SR over best history-conditioned baselines on real bimanual Mug-Replacement / Drawer-Search, beats oracle on Variable-Password | a selective-memory representation on the action-conditioned axis that fixes the OOD-history coverage problem — distinct from the CTRL-WORLD no-memory baseline and the dense-context rows — but events are abstracted, not persisted as geometry, so it omits the where-it-is permanence the world-frame rows keep |
| [[2501.18564\|SAM2Act]] | explicit spatial-memory architecture (memory bank + encoder + attention) inside a multi-view manipulation policy + MemoryBench | SAM2Act+ **94.3%** on non-Markovian MemoryBench (+39.3% over prior), SAM2Act **86.8%** RLBench, **4.3%** drop on Colosseum perturbations | an action-conditioned geometric-memory policy in the mindmap / 3D-ALP family but with a different memory architecture and its own spatial-memory eval suite — yet a single bespoke manipulation policy, not a substrate-agnostic layer, and no episodic compound |
| [[2606.09827\|MemoryVLA++]] | dual perceptual-detail + cognitive-semantic memory bank (redundancy-aware consolidation + gate fusion) plus SVD latent imagination, in a VLA | Libero **98.4%**, **+26%** real memory-dependent / **+28%** imagination-dependent tasks over CogACT, Libero-Plus **82.7%** fine-tuned OOD | a joint fine-grained+semantic memory representation distinct from the table's geometric-only and episodic-only rows — pre-empts the H2 compound on the policy side — but the two memories are both perceptual/semantic, not a persistent world-frame geometry, so it doesn't test geometric-vs-episodic complementarity the way MosaicMem×Chameleon would |
| [[2511.11478\|LIBERO-Mem]] | object-centric slot-SSM maintaining persistent object *identity* (Slot-Attention + temporal-contrastive loss + Slot-Dynamics-Encoder), tracking the same instance through occlusion | LIBERO-Goal **83.0%** (vs SlotVLA 75.5% / OpenVLA 32%), LIBERO-Mem subgoal **14.8%** (vs 5.0% baselines) | the cleanest *object-level* identity-disambiguation mechanism — the manipulation-frame counterpart to [[2603.24576\|Chameleon-Episodic-Memory]]'s disambiguated events, structurally distinct from event-indexing, MemoryVLA++'s perceptual-cognitive consolidation, BPP/MemER keyframe-selection, and SAM2Act's spatial-memory architecture — sharpening H3's perceptual-aliasing concentration; but its LIBERO-Mem suite is a redundant non-Markovian benchmark ([[2605.10921\|RoboMemArena]] + RoboMME cover that), so its value is purely as a method |
| [[2510.07134\|TrackVLA++]] | confidence-gated Target-Identity-Memory (TIM) inside a VLA: *freezes* the identity representation during occlusion, re-IDs only on confident re-detection, + Polar-CoT occlusion-signaling | EVT-Bench Distracted-Tracking **74.0%** SR (vs NavFoM 62.0%), +**7–17%** real on a Unitree GO2 across obstacle/winding/distractor | a memory-*update* mechanism no cited row holds — structurally distinct from [[2511.11478\|LIBERO-Mem]]'s slot-identity, Chameleon's event-indexing, MemoryVLA++'s consolidation and SAM2Act's spatial architecture — operationalizing the Risk-2 "goal-directed, not similarity-only retrieval" and sharpening H3's occlusion-permanence split; but embodied visual *tracking* (target-following), not manipulation-frame coherence, and an identity-token not a world-frame geometry |
| [[2509.22548\|JanusVLN]] | dual implicit neural memory decoupling visual-*semantic* vs *spatial-geometric* memory into separate fixed-size representations, with an ablation removing each | R2R-CE Val-Unseen SR **60.5%** / SPL **56.8%** (+up to 10.8% over explicit-memory methods); removing the spatial-geometric memory drops SPL **8.3** points (**49.2→40.9**) | the cleanest action-conditioned *dual-memory* ablation showing two complementary types are *both* essential — exactly the structure H2's compound turns on, distinct from [[2605.18813\|CoME]]'s diffusion-WM experts by being a navigation policy with an action-grounded ablation; but its compound is geometric × *semantic* (not geometric × episodic), implicit (not explicit world-frame), and navigation (not manipulation) — an H2-adjacent precedent, not an H2 solver |
| [[2608.10886\|GESTO]] | persistent 4D scene graph (geometric) coupled with a two-level activity hierarchy — atomic human-object interactions LLM-grouped into goal-driven events (episodic) — built fully automatically from RGB-D via VLM extraction + geometric-semantic grounding | **0.71** text / **0.75** binary / **0.70** time on retrospective QA, near an externally-grounded baseline (0.70/0.78/0.78) and far above no-GT (0.33/0.29/0.50); removing the event hierarchy collapses time accuracy to **0.40** | the sharpest H2 within-model ablation yet — geometric persistence *and* episodic event-grouping are jointly necessary, built end-to-end with no hand annotation — but retrospective QA over recorded sessions, not action-conditioned RoboMemArena-style manipulation, so like [[2506.05284\|Long-Term-Spatial-Memory-WM]] it settles the compound's *architecture* without settling it on H2's target substrate |
| [[2505.13696\|ESWM]] | spatial world model built from an external bank of sparse, non-sequential one-step (state,action,state) episodic memories; meta-trained to infer missing transitions | navigator **96.8%** SR / **99.2%** path-optimality, **93%** SR after obstacle change by editing memory alone (vs EPN 72% / RL 56%); latent spontaneously forms a geometric map | the episodic→geometric construction bridge at the heart of H2 — assembling coherent geometry from fragmented episodic experience, which no cited row demonstrates — but gridworld navigation with abstract transitions, not action-conditioned manipulation or RGB-D geometry, so the bridge is shown in principle, not on RoboMemArena |
| [[2604.11306\|Hierarchical-Episodic-Memory]] | online hierarchical episodic memory with LLM-estimated relevance-based forgetting + feedback-learned retention rules | **45%** smaller memory / **35%** less query compute, **+70%** second-round QA accuracy after feedback; deployed on Armar-7 humanoid | the forgetting/retention dimension that complements eMEM's tiered R-tree consolidation and Chameleon's episodic events on the action-conditioned side — but consolidation/QA on multimodal logs, no persistent geometry or coherence-under-action, so like eMEM it's a memory-management architecture, not geometric permanence |
| [[2607.13245\|JITOMA]] | just-in-time scene-graph growth: a two-tier memory (ephemeral hypotheses + dormant anchors) that activates dense semantic processing only for task-relevant regions, gated by task intent | **+5.0/+9.4/+7.3** (IoU/mR@1/mR@3) long-horizon grounding gains, active graph bounded to **1–4** objects (vs hundreds/thousands baseline), **9–21×** faster than caption-heavy baselines | a resource-gating mechanism for *scaling* persistent memory to long horizons without perceptual saturation, bears directly on H5's footprint budget, but gates semantic detail, not geometric coherence, so it doesn't itself test the world-frame persistence H1/H4 turn on |
| [[2511.14004\|STAR-Memory-Action]] | unified spatio-temporal memory-action loop: non-parametric timestamped+posed+semantic memory queried (in time) interleaved with embodied skills (in space) by an LLM policy | highest execution SR across all 5 Visible-Object-Search families, **0.67** class-based Interactive-Search (Oracle), transfers to a physical Tiago in a real apartment | the object-permanence-when-relocated core of C3 — jointly reasoning past geometry + temporal events in an action loop, beyond SOMA's semantic-only memory — with its own active spatio-temporal-search benchmark; but mobile object retrieval/navigation, not manipulation-frame coherence, so it doesn't report the geometric-drift the world-frame rows track |
| [[2605.11367\|3D-Belief]] | uncertainty-aware generative 3D-belief memory: diffusion over 3DGS primitives that infers + sequentially updates unobserved regions with multi-hypothesis sampling | object-completion BEV-IoU **0.484** at 55% visibility, object-permanence LPIPS **0.123**, ObjNav **59.17%** sim / **55.56%** real | a probabilistic belief-memory representation absent from the table — directly the out-of-view object-permanence axis H3 isolates (where geometry alone is under-determined) — but exploration/ObjNav, not pinned to an action-conditioned manipulation policy, so it supplies the unobserved-region reasoning the deterministic world-frame rows lack without closing the action-conditioned compound |
| [[2606.17480\|GeneralVLA-2]] | geometry-governed memory: a KnowledgeBank retrieved on utility/safety/geometric-applicability (not similarity-only) + geometry-aware multi-view reconstruction-memory, in a VLA planner | **+4.53%** over similarity-retrieval (ReasoningBank) on Terminal-Bench, **−2.20%** Chamfer / **+2.36%** PSNR recon, 10/14 RLBench + 4/4 real tasks training-free | directly answers the C3 Risk that similarity-only episodic retrieval interferes — a governed-retrieval memory no cited row offers — but the geometry is per-object reconstruction-for-planning, not a persistent world-frame, so it governs *which* memory to reuse without pinning *where* across minute-scale horizons |
| [[2603.19137\|GSMem]] | stored 3D Gaussian Splatting as a persistent, re-renderable spatial memory queried for optimal novel-viewpoint re-observation + online language field | GOAT-Bench **67.2%** SR / **46.9%** SPL, SOTA Active-EQA; robust to missed/wrong detections via post-hoc re-rendering | a stored-3DGS-as-memory representation distinct from PERSIST/EvoWorld (which *generate* 3DGS) and from object-centric/view-based memory — the dense geometric half on the embodied-reasoning flank — but exploration/EQA navigation, not an action-conditioned manipulation policy, and dense 3DGS carries the footprint cost H5 weighs against compact memories |
| [[2411.17735\|3D-Mem]] | compact multi-view 'Memory Snapshots' (explored) + 'Frontier Snapshots' (unexplored) via co-visibility clustering, prefiltered for VLM reasoning | GOAT-Bench **69.1%** SR / **48.9** SPL, A-EQA **52.6** LLM-Match; **10.94** snapshots from 39.76 observations (→**3.26** after prefilter) | the compact-vs-dense memory tradeoff made explicit — a distinct compact-geometric-memory representation contrasting GSMem's dense 3DGS and the dense rows on the scene-memory axis — but image-snapshot memory feeding a 2D VLM, not a metric world-frame, so it scales cheaply at the cost of the continuous re-renderable geometry GSMem/3D-Belief keep |
| [[2606.24068\|ObsGraph]] | hierarchical observation scene-graph (object / view / room layers) with LLM hierarchical retrieval + information-gap-driven adaptive exploration | EM-EQA **61.5%** LLM-Match, A-EQA **54.5%** / **45.0%** SPL, GOAT-Bench **72.7%** SR / **51.5** SPL | a *hierarchical* (multi-abstraction) memory representation distinct from 3D-Mem's flat snapshots and GSMem's dense 3DGS — its object/view/room layering is the layered-reuse structure H1's substrate-agnostic layer would want — but image-crop-and-view memory feeding a 2D VLM for exploration/EQA, not a metric world-frame pinned across action-conditioned manipulation, so it leaves the geometric-coherence-under-action half to the world-frame rows |
| [[2606.28720\|CubifyGS]] | object-centric 3DGS memory: a global reusable-asset library of Gaussian object templates + spatio-temporal object-lifecycle detection (vanish/appear via ray-cast occupancy) + probabilistic ghost-pruning and semantic asset retrieval | **+35.83%** PSNR over the strongest baseline, **20 FPS** map maintenance (**40×** faster than continuous-training dynamic baselines), AP25 **65.2**, ~**100%** fine pose alignment up to **25°** | the moved-object-permanence mechanism the camera rows lack: [[2606.09828\|Mirage-LSM]] *filters* dynamic objects out of the cache, while CubifyGS keeps rearranged objects coherent via object-lifecycle asset reuse (the STAR / 3D-Belief moved-object target), but a camera-side mapping/rendering system, rigid-rearrangement only, not action-conditioned, so it leaves the manipulation-frame compound to the world-frame rows |

**Hypotheses & tests.** The reframed bet — one world-frame memory is a substrate-agnostic persistence *layer*, and geometric + episodic memory *compound* on action-conditioned tasks — decomposed. (The headline "geometric memory beats attention-only for an action-conditioned consumer" is *settled* by [[2505.05495|3D-Persistent-Embodied-WM]]; the front-line is the layer-reuse + compound nobody has run.)
1. **H1 — One world-frame memory layer transfers across occupancy, pointmap, and latent-4D substrates.**
   - *Prediction*: dropping a single world-frame memory mechanism over occupancy (C1), pointmaps (C2), and latent-4D ([[2604.26694|X-WAM]]) raises each substrate's minute-scale coherence above the bare substrate by a margin tracking [[2505.05495|3D-Persistent-Embodied-WM]]'s SRC 81.7% vs 63.4% gap — i.e. the layer is substrate-agnostic, not the bespoke per-model memory PEWM/MosaicMem built.
   - *Test*: each of occupancy / pointmaps / latent-4D with vs without the shared C3 layer, scored on [[2602.08025|MIND-Bench]]'s closed-loop revisit memory-consistency + action-control metric (temporal stability + scene-revisit coherence, decoupled from any one method's bespoke metric) so the coherence lift is read off a substrate-level, model-agnostic suite rather than PEWM/MosaicMem self-numbers — though MIND-Bench is camera/scene-navigation, so it backs the coherence-metric + model-agnostic claim, not all three manipulation substrates by itself.
   - *Row*: 3D Persistent Embodied WM / MosaicMem.
   - *Falsifier*: the lift varies wildly per substrate or one substrate gets none → memory is substrate-bespoke, not a reusable layer.
2. **H2 — Episodic events + geometric memory compound on action-conditioned tasks.**
   - *Prediction*: combining [[2603.24576|Chameleon-Episodic-Memory]]'s indexable events with [[2603.17117|MosaicMem]]'s geometry-consistent patches beats either alone on [[2605.10921|RoboMemArena]] — extending [[2506.05284|Long-Term-Spatial-Memory-WM]]'s camera-video compound to action-conditioned memory-required tasks, where disambiguated *events* + persistent *geometry* address different failure modes.
   - *Test*: events-only vs geometry-only vs both on RoboMemArena. [[2608.05042|BridgeVLA++]] runs the closest within-model version of this test on real action-conditioned manipulation (not RoboMemArena, but RMBench/MemoryBench): its temporal (history/sub-goal) and spatial (occlusion-robust scene geometry) memories are each independently necessary (removing temporal collapses RMBench 96.0%→21.3%; removing spatial costs occlusion-specific tasks), consistent with H2's compound prediction, though its "temporal" memory is history/sub-goal-keyframe, not [[2603.24576|Chameleon-Episodic-Memory]]'s disambiguated *events*, so it doesn't fully settle H2's exact instrument. [[2608.10886|GESTO]] adds a second within-model compound-ablation (geometric 4D scene graph × episodic event hierarchy, time accuracy collapsing 0.70→0.40 without the event layer) — the closest yet to H2's exact events-vs-geometry decomposition, but on retrospective QA, not RoboMemArena's action-conditioned manipulation.
   - *Row*: Chameleon / MosaicMem / Long-Term Spatial Memory WM / BridgeVLA++ (within-model compound, action-conditioned) / GESTO (within-model compound, QA-only).
   - *Falsifier*: both ≈ max(either) → the two memory types are redundant, not complementary.
3. **H3 — The compound advantage concentrates on perceptual-aliasing + out-of-view subtasks.**
   - *Prediction*: decomposing [[2605.10921|RoboMemArena]] by subtask type, the episodic+geometric compound's margin over geometric-only is largest on perceptual-aliasing/occlusion subtasks (where events disambiguate) and ≈0 on pure-revisit subtasks (where geometry alone suffices).
   - *Test*: per-subtask margin of compound vs geometric-only on RoboMemArena's occlusion/counting/revisit splits.
   - *Row*: Chameleon / RoboMemArena.
   - *Falsifier*: a uniform margin across subtask types → episodic memory helps globally, not via aliasing-specific disambiguation.
4. **H4 — World-frame memory holds coherence better than robot-frame memory.**
   - *Prediction*: a geometric memory pinned to a persistent *world*-frame ([[2505.05495|3D-Persistent-Embodied-WM]]'s action→camera-pose retrieval, [[2603.17117|MosaicMem]]'s lifted-3D patches) holds long-horizon coherence better than a robot-frame memory — decoupling memory from the body's pose reduces drift.
   - *Test*: world-frame vs robot-frame memory on long-horizon traversal; report drift.
   - *Row*: 3D Persistent Embodied WM / SOMA.
   - *Falsifier*: robot-frame matches world-frame → the frame choice doesn't drive drift.
5. **H5 — The substrate-agnostic layer costs less than substrate-bespoke memory at equal coherence.**
   - *Prediction*: a single shared C3 layer reaching PEWM-level coherence over all three substrates has a lower total parameter/memory footprint than three bespoke per-substrate memories ([[2505.05495|3D-Persistent-Embodied-WM]] + occupancy-memory + pointmap-memory built separately), against [[2604.16484|DexWorldModel]]'s O(1) TTT budget.
   - *Test*: shared-layer vs three-bespoke-memories on (coherence × footprint) across occupancy / pointmap / latent-4D tasks.
   - *Row*: 3D Persistent Embodied WM / DexWorldModel.
   - *Falsifier*: the shared layer costs as much as three bespoke memories at equal coherence → reuse buys no efficiency, only engineering convenience.

> [!warning] Risks
> - **Explicit geometric memory needs reliable 3D lifting** — off-the-shelf estimators can fail on texture-poor scenes ([[2603.17117|MosaicMem]]). → Hybridize with implicit attention ([[2603.17117|MosaicMem]]'s own design) so the model degrades gracefully when lifting is noisy.
> - **Episodic memory retrieval can interfere** on visually-aliased-but-irrelevant events ([[2603.24576|Chameleon-Episodic-Memory]]). → Use disambiguated indexable encoding + goal-directed retrieval, not similarity-only retrieval; validate on [[2605.10921|RoboMemArena]]'s occlusion/counting splits.
> - **Memory adds footprint** against the real-time deployment budget. → H5's patch-level vs [[2604.16484|DexWorldModel]] O(1) TTT Pareto is the go/no-go; persistent memory only earns its place if coherence gain beats the memory cost.

---

## Cluster D — Reconstruction for Embodied Perception

*Reconstruction built for interaction-readiness — geometry that carries physics and kinematic structure, not just radiance. The two directions split by the unit of reconstruction: a whole scene an agent acts in (D1) and the reusable single object that populates it (D2), the same readiness-over-fidelity inversion applied at two scales.*

### D1 — Interaction-Ready Scene Reconstruction: Whole Environments You Can Act In

| | |
|---|---|
| **Cluster** | D — Reconstruction for Embodied Perception |
| **Thesis** | An embodied agent acts on *scene-level interaction-readiness* — geometric validity, physical parameterization, kinematic executability of a whole reconstructed environment — not on appearance. The reconstruction field optimizes visual fidelity (PSNR/FID) and assumes a higher-fidelity NeRF/3DGS scene is a better embodied asset. The bet is in First-principles below. |
| **Anchor papers** | [[2604.26509\|3D-Generation-for-Embodied-AI-Survey]] (survey), [[2506.20134\|3D-World-Models-Survey]] (survey), [[2510.16732\|World-Models-for-Embodied-AI-Survey]] (survey), [[2505.17966\|Single-View-Mesh-for-Robotics]] (benchmark), [[2404.09833\|Video2Game]] (method), [[2605.26115\|TriSplat]] (method), [[2506.06440\|Vid2Sim]] (method) |
| **Key targets** | Single-video → interactive environment at ≥100 FPS browser-compatible ([[2404.09833\|Video2Game]]); per-criterion readiness pass/fail on [[2505.17966\|Single-View-Mesh-for-Robotics]]'s 5 robotics desiderata (Chamfer<**2 mm**, no-collision, stability<5° tilt, occlusion-robustness, <2 s — 12 recon models on YCB-Video + Aria, ~**50%** grasp transfer); simulation-readiness criteria met (geometric validity + physical parameterization + kinematic executability + URDF/MJCF compat) per [[2604.26509\|3D-Generation-for-Embodied-AI-Survey]]; [[2602.10116\|SAGE-Scene]] 99.9% stability / 1.9% collision in Isaac Sim (the readiness-optimization endpoint H1's dual-objective must reach) |

**Why it matters.**
- **The gap**: the reconstruction community optimizes radiance (PSNR/SSIM/FID), and [[2604.26509|3D-Generation-for-Embodied-AI-Survey]] names the mismatch as *the* embodied bottleneck — "a crucial distinction between conventional 3D generation (focused on visual appearance) and embodied-oriented 3D generation, which demands interaction readiness, physical grounding, and simulator compatibility," blocked by "scarcity of physical annotations."
- **Today's answers**: the sim-ready-twin-from-single-video build is *crowded* — [[2510.05560|HoloScene]] (NeurIPS'25) bakes physics into reconstruction (Isaac-Sim-in-the-loop), reports an Object-Readiness/Stable-Ratio metric (**81.7%** stable, **100%** object-reconstruction on Replica) *alongside* PSNR/SSIM, and surfaces the readiness-vs-fidelity trade-off as an ablation co-product; [[2404.09833|Video2Game]] fuses NeRF + baked mesh + physics at 100+ FPS; [[2605.26115|TriSplat]] outputs sim-loadable meshes (33–249× faster); [[2506.06440|Vid2Sim]] reconstructs appearance + geometry + physics. None runs the *controlled* readiness-vs-fidelity per-criterion comparison (same scene, PSNR-optimized vs the four readiness criteria), and all but HoloScene's future-work are rigid-body only.
- **The opening**: [[2406.10788|Embodied-Gaussians]] couples a 3DGS render to PBD particles in a predict-correct loop at 30 Hz with lower tracking error — proof a physics-coupled reconstruction an agent acts in is buildable, not a downstream conversion of a fidelity asset.

**First-principles framing.**
- **First principle**: an embodied agent interacts with geometry, physics, and kinematics — not radiance. A reconstruction's embodied value is its *interaction-readiness* (can the agent collide, grasp, articulate in it), a property orthogonal to and not implied by visual fidelity. [[2003.08515|SAPIEN]] sets the kinematic-executability (URDF) bar a reconstructed scene must hit to be acted in.
- **Assumption being challenged**: this is a measurement gap, not a contested belief — no reconstruction paper argues that PSNR/FID fidelity *causes* embodied usefulness. The field optimizes it because that is the metric inherited from novel-view-synthesis benchmarks (held-out-view PSNR, the NeRF/Mip-NeRF lineage), not because anyone has defended fidelity against a physical-readiness criterion and won. Where the two are measured side by side, fidelity already loses: [[2510.05560|HoloScene]]'s Table-3 ablation reports "a trade-off between scene-level reconstruction and instance-level physical stability," and [[2505.17966|Single-View-Mesh-for-Robotics]] (RA-L'25) shows CV-benchmark-winning single-view reconstructions miss the 2 mm robotics bar (median Chamfer ~5 mm–1 cm, ~50% grasp transfer) with colliding/unstable objects. So what's genuinely missing is not a rebuttal to defend against — it's the *controlled* per-criterion measurement (same scene, PSNR-objective vs the four readiness criteria) that isolates the trade-off HoloScene only surfaces as an ablation co-product, and the *articulation* readiness HoloScene names as future work but does not recover.
- **The bet**: the deliverables are the *controlled measurement* and the *articulation* readiness HoloScene's joint objective leaves out — specifically, (i) building the same scene under (a) a PSNR objective vs (b) the survey's four readiness criteria, the fidelity-optimal asset fails ≥1 criterion measurably more often, reported per-criterion pass/fail (the controlled isolation HoloScene only surfaces as an ablation co-product); and (ii) single-video reconstruction recovers rigid decomposition reliably but fails articulated structure (joints/DoF) for URDF/MJCF export without multi-view or interaction data — the rigid-body ceiling HoloScene names but does not cross.

**Related research papers.** One table on the axis the direction turns on — *what the reconstruction outputs for embodied use* (physics-baked-scene / sim-format-mesh / scene-physics-for-sim / physics-coupled-render / dynamics-carrying-3DGS / articulation-reference / transfer-gap-neighbor) — with `Key result` and what each leaves missing. The transfer-gap rows are the [[Sim2Real|Sim2Real]] neighbors D1 borders but does not own.

| System | Reconstruction output | Key result | What's missing |
|---|---|---|---|
| [[2404.09833\|Video2Game]] | single-video → NeRF + baked mesh + rigid-body physics | **100+ FPS** browser-compatible interactive environment | rigid entities only — articulated structure from single video unproven |
| [[2510.05560\|HoloScene]] | single-video → sim-ready interactive twin, Isaac-Sim-in-the-loop | **81.7%** stable-ratio, **100%** object-reconstruction (Replica); Table-3 fidelity-vs-stability trade-off | reports the orthogonality as an ablation co-product, not D1's controlled per-criterion measurement; rigid-body only (articulation named as future work) |
| [[2505.17966\|Single-View-Mesh-for-Robotics]] | per-criterion readiness scorecard over 12 single-view recon models (benchmark) | 5 robotics desiderata (Chamfer<2 mm / no-collision / stability<5° / occlusion / <2 s); most miss **2 mm** bar (~5 mm–1 cm Chamfer), ~**50%** grasp transfer, colliding/unstable poses | the standardized per-criterion readiness protocol H1 needs (object-level on YCB-Video + Aria), but a one-sided probe — no readiness-*optimizing* pipeline, no scene-level dual-objective isolation |
| [[2603.14010\|URDF-Anything+]] | image→articulated URDF generation | Part IoU **0.879**, Joint-Axis-Err **0.129 rad**, zero-shot real laptop 100% / drawer 90% | recovers articulation→URDF (D1's H2 gap), but from a curated image, not in-the-wild single video of a whole scene |
| [[2506.08334\|iTACO]] | casual-capture → articulated object reconstruction | Joint-Axis-Err **0.32 rad** (vs 0.82–1.16), type-error **15.9%** (vs 40–89%) | articulation from casual capture, but per-object — not whole-scene articulated decomposition for an act-in environment |
| [[2509.17647\|VideoArtGS]] | monocular video → articulated digital twin (3DGS) | revolute-axis error **0.32°** (vs 13.83°), position error **0.42 cm** | the single-video articulation path D1's H2 needs, but object-level twins, not a readiness-scored scene |
| [[2603.19231\|MonoArt]] | single-image → articulation recovery on PartNet-Mobility | Chamfer **0.77**, type-accuracy **88.26%** | establishes the single-image articulation ceiling H2's test is read against, but single-image not single-video, no whole-scene decomposition |
| [[2608.04842\|RORA]] | single *static*-configuration video (no motion, no multi-state capture) → hybrid 3DGS+mesh reconstruction + geometry-driven automatic joint-suggestion (boundary detection, axis estimation, bidirectional fusion) + light human-in-the-loop confirmation (**42s**) → URDF | chained-linkage (hardest category) angular error **0.849°** vs Articulate-Anything **22.545°** / ScrewSplat **62.820°**, positional error **0.028m** vs **1.367m/0.636m**, both baselines "catastrophically fail" where RORA succeeds; **7m23s** total runtime, deployed in Isaac Sim | the sharpest H2 instrument yet: recovers *multi-joint chained* articulation from a single static video, closer than [[2603.14010\|URDF-Anything+]]/[[2506.08334\|iTACO]]/[[2509.17647\|VideoArtGS]] to closing the ceiling, but human-in-the-loop (not automatic) and object-level, not the whole-scene decomposition H2 asks for |
| [[2605.26115\|TriSplat]] | feed-forward triangle meshes loadable in Isaac Sim/Unity | **33–249×** faster than Gaussian baselines, no post-hoc meshing | simulator-format geometry, but no physical parameterization baked in |
| [[2506.06440\|Vid2Sim]] | appearance + geometry + physics for mesh-free scene sim | reconstruction optimized for the simulator, not the renderer | scene physics, but readiness-vs-fidelity not quantified |
| [[2406.10788\|Embodied-Gaussians]] | 3DGS render + PBD particles in a predict-correct loop | **30 Hz** on 3 cameras, lower tracking error | physics-coupled, but a tracking WM more than a reusable asset |
| [[2607.08948\|SplatCtrl]] | live 3DGS reconstruction (RGB-D, fixed Gaussian budget) → Gaussian-Process distance field → continuous SDF + collision-probability field feeding QP-IK control-barrier constraints | planning success **95.01%** vs plain-3DGS **32.83%** (2 cameras, 942 trials), real Franka **95.8%**, reconstruction **~240 Hz** / QP **>100 Hz** | a direct, quantified instance of D1's geometric-validity criterion — turns a radiance asset into a collision-checkable field with a huge (~3×) success margin over the un-converted 3DGS — but a real-time *live-reconstruction* controller, not a readiness-scored offline asset pipeline, so it complements rather than replaces H1's controlled PSNR-vs-readiness test |
| [[2606.21753\|RAF]] | representation-abstraction layer: 3DGS + meshes + fluids unified into one particle set, dispatched to a solver-agnostic multi-solver kernel (rigid/articulated/MPM/SPH/PBD) against a reconstructed static collision mesh, then reverse-translated to per-asset visuals | five previously-impossible heterogeneous scene-level scenarios (fluid-on-mesh, MPM-on-3DGS soft body, articulated arm, cloth-on-statue, rigid-in-basket), each under **10 minutes**; ablating the unified kernel lets materials pass through each other | the richest physics-coupled-reconstruction mechanism in the table — multi-material, multi-solver, scene-level (vs Embodied-Gaussians' single-solver PBD tracking loop) — but demonstrates *coupling capability*, not a readiness scorecard, and doesn't run D1's controlled PSNR-vs-readiness comparison |
| [[2411.12789\|Sim-GS]] | open-vocab 3DGS + MLLM zero-shot per-object material (density / Young's / Poisson) for MPM sim | physics-based 4D scene dynamics in **~2 min** on one GPU (vs 0.1–1.5 hr) | infers scene material *zero-shot* but doesn't score readiness-vs-fidelity; material is predicted, not validated against contact |
| [[2403.08321\|ManiGaussian]] | 3DGS world model carrying dynamics for manipulation | dynamics-carrying scene reconstructions | dynamics for action, but not a full readiness scorecard |
| [[2003.08515\|SAPIEN]] | articulated-object simulator (URDF reference) | the kinematic-executability bar | hand-authored assets, not reconstructed-for-readiness |
| [[2604.25459\|GS-Playground]] | high-throughput 3DGS sim (Sim2Real-A1 neighbor) | **90%** real SR | minimizes the transfer gap *given* an asset — not readiness itself |
| [[2511.04665\|Real-to-Sim-GS]] | 3DGS + soft-body digital twins (Sim2Real-B1 neighbor) | **r=0.915** sim-real correlation | reconstruction-for-transfer, not interaction-readiness as the target |
| [[2604.26509\|3D-Generation-for-Embodied-AI-Survey]] | the four readiness criteria | "simulation-readiness over visual fidelity" | the organizing frame — names criteria, prescribes no pipeline |
| [[2510.16732\|World-Models-for-Embodied-AI-Survey]] | reconstruction → geometry for world models | the D→C handoff | survey framing only |
| [[2602.10116\|SAGE-Scene]] | agentic text→scene generation with an Isaac-Sim Physics Critic validating stability + collision-free readiness in the loop | **99.9%** stability / **1.9%** collision (vs Holodeck 63.8% / 22.0%); physics critic alone cuts collisions 7.8%→**1.9%** | the readiness-*optimizing* pipeline H1 says doesn't exist — but generate-then-validate, not reconstruct-from-observation; closes the loop HoloScene bakes jointly via an explicit agentic critic, yet never runs D1's controlled PSNR-vs-readiness dual-objective on a matched scene |
| [[2608.11876\|D3D-GEN]] | text→simulator-ready world generation via a research-grounded constraint database (building codes, robot-clearance rules with provenance) + RAG scene-graph-to-floorplan + collision-checked asset placement, exported to Isaac Sim/Gazebo | 450 worlds at exact room-count fidelity; **54.56** objects / **9.19 m** spacing / **0.400** VQA-accuracy vs DiffuScene's **46.60**/lower spacing on 18 residential worlds | a second readiness-optimizing generation pipeline beside SAGE-Scene, but the readiness signal is *domain-constraint compliance* (codes, clearance) rather than a physics critic — evidence the readiness-optimizing space has more than one viable mechanism, still no controlled PSNR-vs-readiness dual-objective run against it |
| [[2601.03200\|3DGS-Digital-Twin]] | post-hoc 3DGS → watertight collision-mesh (visibility-aware semantic fusion + 3-stage cleaning + Alpha-Shapes meshing) → planning-ready geometry | Chamfer **0.0020** / F1 **0.9989** cleaned geometry, **90%** zero-shot real pick-place SR, **zero** collisions (Franka, 10 trials) | the post-hoc-meshing-for-planning path H5 pits against [[2605.26115\|TriSplat]]'s feed-forward sim-mesh — strong readiness from a fidelity 3DGS asset, but meshing is a downstream conversion, the speed/readiness cost H5 measures |
| [[2510.21307\|Physically-Executable-3DGS-Nav]] | reconstructed 3DGS → physically-executable scene via 3DGS-Mesh hybrid (original mesh = collision bodies for physics + 3DGS = photorealistic render) + SAGE-Bench (Continuous-Success-Ratio / Integrated-Collision-Penalty / Path-Smoothness on reconstructed scenes) | NaVILA **0.39** SR on SAGE-Bench (vs 0.54 on VLN-CE), up to **+34%** Oracle-SR generalization, 6.2 ms/frame render | embeds collision geometry *into* an existing fidelity 3DGS scene and scores act-in-ability on the reconstructed asset — the readiness-measured-separately-from-rendering slot (its metrics are collision/path-feasibility, not PSNR — validating H1's premise) — distinct from the cited [[2602.10116\|SAGE-Scene]] (different team's agentic text-to-scene *generation*); but navigation-oriented (collide-through, not grasp/articulate), an additive slot, and its SAGE-Bench rides with the method |
| [[2507.02861\|LiteReality]] | RGB-D scan → graphics-ready scene with object individuality, articulated geometry, PBR materials, physical properties, collision resolution | Chamfer **0.0986** retrieval, full-scene RMSE **0.2664** / SSIM 0.5818 on iPhone scans (vs Phone2Proc / Digital Cousin) | the graphics-pipeline-compatible recon that *outputs* articulated geometry + physical props as structure (D1's H2 target), but reports fidelity/material-recovery metrics — readiness is asserted, not contact-validated like [[2510.05560\|HoloScene]]'s stable-ratio |
| [[2605.20290\|TelePhysics]] | single-image → multi-object interactive scene (per-instance mesh + VLM material + decoupled RBD/MPM/PBD backend), unified world-frame anti-interpenetration | **0.00** penetration rate, **0.90** interaction-success rate, real-time preview at **15 FPS** (46 s init) | the multi-object whole-scene image-input sibling of [[2503.20746\|PhysGen3D]] (beyond single-MPM) — but optimizes physical-commonsense human-pref for video, not the controlled readiness-vs-fidelity per-criterion split [[2506.06440\|Vid2Sim]] also leaves unquantified |
| [[2503.20746\|PhysGen3D]] | single-image → interactive miniature world (3D reconstruction + MPM dynamics + physics-based rendering) | human-eval superiority over Pika / Kling / Gen-3 in physical realism, photorealism, semantic consistency (no hard readiness number) | the image-conditioned end of the recon-for-interaction spectrum the whole-scene rows leave to video input ([[2404.09833\|Video2Game]] / [[2510.05560\|HoloScene]]) — but single-object MPM and qualitative-only, no per-criterion readiness scorecard |

**Hypotheses & tests.** The FP bet — readiness, not fidelity, is the embodied target, and it is reachable from single video — decomposed.
1. **H1 — Fidelity-optimal scenes fail readiness criteria measurably more often (controlled, beyond HoloScene's co-product).**
   - *Prediction*: building the same scene optimizing (a) PSNR vs (b) [[2604.26509|3D-Generation-for-Embodied-AI-Survey]]'s four readiness criteria, the fidelity-optimal asset fails ≥1 readiness criterion substantially more often — the *controlled* per-criterion isolation [[2510.05560|HoloScene]] surfaces only as a Table-3 trade-off ablation and [[2505.17966|Single-View-Mesh-for-Robotics]] shows only as a one-sided probe.
   - *Test*: dual-objective reconstruction on a matched scene set; report per-criterion pass/fail rates against [[2505.17966|Single-View-Mesh-for-Robotics]]'s 5-desiderata protocol (Chamfer<2 mm / no-collision / stability<5° / occlusion / <2 s + grasp-transfer SR), the standardized per-criterion readiness scorecard the card otherwise treats as nonexistent.
   - *Row*: HoloScene / Single-View Mesh for Robotics / 3D Generation for Embodied AI Survey.
   - *Falsifier*: fidelity-optimal assets also pass readiness → fidelity and readiness aren't orthogonal.
2. **H2 — Single-video recovers rigid structure but not full-automatic articulation ([[2608.04842|RORA]] narrows the ceiling; the automatic, whole-scene residual stays open).**
   - *Prediction*: [[2404.09833|Video2Game]]/[[2510.05560|HoloScene]]-style single-video reconstruction recovers rigid-entity decomposition reliably but fails articulated structure (joints, DoF) for URDF/MJCF export without multi-view or interaction data — closable by porting the [[2603.14010|URDF-Anything+]] / [[2506.08334|iTACO]] / [[2509.17647|VideoArtGS]] articulation path into the scene pipeline. [[2608.04842|RORA]] narrows this ceiling directly: from a single *static*-configuration video (no motion, no multi-state capture) plus 42 seconds of human-guided joint confirmation, it recovers *chained multi-joint* articulation where fully-automatic VLM-prior ([[2603.14010|URDF-Anything+]]-class) and motion-based (ScrewSplat) baselines both catastrophically fail, so the open residual isn't "does single-video recover articulation at all" but whether it holds *without* the human-in-the-loop step, and at *whole-scene* (not single-object) scale.
   - *Test*: single-video vs multi-view kinematic recovery on articulated scenes; report URDF-exportability + joint-axis/type error on [[2604.05621|FunRec]]'s RealFun4D (the only scene-level articulated-readiness suite — 127 photorealistic sequences in 12 OmniGibson scenes + a real counterpart, URDF/USD-exportable, part-mIoU / joint-axis / pose metrics), with [[2608.04842|RORA]]'s fully-automatic-vs-HIL split (42s active interaction out of 7m23s total) as the baseline for how much of the gap closes without human guidance, and [[2603.19231|MonoArt]] establishing the single-image articulation ceiling on PartNet-Mobility (Chamfer **0.77**, type-accuracy **88.26%**).
   - *Row*: VideoArtGS / URDF-Anything+ / SAPIEN / RORA.
   - *Falsifier*: single-video recovers articulation directly *without* human guidance and at whole-scene scale → the multi-view/HIL requirement is unnecessary entirely.
3. **H3 — A readiness threshold for physical parameters exists below sim-accurate fidelity.**
   - *Prediction*: a policy trained in the reconstructed asset acts correctly once physical parameters (mass, friction, restitution) cross a *sufficiency* threshold well below sim-accurate fidelity — readiness is a threshold, not a fidelity race.
   - *Test*: degrade physical-parameter fidelity; locate the SR-preserving threshold.
   - *Row*: Vid2Sim / Embodied Gaussians.
   - *Falsifier*: SR scales smoothly with parameter fidelity (no threshold) → readiness reduces to the transfer-gap question Sim2Real owns.
4. **H4 — A readiness-asset feeds A1 and C1 end-to-end.**
   - *Prediction*: a D1 interaction-ready asset directly feeds A1's point-cloud head (act-on geometry) and C1's occupancy WM (rollout-over geometry), and the end-to-end D→A and D→C chains match hand-built-asset baselines.
   - *Test*: drive A1/C1 from a D1 asset vs a hand-built asset; compare downstream SR.
   - *Row*: Video2Game / TriSplat.
   - *Falsifier*: the reconstructed asset underperforms hand-built downstream → readiness isn't yet sufficient for the consumer.
5. **H5 — Simulator-format output beats post-hoc meshing for readiness.**
   - *Prediction*: [[2605.26115|TriSplat]]'s feed-forward simulator-format mesh passes the kinematic-executability + format-compat criteria more reliably (and far faster) than meshing a fidelity-optimized 3DGS scene post-hoc.
   - *Test*: feed-forward sim-mesh vs post-hoc-meshed 3DGS on the format-compat + executability criteria.
   - *Row*: TriSplat / Video2Game.
   - *Falsifier*: post-hoc meshing matches feed-forward on readiness → simulator-format output is a speed win only, not a readiness one.

> [!warning] Risks
> - **Interaction-readiness lacks a standard benchmark** — readiness is a checklist, not a leaderboard number. → Adopt [[2604.26509|3D-Generation-for-Embodied-AI-Survey]]'s four criteria as the explicit scorecard (H1) and report per-criterion pass/fail, not a single fidelity number.
> - **Single-video may not recover articulation/physics** for complex scenes. → H2 bounds what single-video reconstruction can recover; fall back to multi-view or interaction data where articulation needs it, and state the boundary.
> - **Boundary blur with [[Sim2Real|Sim2Real]]-A1/B1.** → Keep D1 pinned to *readiness as the optimization target* (H1, H3) and explicitly route the *transfer-gap evaluation* to [[2604.25459|GS-Playground]] / [[2511.04665|Real-to-Sim-GS]].

### D2 — Object-Level Physical-Asset Generation: One Object That Carries Its Own Physics

| | |
|---|---|
| **Cluster** | D — Reconstruction for Embodied Perception |
| **Thesis** | The reusable unit of an interactive environment is the *object*, not the scene — and an object is only embodied-useful if it carries its own material, mass, and kinematic structure, not just a watertight mesh. The field's old defense — that geometry and physics must be separable stages, generate appearance first and annotate physics later — is already refuted: [[2605.21572\|PhysX-Omni]] ships one generator spanning rigid, deformable, and articulated classes, and [[2504.12684\|SOPHY]] already shows joint generation beats staged annotation on its own object set. What's still open is whether that joint-over-staged advantage survives leaving SOPHY's own class mix. The bet is in First-principles below. |
| **Anchor papers** | [[2604.26509\|3D-Generation-for-Embodied-AI-Survey]] (survey), [[2506.20134\|3D-World-Models-Survey]] (survey), [[2605.21572\|PhysX-Omni]] (method), [[2605.05163\|PhysForge]] (method), [[2605.30347\|NeuROK]] (method), [[2503.17973\|PhysTwin]] (method), [[2311.12198\|PhysGaussian]] (method), [[2303.05512\|PAC-NeRF]] (method) |
| **Key targets** | [[2504.12684\|SOPHY]]'s own-class joint-vs-staged gap (material-class **+>20%**, Sim-CD **5×** lower) replicated on a matched cross-class object set SOPHY never tested; Per-object kinematic score ≥0.92 ([[2605.21572\|PhysX-Omni]] PhysXVerse); Chamfer-L1 ≤0.028 / IoU ≥0.764 ([[2605.30347\|NeuROK]]); deformable-twin Hand Chamfer ≤7.17 mm ([[2605.09538\|PhysHanDI]]); PhysX-Bench readiness across material / affordance / kinematics; [[2606.16202\|EgoPhys]] category-generalizable deformable Chamfer ≤0.015 (vs PhysTwin 0.024), zero-shot on 11 held-out deformables, 77.6% real sim-to-real config-error reduction |

**Why it matters.**
- **The gap**: D1 builds a whole scene you can act in, but the scene's *reusable parts* are objects — and a fidelity-first object is a hollow prop, a watertight surface with no mass, friction, or joints. [[2604.26509|3D-Generation-for-Embodied-AI-Survey]] names the sharpest version at the object level: the "trade-off between geometric quality and physical validity" and "scarcity of physical annotations," with *deformable assets* called out as unsolved.
- **Today's answers**: the unified generator + scorecard is *already built*, not something D2 needs to propose — the card's own anchor [[2605.21572|PhysX-Omni]] is titled for unified rigid+deformable+articulated generation and ships PhysX-Bench (six readiness attributes), and [[2504.12684|SOPHY]] already shows joint beats geometry-first (material-class +>20%, sim-Chamfer **5×** lower) on its own dataset, a 12-category, deformable-leaning slice of 3DCoMPaT200, and skips separate rigid-material modeling because rigid nearly suffices. The rigid+articulated half is owned in one pass by [[2511.13648|PhysX-Anything]] (abs-scale error **43.44→0.30**, MuJoCo-importable) and [[2511.21887|UniArt]] (>**85%** retention on unseen categories vs <60% baselines). [[2605.05163|PhysForge]] / [[2605.30347|NeuROK]] add kinematics/energy-conservation; [[2503.17973|PhysTwin]] / [[2605.09538|PhysHanDI]] deformable twins. None runs a *matched-object joint-vs-staged* head-to-head decomposed per-class on one scorecard — SOPHY's own gap is aggregate, within its own class mix, never tested against objects outside it.
- **The opening**: [[2406.04338|Physics3D]] recovers material parameterization from video diffusion with *no annotation*, and [[2412.17804|GausSim]] runs a per-object elastic rollout (L2 1.85 vs 11.32) with mass/momentum conservation — proof physics-consistent generation is buildable without a physics-annotation step.

**First-principles framing.**
- **First principle**: an object's embodied value is a *joint* (geometry, material, kinematics) — the physics is not a separable post-hoc label but a property the geometry must be generated *consistent with* (a mesh and its mass/friction/joints co-determine how it behaves under contact). Splitting them discards the consistency a single-pass generator could enforce; [[2311.12198|PhysGaussian]] shows continuum mechanics baked into the Gaussians themselves is the consistent form.
- **Assumption being challenged**: the geometry-first convention (NeRF/3DGS-then-annotate) is *already refuted* — [[2605.21572|PhysX-Omni]], [[2504.12684|SOPHY]], and [[2511.21887|UniArt]] each generate physics-with-geometry in one pass and generalize to unseen categories, and PhysX-Omni's single VLM-based framework already spans rigid, deformable, *and* articulated in one generator (kinematic **0.9185** on PhysXVerse). So neither "build the unified generator" nor "does joint beat staged at all" is open — [[2504.12684|SOPHY]] settled the latter (material-class **+>20%**, Sim-CD **5×** lower) inside its own 12-category, deformable-leaning 3DCoMPaT200 slice. What D2 attacks is the assumption that a within-class gain, measured on the classes SOPHY happened to train and evaluate on, generalizes to a *matched object set spanning classes SOPHY never decomposed for* — nobody has run that controlled comparison.
- **The bet**: the sole surviving falsifier — not the generator [[2605.21572|PhysX-Omni]] already built, not the within-class advantage [[2504.12684|SOPHY]] already measured — is whether SOPHY's joint-over-staged gap *replicates decomposed per class* on a matched rigid/deformable/articulated object set: the advantage concentrates on deformable + articulated (≈0 on rigid props), as SOPHY's own choice to skip separate rigid-material modeling already hints, rather than holding uniformly or collapsing outside SOPHY's trained class mix.

**Related research papers.** One table on the axis the direction turns on — *how per-object physics is recovered* (unified-joint-generation / generative-kinematics / deformable-twin-from-video / hand-object-twin / baked-into-Gaussians / elastic-simulator / from-video-diffusion) — with `Key result` and what each leaves missing. The contrast is joint generation (physics-with-geometry) vs staged annotation.

| System | Per-object physics recovery | Key result | What's missing |
|---|---|---|---|
| [[2605.21572\|PhysX-Omni]] | unified generator: rigid + deformable + articulated, physics baked | kinematic **0.9185** PhysXVerse, abs-scale **2.79** vs >298, deployed in policy learning | the card's own anchor already ships the unified generator + PhysX-Bench scorecard — but never the matched joint-vs-staged controlled comparison; deformable readiness undertested |
| [[2504.12684\|SOPHY]] | joint shape + material generation (deformable-focused) | material-class accuracy **+>20%**, deformation Sim-CD **5×** lower vs geometry-first baseline, across 12 3DCoMPaT200 categories | *already run by SOPHY* — dropped from this card's Hypotheses & tests as redundant: joint beats geometry-first at all (the aggregate numbers above) and the advantage concentrates away from rigid (SOPHY skips separate rigid-material modeling as nearly-sufficient, a design choice rather than a decomposed ablation). What's genuinely missing is whether that gap, measured in aggregate inside SOPHY's own 12-category mix, *replicates decomposed per class* on a matched object set outside it — no cross-class scorecard or energy-predicts-OOD test |
| [[2511.13648\|PhysX-Anything]] | single-image → physics-grounded asset, MuJoCo-importable | abs-scale error **43.44→0.30**, VLM geometry/kinematics **0.94**, contact-rich policy learning | rigid+articulated one pass with URDF, but doesn't isolate joint-vs-staged or run the per-class deformable comparison |
| [[2608.08053\|Structured Physical CoT]] (PhysX-CoT) | single-image → an ordered, machine-parseable Structured Physical CoT (part decomposition → boxes → relations → primitives → surface cues) as SFT target *and* GRPO process-reward target, with position-factorized local geometry (box = placement, RLE local code = shape) | beats [[2511.13648\|PhysX-Anything]] under a unified retrained-baseline protocol: CD **0.041** vs **0.095**, kinematic **0.73** vs **0.53**, scale error **6.57%** vs **12.43%**; oracle CoT states lift kinematic to **0.92**; **58%** fewer geometry tokens, **30%** faster | makes the *reasoning process itself* the generation target (CoT-aligned GRPO with process rewards) rather than a single forward pass like [[2605.21572\|PhysX-Omni]]/PhysX-Anything — the oracle-state ablation (0.73→0.92) is a construction-accuracy-is-the-bottleneck finding structurally parallel to B1's H5, but on asset generation not reasoning; still doesn't run D2's matched-object cross-class joint-vs-staged split |
| [[2507.12465\|PhysX-3D]] | physics-grounded 3D generation (PhysXNet, 6M assets) | PSNR **24.53**, abs-scale error **7.24** vs 13.21, material **13.01** vs 8.63, affordance **11.30** vs 7.23 | the data+generator scale, but reports aggregate fidelity+physics, not a controlled joint-vs-staged per-class readiness split |
| [[2511.21887\|UniArt]] | unified articulated-object generation + URDF | PSNR **28.52** rest / 23.77 articulated, **>85%** retention on unseen categories (vs <60%) | owns the articulated half with strong OOD, but articulated-only — not the cross-class joint-vs-staged falsifier |
| [[2605.05163\|PhysForge]] | two-stage VLM physical-planning + diffusion realizing geometry **and** continuous kinematic params (KVI) | Joint-Axis-Err-5 **0.101** (SOTA), assets simulation-ready in robotic sims | strong on articulated kinematics, but physics-property breadth (material/deformable) less validated than PhysX-Omni |
| [[2605.30347\|NeuROK]] | generative 4D neural object kinematics, energy-conserving | Chamfer-L1 **0.028**, IoU **0.764**, **83.33%/81.43%** user-pref on unseen categories | kinematics-consistent, but material parameters not the focus |
| [[2503.17973\|PhysTwin]] | physics-informed deformable twin from video | real-time sim + robotic motion planning | per-object deformable, single-instance — not a category generator |
| [[2608.06164\|BendTwin]] | axial-only [[2503.17973\|PhysTwin]] spring-mass extended with explicit bending stiffness + viscous bending damping over local node triplets (O(N) construction) | Chamfer **-19.0%** (reconstruction) / **-16.8%** (future prediction) vs axial-only baseline; at sparsity r=0.05, **-26.5%/-18.3%**; surface-only nodes match or beat axial-only-with-interior-nodes | a direct mechanical-fidelity upgrade to the cluster's own deformable-twin anchor, showing the axial-only formulation is under-constrained exactly where sparsity or surface-only discretization stresses it most: strengthens D2's "physics-consistent, not staged" case at the *twin* level, but single-object, no cross-class generator or energy-predicts-OOD test |
| [[2607.17132\|BoxTwin]] | per-hinge constitutive twin from video: joint-space Lagrangian dynamics + a differentiable elastic-torque map + yield/flow rule shifting rest angle + a damage law attenuating both elastic and plastic response with accumulated slip | tracks real joint angles across long folding + dual-arm-Aloha-replay sequences, holding under accumulated hysteresis and wear over repeated actuation (qualitative, no reported quantitative error) | a distinct physical phenomenon from every other deformable-twin row — plastic deformation *and* damage/wear accumulation, not [[2503.17973\|PhysTwin]]/[[2608.06164\|BendTwin]]'s elastic spring-mass — sharpening the deformable half of D2's readiness scope, but per-object (not a category generator) and qualitative-only evaluation |
| [[2607.13586\|UniPhysGen]] | SAM part-decomposition + PartField + Hungarian matching + Apriori-mined parts → LVLM articulation selection over geometry-constrained axis-pivot candidates → 4-level simulation-verified annotation, trained via physical-semantic-alignment pretraining + SO(3) augmentation | UniPhys-Bench: **89.96%** joint-type accuracy, **0.099** pivot error (beats URDFormer/Real2Code/PARTICULATE); material **77.92%** vs NeRF2Physics **53.09%**, friction MAE **0.062** vs **0.142** | a comparable-tier unified pipeline to [[2605.21572\|PhysX-Omni]]/SOPHY with its *own* benchmark (UniPhys-Bench) and a simulation-driven verification step none of the table's other rows run — evidence the unified-joint-generation space has independently converged on the same architecture from a different data pipeline, but still no cross-class matched-object joint-vs-staged split against D2's own H1 |
| [[2605.09538\|PhysHanDI]] | hand–deformable-object physics twin from sparse RGB-D | Hand Chamfer **7.57→7.17 mm**, ~2×/>7× lower spring RRD than PhysTwin | contact-rich deformable case, but hand-object-specific |
| [[2311.12198\|PhysGaussian]] | continuum mechanics baked into Gaussians (elastic/plastic/fracture) | physical parameterization in the 3DGS object itself | per-object physics, but not a generator across categories |
| [[2412.11258\|GaussianProperty]] | training-free SAM part-seg + LMM material reasoning, voted onto 3D Gaussians | **55.83%** mIoU material seg (vs Nerf2Physics 25.59%), **100%** real adaptive-grasp success | tags material onto a *given* mesh — recovery, not generation; no kinematic structure |
| [[2510.22975\|VoMP]] | feed-forward Geometry-Transformer + MatVAE predicting per-*voxel* volumetric mechanical-property fields (Young's modulus, Poisson, density) onto *given* geometry | ALRE **0.0409** Young's (vs NeRF2Physics **0.1346**) on a 166-object / 4.9M-voxel benchmark, **5–100×** faster than optimization, infers internal structure | the *strongest staged-annotation* baseline H1's joint-vs-staged falsifier must beat — strictly stronger and volumetric where [[2412.11258\|GaussianProperty]] is surface-level (no internal param) — and a per-object material-error yardstick (ALRE) for H2's consistency-vs-fidelity regression / H3's material-fidelity ablation; but annotate-onto-static-geometry, distinct from PAC-NeRF/UniPhy/Physics3D which infer material from observed *motion*, so it cannot enforce generation-time consistency |
| [[2412.17804\|GausSim]] | continuum elastic Gaussian simulator | L2 **1.85** vs 11.32, **95%** fewer computations, 0.13 s/frame | elastic rollout, not asset generation |
| [[2406.04338\|Physics3D]] | material parameterization from video diffusion (no annotation) | PSNR **14.72**, material recovered annotation-free | the recover-physics-from-appearance variant — fidelity-limited |
| [[2505.16971\|UniPhy]] | unified neural constitutive model — inverse material inference from observed 3D motion | elastic reconstruction error **5.2e-6** vs **2.4e-4** (NCLaw), material-type-agnostic | infers material *post-hoc* from motion — needs an observed trajectory, not a one-pass generator |
| [[2604.26509\|3D-Generation-for-Embodied-AI-Survey]] | the geometry-vs-physics trade-off + deformable gap | names the bottleneck | the organizing frame — prescribes no generator |
| [[2003.08515\|SAPIEN]] | articulated-object simulator (URDF reference) | the kinematic-executability bar a generated object must hit | hand-authored, not generated |
| [[2508.13911\|PhysGM]] | feed-forward joint generator: single image → 3DGS geometry + MPM physical properties in one optimization-free pass | sub-minute 4D generation, **42.8%** User Preference Rate (vs OmniPhysGS/DreamerPhysics), LPIPS **0.039** / SSIM **0.953** GSO recon | the feed-forward sibling of [[2605.21572\|PhysX-Omni]] — closes the card's named gap ([[2311.12198\|PhysGaussian]]/[[2412.17804\|GausSim]] are per-object *simulators*, not generators) — but never runs the matched joint-vs-staged head-to-head, and material-class breadth limited to MPM (no articulated/URDF) |
| [[2606.16202\|EgoPhys]] | category-generalizing deformable generator: material-prototype codebook → dense stiffness for unseen objects from single egocentric RGB-only video | Chamfer **0.015** vs PhysTwin **0.024**, zero-shot on **11** held-out deformables, real xArm6 sim-to-real with **77.6%** config-error reduction | turns [[2503.17973\|PhysTwin]]/[[2605.09538\|PhysHanDI]]'s per-instance deformable twin into a *category prior*, closing the deformable-generalization question outright — but the codebook mechanism is deformable-only: whether it transfers to rigid/articulated classes, and where the gap-closing margin concentrates once it does, is untested (H4/H5) |
| [[2501.18982\|OmniPhysGS]] | unified-across-material-class: per-particle learnable constitutive networks (ensemble of 12 expert models) baked into Gaussians | **+3–16%** CLIPSIM single-object across elastic/viscoelastic/plastic/fluid, **+4%** multi-object, **75%** less MPM memory | adds the unified-material-class axis the table lacks (distinct from [[2311.12198\|PhysGaussian]] single-MPM, [[2412.17804\|GausSim]] elastic-only) — but text-to-dynamics generation, no per-object readiness scorecard or kinematic structure |
| [[2303.05512\|PAC-NeRF]] | foundational joint geometry **and** material recovery: geometry-agnostic system ID (Young's modulus, density, plasticity) from video, no prior geometry | best in **14/17** physical-property categories vs SOTA, recovers geometry + parameters across 9 material classes | the canonical joint-vs-staged first-principle in recovery form (NeRF-side complement to [[2311.12198\|PhysGaussian]]) — but recovers from *observed multi-view video*, not a one-pass generator, and surface geometry degrades under few views |
| [[2311.13099\|PIE-NeRF]] | NeRF-side baked physics: meshless Q-GMLS elastodynamics with material baked into the NeRF, no mesh/voxel proxy | outperforms [[2303.05512\|PAC-NeRF]] render quality under large deformation, up to **2 orders** faster interactive simulation | the NeRF-substrate analog of [[2311.12198\|PhysGaussian]]'s baked-in continuum mechanics (Gaussian-side only before) — but bakes physics into a *given* NeRF, an elastodynamics engine not a generator, and elastic-only |
| [[2505.10755\|Infinigen-Articulated]] | procedural articulated-asset generation: Blender Geometry-Node joints with full pivot/axis metadata, exported URDF/USD/MJCF | **2.86×** RL success-rate gain vs PartNet-Mobility, **18/30** vs **3/30** zero-shot sim-to-real door-opening, Lamp-Switch mAP **11.45→17.44** | the procedural (non-neural) generation route the all-neural table lacks ([[2605.21572\|PhysX-Omni]]/[[2511.21887\|UniArt]]/[[2605.05163\|PhysForge]]) — a hand-engineered baseline for H1's articulated leg — but no learned joint shape/material co-generation, articulated-class only |

**Hypotheses & tests.** The FP bet narrows to one sole surviving falsifier: does SOPHY's joint-over-staged readiness gap replicate decomposed per class on a matched object set spanning classes SOPHY didn't decompose for. One further sub-question this card originally listed as its own hypothesis (the joint-generation advantage concentrating on deformable + articulated because SOPHY *skips* rigid as nearly-sufficient) is *already run by SOPHY* as a design choice, not a controlled ablation, and has moved into the Related-table's SOPHY row as evidence rather than an open hypothesis — see that row for the numbers. The remaining hypotheses below extend the direction's *other* first-principles claims (physical-consistency-as-generalization-signal, downstream transfer, and whether the deformable-category-prior [[2606.16202|EgoPhys]] built generalizes past deformables), decomposed.
1. **H1 — SOPHY's joint-over-staged advantage replicates on a matched object set spanning classes it never decomposed for.**
   - *Prediction*: generating the same matched object set (a) geometry-first then annotate vs (b) jointly via a [[2605.21572|PhysX-Omni]]-style unified head, the staged pipeline fails the kinematic/material readiness criterion substantially more often, decomposed per class — [[2504.12684|SOPHY]] already showed the *aggregate* gap (material +>20%, Sim-CD 5×) inside its own 12-category 3DCoMPaT200 mix; the open test is whether it holds class-by-class on rigid + deformable + articulated objects outside that mix, with the advantage concentrated on deformable + articulated (≈0 on rigid, as SOPHY's own choice to skip rigid-material modeling already hints) rather than spread uniformly.
   - *Test*: dual-pipeline generation; per-criterion readiness ([[2604.26509|3D-Generation-for-Embodied-AI-Survey]]'s four) on a matched object set spanning all three classes, scored per class not aggregate. [[2608.08053|Structured Physical CoT]] adds a *within-model* construction-accuracy data point relevant to the same generalization question: its oracle-CoT-state ablation (kinematic 0.73→0.92) shows how much of a unified joint generator's own readiness gap is bottlenecked by the *reasoning process*, not the architecture — a variable H1's dual-pipeline test should hold fixed when comparing joint vs staged.
   - *Row*: PhysX-Omni / SOPHY / PhysX-Anything / PhysHanDI / Structured Physical CoT.
   - *Falsifier*: staged annotation passes as often as joint outside deformable, or the advantage is uniform across classes → SOPHY's aggregate gap doesn't decompose the way the first principle predicts, and physics-with-geometry isn't specifically load-bearing on the hard cases.
2. **H2 — Energy-conservation predicts out-of-category readiness better than fidelity.**
   - *Prediction*: enforcing a physical-consistency constraint (energy, momentum, [[2412.17804|GausSim]]'s mass conservation) at generation time predicts out-of-category readiness better than fidelity (PSNR/Chamfer) alone — mirroring [[2605.30347|NeuROK]]'s energy-conserving generalization.
   - *Test*: regress out-of-category readiness on a consistency metric vs a fidelity metric — using [[2503.21745|3DGen-Bench]]'s standardized human-preference fidelity yardstick (3DGen-Score, **0.725/0.767** human-alignment for text/image-to-3D) as the *fidelity arm* the consistency-vs-fidelity comparison structurally requires (cited only as the fidelity comparator — never as a physical-readiness backer, which would invert the bet's contrast).
   - *Row*: NeuROK / GausSim.
   - *Falsifier*: fidelity predicts out-of-category readiness as well → physical consistency isn't the generalization signal.
3. **H3 — A generated object transfers to a sim-trained policy via material *or* kinematics.**
   - *Prediction*: a D2 physics-jointly-generated object dropped into a D1 scene lets a sim-trained policy act correctly, and the binding constraint is identifiable as either per-object material fidelity (mass/friction) or kinematic structure — not both equally.
   - *Test*: ablate material-fidelity vs kinematic-structure on the generated object; report which gates transfer.
   - *Row*: PhysX-Omni / NeuROK.
   - *Falsifier*: neither ablation degrades transfer → the object's physics isn't the transfer constraint.
4. **H4 — EgoPhys's category-generalizing prior mechanism transfers to rigid/articulated classes, not just deformables.**
   - *Prediction*: the material-prototype-codebook mechanism that lets [[2606.16202|EgoPhys]] infer dense stiffness for unseen deformable objects from a single egocentric RGB-only video generalizes to rigid and articulated classes once the codebook target is swapped from material stiffness to kinematic/joint parameters — a category-level rigid/kinematic-property prior is learnable from the same single-video-observation setup, not just a deformable-material one.
   - *Test*: adapt EgoPhys's codebook-prior mechanism to a rigid+articulated object set ([[2605.21572|PhysX-Omni]]/PhysXVerse classes), scoring zero-shot readiness on held-out instances the way EgoPhys scores held-out deformables (Chamfer + PhysX-Bench's kinematic-executability criterion), against a [[2503.17973|PhysTwin]]/[[2605.09538|PhysHanDI]]-style per-instance-twin baseline with no category prior.
   - *Row*: EgoPhys / PhysX-Omni / PhysX-Anything.
   - *Falsifier*: the codebook mechanism fails to transfer — zero-shot rigid/articulated readiness collapses to the per-instance-twin baseline → the category-generalizing prior is deformable-specific, not a general D2 mechanism.
5. **H5 — The gap-closing margin concentrates on articulated, not rigid, once the category prior generalizes.**
   - *Prediction*: if the prior transfers (H4), the readiness-gap-closing margin over a per-instance twin is largest on articulated objects and smallest on rigid ones, because kinematic structure — not material stiffness — is the binding constraint for articulated readiness, mirroring SOPHY's own choice to skip rigid-material modeling as nearly-sufficient (H1's rigid-≈0 finding).
   - *Test*: decompose H4's cross-class zero-shot readiness gain by class (rigid / articulated / deformable), reading the margin against [[2604.26509|3D-Generation-for-Embodied-AI-Survey]]'s four readiness criteria, per class.
   - *Row*: EgoPhys / UniArt / PhysX-Anything.
   - *Falsifier*: the margin is flat across classes, or largest on rigid → kinematic structure isn't the binding constraint the first principle predicts, and the category-prior's value is uniform rather than concentrated where structure (not material) gates readiness.

> [!warning] Risks
> - **Per-object readiness has no shared leaderboard across categories** — PhysX-Bench is rigid/articulated-leaning, PhysTwin/PhysHanDI are deformable-specific. → Adopt [[2604.26509|3D-Generation-for-Embodied-AI-Survey]]'s four criteria as the common scorecard (H1) and report per-class (rigid/deformable/articulated) pass/fail, not one aggregate.
> - **Physical parameters from a single video / sparse RGB-D may be under-determined** (mass/friction unobservable from kinematics alone). → H2's physical-consistency constraints (energy/momentum) regularize the under-determined parameters; bound the claim to where the constraint identifies them.
> - **Boundary blur with D1** if "object" and "scene" leak. → Pin D2 to the *single-object* generation unit (H1, H3) and D1 to *whole-scene* environments; a D2 object *populating* a D1 scene is the intended composition (H3), not an overlap.

---

## Cross-Cutting Themes

> [!tip] Geometry Is What the Task Keeps Fixed; Appearance Is Noise
> A1, A3, B2, C1, and C2 all make the same move: build the representation around the geometry the *task* keeps fixed (where things are, how they persist in 3D), not the appearance the *rendering* adds. The bets are independent but converge on one falsifiable claim — A1 predicts the geometry-vs-RGB gap *widens* under appearance shift; B2 predicts the 4D-*consistency* constraint (not raw 3D) drives OOD robustness; C2 predicts cross-view *consistency* (not RGB fidelity) drives the action gain — provisionally ≈5× in the anchor's own unreplicated eval, pending the RoboWM-Bench replication check. Three directions, one claim: the geometric channel, not the appearance channel, carries the action-relevant signal. This is the Hinton tenet that the brain plans in world coordinates, not image coordinates, turned into a measurable bet — and the diagnostics ([[2605.30161|Why-Far-Looks-Up]]'s shortcut gap, [[2604.24300|ReVSI]]'s hallucination) show the field's RGB default is exactly the failure mode.

> [!tip] The RGB-Token Tax Is Paid at Every Layer of the Stack
> The representation-supervision bottleneck [[2606.03943|PointAction]] names isn't local to the policy — it shows up at every layer, which is why A1/A3, B1/B2, C1/C2, and D1/D2 *stack* instead of compete. A1/A3 pay the tax at the *action head*; B1/B2 at the *cognition layer* (ungrounded language over RGB); C1/C2 at the *world-model substrate* (latent/pixel rollouts that re-parse geometry); D1/D2 at the *asset* (fidelity-optimized scenes and objects that aren't interaction-ready). Each cluster removes the tax at its layer, and the gains *compound down the stack*: a D2 object populates a D1 scene, which feeds A1's geometric head and C1's occupancy WM; a B1 scene-graph feeds A1's action head; B2's consistency is the temporal half of B1's spatial grounding. The whole is one geometric pipeline, not five isolated tricks.

> [!tip] Cheap Geometry vs Full Geometry — A Frontier, Not a Binary
> A3, B2, and C1 each sit at the *cheap* end of a cost/capability frontier whose expensive end a sibling owns. A3's single-view depth-token bridge is the cheap version of A1's full point branch (≥80% of the gain at side-channel cost); B2's implicit 4D-consistency is the cheap version of C2's explicit pointmap generation (same accuracy, 2.31× faster, no frame generation); C1's externally-readable occupancy is the long-horizon-cheap complement to X-WAM's per-frame-expensive latent-4D fidelity (now charted within the same card). The non-consensus reading: the field frames these as either/or (RGB vs 3D, implicit vs explicit, latent vs occupancy), but each pair is a *frontier* whose operating point is task-conditional — cheap geometry for transit/appearance-bound segments, full geometry for contact/spatial-bound ones. The research question isn't "which substrate wins" but "where is the crossover" — measured in A1-H1, A3-H1, B2-H4, C1-H1.

> [!tip] The Unpriced Geometry-Source / Sensing Tax Is the Hidden Variable Behind Every Bet
> Across A1, A2, A3, B1, C1, C2, and C3 the same sixth-perspective gap recurs: every direction assumes geometry arrives *clean and free*, yet it never does. Sensed geometry needs a depth/point/multi-view sensor most real cells lack; *predicted* geometry (the doc's universal escape hatch — predicted pointmaps, derived occupancy GT, monocular-depth bridges) is a **second large network** whose params, latency, and error must be charged to the bet, and whose error is worst exactly on the contact-rich / textureless / specular / occluded tail where every cluster's Finding-1 says geometry matters most. The doc's recommendations (externalize the geometry, keep the explicit point head, bridge depth in) are therefore made on a *zero-sensing-tax* axis — and several may **invert** once the depth source is priced: A1's explicit-head-beats-latent (H7), A3's deployable-depth-channel-beats-train-time-only (H6), C1's geometry-online-beats-latent claims (both the occupancy-vs-latent-4D bet and the latent-4D frontier now charted alongside it) all assume free geometry against latents (VLA-JEPA, Kairos-class) that pay *no* geometric-supervision tax and can drink open-world video. Every OOD/horizon-leaning claim must therefore add a **benchmark-validity check** (run the contest under *realistic estimated-geometry noise*, not clean rendered depth, and on suites that separate geometric from appearance drift), or the measured advantage is a clean-geometry artifact. The sensing tax, not the substrate, may be what decides the durable bet.

> [!tip] Explicit-External ↔ Latent-Internal Is the Doc's Deepest Axis (and the Real C-vs-B Line)
> The deepest split in the doc isn't 2D-vs-3D but *who reads the geometry*, and it cuts across clusters. B1 and B2 keep it *internal* — a scene-graph or consistency-attention the action head consumes end-to-end (cheaper, less inspectable). C1, C2, and D1 make it *external* — occupancy a third-party planner reads (C1), pointmaps a tracker reads for 6-DoF (C2), assets a simulator loads (D1). Externality buys debuggability, composability, and tool-reuse (C1-H2, C2-H4) at the cost of a decoder/extractor step. This is also Cluster C's *internal* organizing principle: the latent-4D comparator ([[2604.26694|X-WAM]], folded into C1) keeps its 4D state *internal* (decoded for imagination), while C1's own occupancy substrate and C2 keep geometry *external*, and C3 makes whichever persist — the split now lives *inside C1's card*, not across the [[WAM|WAM]] boundary. The same axis cleanly separates B2 (implicit, internal) from C2 (explicit, external) on the overlapping 4D-consistency idea.

---

## Benchmark Gaps

| Gap | Direction | Existing closest |
|---|---|---|
| No benchmark isolates *cross-embodiment appearance-shift* SR to test the geometry-vs-RGB claim, nor scores *graded cross-object/articulation generalization* — current suites mix appearance and geometry shift | A1 | [[2606.03943\|PointAction]] xArm7 zero-shot (43.0%) conflates embodiment + appearance + geometry in one number; the gap is now *narrowed via single-axis benchmarks* — [[2505.14986\|AnyBody]] isolates the embodiment axis (18 morphologies, interpolation/extrapolation/composition splits, reach/push only) and [[2509.18953\|Eva-VLA]] the real-world appearance axis (graded illumination + 3D-transform + patches, per-variation failure rates), but no single suite does both on contact-rich tasks. A1's geometry-bound generalization is otherwise anchored to LIBERO-Plus perturbation + RoboCasa (appearance/viewpoint shift, not object structure); [[2410.01345\|GemBench]]'s 4-level progressive suite (novel rigid → articulated → long-horizon) is the missing protocol on which a native point head's advantage over RGB should widen monotonically with object-structure novelty |
| No manipulation benchmark measures *rollout-horizon-to-geometric-divergence* for forward models — SR is reported at fixed horizon, hiding stability | A2 | [[2604.19092\|RoboWM-Bench]] scores WM→executable-action→step+final SR (discriminative beyond fixed-horizon, but SR not geometric divergence), [[2604.22152\|dWorldEval]] measures pixel drift over a 20-step rollout (LPIPS, round-trip consistency), and [[2510.18135\|World-in-World]] standardizes closed-loop WM utility — but none scores *geometric* divergence at sub-cm voxel resolution (the only occupancy-forecasting suites — UniOcc driving, restructured OccWorld-ScanNet inside [[2505.05512\|Occupancy-World-Model]] — have no tabletop analog), the residual A2 fills |
| No benchmark reports the *recovery-fraction* of a cheap depth-bridge vs a full-3D branch on a common backbone — gains are vs weak RGB baselines, not the full-3D ceiling | A3 | [[2306.03310\|LIBERO]]-Spatial/long-horizon is the named SR substrate, but no protocol plots recovery-fraction vs [[2508.09071\|GeoVLA]]'s full-3D ceiling (97.7%) on one backbone. The field now offers many drop-at-inference geometry-distillation points (Spatial Forcing single-layer, [[2602.17951\|ROCKET]] multi-layer at ~4% compute, [[2604.21914\|VistaBot]] view-normalize, [[2512.00903\|SwiftVLA]] mask-and-reconstruct, [[2606.14048\|WAM4D]] register tokens) and explicit-channel forms ([[2510.14836\|QDepth-VLA]] tokens, [[2603.09079\|GST]] Gaussian tokens, [[2603.12772\|PVI]] residual plug-in, [[2602.10698\|AugVLA]] point features), yet none plots the recovery-fraction-vs-added-params knee against GeoVLA's full-3D ceiling on a common backbone (H1); the alignment-preservation half (H4) has only [[2505.05456\|SITE]]'s spatial-VQA→SR correlation (0.902), which links alignment to SR but not the frontier — both the recovery-fraction knee and the VQA-probe alignment-drift remain genuine gaps |
| No benchmark splits the human-model spatial gap into *reasoning* vs *perception* failure, nor isolates *genuine metric 3D* from *shortcut* answering | B1 | [[2601.13304\|CausalSpatial]] (GPT-5 54.17% vs human 84.49%) measures the gap; [[2605.29074\|Embodied3DBench]], [[2605.30161\|Why-Far-Looks-Up]] (36.9 pp), [[2605.30557\|SpatialUncertain]] diagnose the shortcut; for the metric-vs-shortcut isolation, [[2412.07825\|3DSRBench]] (12 relation types, FlipEval/CircularEval controls, 6D-viewpoint OOD) and [[2506.03135\|OmniSpatial]] (50 subcategories + built-in PointGraph scene-graph baseline) are the closest standardized substrates; the H5 perception-decoupled probe is now partly instrumented by [[2603.15386\|RieMind]] (a ground-truth 3D scene-graph queried by a deterministic toolbox, isolating reasoning-given-a-correct-graph as a clean upper bound) — but it injects GT annotations rather than scoring construction itself, so a suite that scores *learned* graph-construction accuracy and regresses it against answer accuracy on one probe stays open (H5) |
| No benchmark plots the *implicit-vs-explicit 4D* cost/accuracy frontier at matched accuracy, nor traces the consistency-vs-perception delta over *time* and across rising difficulty | B2 | [[2603.22078\|WAM-vs-VLA-Robustness]] already plots the implicit-VLA-vs-explicit-generation-WAM SR×latency frontier (WAMs ≥4.8× slower than π0.5's 63 ms/chunk) over a shared LIBERO-Plus + RoboTwin-2.0-Plus suite, and [[2510.13626\|LIBERO-Plus]] supplies the factor-decomposed OOD axis; the H3 traces-help-OOD arm is now instrumented by [[2605.13632\|GTA-VLA]]'s SimplerEnv-Plus (camera/lighting/unseen-object/language OOD: unseen-object 58.3% vs 36.2%, trace-guidance recovers ~20% of failures) — the named OOD-SimplerEnv suite the implicit-only-vs-traces-only-vs-both test lacked; but those suites test OOD only on spatial/visual perturbation (7 isolated factors / 21 sub-dims) — they lack a *temporal*-OOD substrate ([[2506.19816\|CronusVLA]]'s 24 temporal+spatial disturbances × 120 severities) and a *graded-difficulty* axis ([[2602.06556\|LIBERO-X]]'s 5 progressive levels), and none plots the frontier with *perception held fixed* (the consistency-as-driver isolation H1 needs), only architecture-family comparisons |
| No manipulation benchmark stress-tests *long-horizon geometric coherence* of occupancy vs latent at sub-cm resolution, nor scores *native-4D-at-deployment vs lift-after-pixel* latency-fidelity jointly, nor whether the latent-4D substrate transfers WM↔policy (H6/H7) | C1 | [[2604.19092\|RoboWM-Bench]] supplies the manipulation rollout substrate (step+final SR, discriminative beyond perceptual metrics) and [[2604.22152\|dWorldEval]] the horizon-DRIFT metric (round-trip LPIPS@20-step), but both score pixel/video drift. H2's "nobody ran a planner on a manipulation occupancy grid" is *narrowed* by [[2509.05314\|ManipDreamer3D]], which plans A*+CHOMP on a reconstructed 64³ manipulation occupancy grid — yet only to guide single-image video synthesis, never closing an occupancy rollout loop nor running the occupancy-vs-latent horizon comparison. No benchmark measures sub-cm *occupancy*-geometric horizon-to-divergence at tabletop scale (all 3D occupancy-forecasting suites are driving — Cam4DOcc/UniOcc/Occ3D-nuScenes — or static indoor — Occ-ScanNet/RoboOcc, no temporal horizon); the sub-cm occupancy variant (H4 + occupancy half of H1) is a true field gap. For the latency-fidelity and WM→policy-transfer gap (H6/H7), [[2603.03485\|Phys4D]] scores geometry-over-horizon (per-frame AbsRel → 4D-Chamfer → Worldline-L2) and [[2602.08971\|WorldArena]] scores WM functional utility (action-planner / policy-evaluator), with [[2406.02523\|RoboCasa]] the in-dist SR substrate; [[2606.13877\|ContactWorld]] corroborates on contact-rich tasks where explicit-spatial (point-cloud) WMs beat image-substrate WMs on planning SR (32.1% vs 20.7%, +tactile 36.1%) — but it scores planning success, not native-vs-recovered Chamfer fidelity; so the *single-system matched-backbone* native-4D-at-deploy vs lift-after-pixel head-to-head scoring Chamfer-over-horizon + SR + latency on one plane stays method-internal |
| No benchmark isolates *6-DoF-extraction accuracy from predicted vs sensed geometry* across viewpoints | C2 | [[2604.19092\|RoboWM-Bench]] (executability-vs-perceptual discrimination, step+final SR) and [[2602.08971\|WorldArena]] (perception–functionality gap) provide the discriminative predicted-video→action SR axis the headline needs, but neither isolates 6-DoF-extraction-accuracy from predicted vs sensed geometry across viewpoints. [[2507.00990\|RIGVid]] sharpens the gap: it is the RGB-only-video + off-the-shelf-tracker (FoundationPose) baseline whose stated monocular-depth bottleneck is precisely H2's predicted-pointmaps-vs-sensed-depth concern, so H1's consistency-ablation should add an RGB-video+FoundationPose variant alongside the single-view-RGB-D + learned-estimator variant — but the predicted-vs-sensed pose-readout isolation (H2/H3) itself remains a genuine field gap (no standardized suite scores it) |
| No benchmark scores *persistent geometric + episodic memory* on memory-dependent manipulation over minute-scale horizons, model-agnostic across substrates | C3 | [[2602.08025\|MIND-Bench]] is the model-agnostic memory-CONSISTENCY benchmark (closed-loop revisit-coherence + action control at the WM substrate level) and [[2605.10921\|RoboMemArena]] the action-conditioned-manipulation demand-side anchor; [[2606.09803\|Echo-Memory]] supplies the memory-isolated head-to-head (camera-revisit + semantic-identity only), and [[2603.04639\|RoboMME]] now decomposes memory *by representation type* (symbolic/perceptual/recurrent over Counting/Permanence/Reference/Imitation, 16 tasks × 1,600 demos) on action-conditioned manipulation — the per-representation comparison H1's layer-reuse and H3's per-subtask splits need ("no single representation dominates") — but it scores per-representation SR, not the geometric-vs-episodic *compound* (H2) nor geometric coherence; and MIND is scene-navigation (not manipulation at sub-cm), so no suite instantiates the *same* memory layer over occupancy (C1) / pointmaps (C2) / latent-4D ([[2604.26694\|X-WAM]]) substrates, and no world-frame-vs-robot-frame drift suite exists for H4 |
| No benchmark scores *scene* reconstruction on *interaction-readiness* (the four criteria) rather than fidelity — readiness is a checklist with no leaderboard | D1 | [[2505.17966\|Single-View-Mesh-for-Robotics]] is the closest readiness-CRITERIA protocol (5 desiderata, 12 recon models, YCB-Video + Aria, per-criterion pass/fail + ~50% grasp transfer) but object-level with no readiness-OPTIMIZING pipeline, and [[2604.05621\|FunRec]]'s RealFun4D the closest scene-level articulation-readiness suite (127 seqs / 12 OmniGibson, URDF/USD-exportable). The readiness-optimizing-pipeline slot is now partly filled — [[2602.10116\|SAGE-Scene]]'s agentic generate-then-validate (Physics Critic, 99.9% stability), [[2601.03200\|3DGS-Digital-Twin]]'s post-hoc collision-meshing (Chamfer 0.0020, 90% real SR), and [[2608.11876\|D3D-GEN]]'s constraint-database-grounded generation (exact room-count fidelity, code-compliant placement) — but all three validate readiness as a generation/conversion side-objective (physics-critic, collision-mesh, or domain-constraint compliance, never all four criteria at once); the residual gap is a standardized scene-level four-criteria leaderboard running the *controlled* PSNR-vs-readiness dual-objective comparison ([[2604.25459\|GS-Playground]]/[[2511.04665\|Real-to-Sim-GS]] grade transfer, not readiness) |
| No benchmark scores *per-object* physical-asset generation uniformly across *rigid + deformable + articulated* classes — readiness metrics are class-siloed | D2 | The only cross-class readiness scorecard is PhysX-Bench, shipped by the [[2605.21572\|PhysX-Omni]] method itself (kinematic 0.92, rigid/articulated-leaning); the deformable leg is now *category-generalizable* via [[2606.16202\|EgoPhys]] (codebook prior, zero-shot on 11 unseen objects, Chamfer 0.015 vs [[2503.17973\|PhysTwin]] 0.024 with real xArm6 transfer) rather than the per-instance [[2503.17973\|PhysTwin]]/[[2605.09538\|PhysHanDI]] twins. The closest *standardized* 3D-gen benchmark is fidelity-only ([[2503.21745\|3DGen-Bench]] human-preference/3DGen-Score) — which scores appearance fidelity, the very axis D2's bet argues against; so the residual gap is a standardized *cross-class* (rigid+deformable+articulated) readiness leaderboard, not the absence of a generalizable deformable generator |

---

## Cross-References

> [!tip] Focus-Direction tie-in
> The coupling term $M_{\text{base,arm}}$ in [[Focus-Direction|Focus-Direction]] is irreducibly geometric. **A1 (point-cloud-native action heads) plugs into [[Focus-Direction|Focus-Direction]] under WB-A1's representation layer, and is the policy-side twin of WAM-A2's wrench imagination** — an explicit-geometry representation enabler under the anchor, not a 5th corner. A1 supplies the metric-3D state on which the explicit coupling head operates; the direction's four corners (WB-A1 anchor / WAM-A2 predict / Sim2Real-B2 ground / EAI-B1 verify) are unchanged.

**Sibling research-direction docs:**
- [[WAM|WAM]] — the WAM-machinery sibling. WAM owns the machinery (latent/architecture substrate, training, grounding); [[2604.26694|X-WAM]]'s natively-4D imagination (folded into C1 as its latent-4D comparator) and C3 ([[2603.17117|MosaicMem]], [[2603.24576|Chameleon-Episodic-Memory]] geometric memory) are the *representations* any policy reuses. Cluster C's explicit-vs-latent split sits *inside C1's card*, not across the WAM boundary.
- [[Sim2Real|Sim2Real]] — A1/B1 ([[2604.25459|GS-Playground]] 90% real SR, [[2511.04665|Real-to-Sim-GS]] r=0.915) own the *transfer-gap* face of 3DGS reconstruction; this doc's D1 owns the *interaction-readiness* face. Cross-reference, never re-own.
- [[Embodied-AI|Embodied-AI]] — the umbrella; cross-cutting joint-evaluation and cross-embodiment directions live there.
- [[Whole-Body|Whole-Body]] — WB-A1's coupled-dynamics action model consumes A1's geometric representation (see Focus-Direction tie-in).
- [[Focus-Direction|Focus-Direction]] — the four-corner focused direction A1 plugs into under WB-A1's representation layer.

**Deep-dives:**
- [[../Embodied-AI/07_Latent-World-Models|07_Latent-World-Models]] — the latent-vs-explicit substrate debate Cluster C extends to the explicit-geometry side.
- [[../Embodied-AI/08_Physics-Aware-Embodied-AI|08_Physics-Aware-Embodied-AI]] — physical parameterization (D1, D2) and physics-grounded geometry.
- [[../Embodied-AI/05_VLA-Reasoning-and-CoT|05_VLA-Reasoning-and-CoT]] — the spatial-CoT cognition layer (Cluster B).
- [[../Embodied-AI/04_VLA|04_VLA]] — the RGB-token policy baselines Cluster A inverts.
- [[../Embodied-AI/02_Dataset-Benchmark-Environment|02_Dataset-Benchmark-Environment]] — the benchmark suites and simulation environments cited throughout.

**General topics:**
- [[../General/02_Computer-Vision-and-3D|02_Computer-Vision-and-3D]] — 3D-understanding foundations.
- [[../General/11_Robotics-and-Embodied-AI|11_Robotics-and-Embodied-AI]] — the policy / world-model / manipulation papers.
- [[../General/07_Reasoning-and-Planning|07_Reasoning-and-Planning]] — spatial reasoning and CoT (Cluster B).
- [[../General/12_Benchmarks-and-Surveys|12_Benchmarks-and-Surveys]] — the five anchor surveys.
